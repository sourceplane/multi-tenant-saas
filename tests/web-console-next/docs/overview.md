# web-console-next-tests

Verification suite for `web-console-next`.

A verify-only component: its lane runs this suite on every plan that includes it. Nothing deploys from here — a red lane blocks the convergence, which is the point.

## Depended on by

- (none)
