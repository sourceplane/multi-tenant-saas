apiVersion: sourceplane.io/v1
kind: Component
metadata:
  name: metering-worker
  description: Cloudflare Worker for the Metering API surface (usage recording, quota checks)
spec:
  type: cloudflare-worker-turbo
  domain: starter-runtime
  path: apps/metering-worker
  dependsOn:
    - component: membership-worker
    - component: policy-worker
  subscribe:
    environments:
      - name: dev
        profile: verify
      - name: stage
        profile: verify
        profileRules:
          - profile: deploy
            when:
              triggerRef: github-push-main
      - name: prod
        profile: verify
        profileRules:
          - profile: deploy
            when:
              triggerRef: github-push-main
  parameters:
    nodeVersion: "22"
    pnpmVersion: "10.12.1"
    workspaceDir: ../..
    appDir: .
    turboFilter: "@saas/metering-worker"
    wranglerConfig: wrangler.jsonc
    installCommand: pnpm install --frozen-lockfile
    buildCommand: pnpm exec turbo run build --filter=@saas/metering-worker
    typecheckCommand: pnpm exec turbo run typecheck --filter=@saas/metering-worker
    dryRunCommand: pnpm exec wrangler deploy --dry-run --config wrangler.jsonc --env prod
    deployCommand: pnpm exec wrangler deploy --config wrangler.jsonc --env prod
    preDeployCommand: "echo 'No pre-deploy step configured.'"
    migrationCommand: "echo 'No migrations configured.'"
    smokeCommand: "echo 'No smoke test configured.'"
  labels:
    team: platform
    layer: runtime
    surface: metering
    runtime: cloudflare
