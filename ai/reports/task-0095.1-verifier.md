# Task 0095 / 0095.1 Verifier Report — PASS / B3 LIVE

Date: 2026-05-30
PR: #143 — Task 0095 — Edge idempotency replay store (B3)
Branch: `impl/task-0095-edge-idempotency-replay-store` (deleted post-merge)
Branch HEAD at verification: `1bcb1946957623109c274f43f3bbf854933f345b`
Squash-merge SHA on main: `d9116aa`
Main-CI run: https://github.com/sourceplane/multi-tenant-saas/actions/runs/26684916084

This report supersedes the prior FAIL at `ai/reports/task-0095-verifier.md`. The Phase-5 blocker (placeholder KV IDs + no substitution mechanism) was closed by:

1. PR #147 (Task 0095.0) — provisioned the real Cloudflare Workers KV namespaces via the carved-out `infra/terraform/cloudflare-kv/` component and merged it to main.
2. PR #143 fix-up commit `1bcb194` — wired the real 32-char hex IDs into `apps/api-edge/wrangler.jsonc`, added an `EXPECTED_KV` guard to `apps/api-edge/scripts/verify-bindings.mjs`, and dropped the duplicate `infra/terraform/cloudflare-kv/**` slice (now owned by main).

## Phase-by-phase verdict

| Phase | Verdict | Notes |
|---|---|---|
| 1 — Repo / PR sanity | PASS (sealed from prior run) | minor task-doc bleed-through, not a blocker |
| 2 — Hazard + boundary scan | PASS (sealed) | no eslint-disable / @ts-ignore / @ts-expect-error / `as unknown as` |
| 3 — Local gates | PASS (sealed) | typecheck / lint / 282/282 tests / terraform validate all green |
| 4 — `replayOrExecute` code path | PASS (sealed) | envelope v1, allowlist, base64, 4xx cache, 5xx no-cache, degrade-open |
| 5 — Wrangler binding | **PASS (re-run)** | real KV IDs + verify-bindings guard + carve-out clean |
| 6 — Merge | **PASS** | squash-merged with `--admin` (BEHIND base, CI green); merge SHA `d9116aa` |
| 7 — Post-merge main-CI | **PASS** | api-edge × {dev, stage, prod} Verify-deploy SUCCESS; deploy logs show real KV IDs bound |
| 8 — Live evidence | **PASS** | cases (a, b, c, d, f, g) on stage; (a, isolation, g) on prod; (e) `needs human` (no controlled 500 path on no-auth route) |
| 9 — Console smoke | **PASS** | stage + prod consoles 307 → /orgs unchanged |
| 10 — State files | **PASS** | this commit |

## Phase 5 — Wrangler binding (re-run)

apps/api-edge/wrangler.jsonc:
- `env.stage.kv_namespaces[*].binding=IDEMPOTENCY_KV` → id `2f5a03d0a14e4ead8f2b6658f6bfd722` ✓
- `env.prod.kv_namespaces[*].binding=IDEMPOTENCY_KV`  → id `fac1d319c8894466b4860bff9c6cb99d` ✓
- No occurrences of `0000000000000000000000000000000a` / `…000b` anywhere

apps/api-edge/scripts/verify-bindings.mjs:
- `EXPECTED_KV` block present, `KV_ID_PATTERN=/^[0-9a-f]{32}$/`, `KV_ID_SENTINELS` set with both sentinels
- Four-step assertion: binding present → regex match → sentinel reject → success log
- `node apps/api-edge/scripts/verify-bindings.mjs` exit 0 with both real IDs printed

`git diff --stat origin/main...HEAD -- infra/terraform/cloudflare-kv/` empty (carve-out clean).

## Phase 7 — Post-merge main-CI

Run id 26684916084, branch=main, conclusion=success.

Per-job: `plan` ✓, `api-edge-tests · dev · Verify` ✓, `api-edge · dev · Verify deploy` ✓, `api-edge · stage · Verify deploy` ✓, `api-edge · prod · Verify deploy` ✓.

Wrangler deploy log evidence (provider-side proof of real KV binding):
- stage: `env.IDEMPOTENCY_KV (2f5a03d0a14e4ead8f2b6658f6bfd722) KV Namespace`
- prod:  `env.IDEMPOTENCY_KV (fac1d319c8894466b4860bff9c6cb99d) KV Namespace`

Note: The Cloudflare REST `/storage/kv/namespaces` listing was not directly executed in the verifier shell (creds empty), but Cloudflare validates KV bindings server-side at deploy time — sentinel/invalid IDs would have failed the deploy. Live replay traffic in Phase 8 additionally exercises KV reads/writes end-to-end, so KV operability is provably confirmed.

## Phase 8 — Live evidence

Stage `https://api-edge-stage.rahulvarghesepullely.workers.dev`:

| Case | Result | Evidence |
|---|---|---|
| (a) hit replay | PASS | 2nd call: `x-saas-replay-source: edge-idempotency`, byte-identical body (challengeId chl_d85fb39…, code 585709, requestId req_d2ead6e…), latency 4822ms → 153ms |
| (b) miss-then-store | PASS | fresh key, 5s gap, replay header + identical body (chl_5b2e632…) |
| (c) GET passthrough | PASS | 2× GET /v1/auth/session same Idempotency-Key → no replay header, fresh requestIds |
| (d) 4xx cached (422) | PASS | invalid-email → 422; 2nd call has `x-saas-replay-source` and identical requestId |
| (e) 5xx not cached | NEEDS HUMAN | no controlled 500 path on no-auth route on stage; covered by Phase-4 unit tests |
| (f) identity-agnostic key | PASS | second call without bearer/cookie still replays first call's stored envelope |
| (g) header allowlist | PASS | replayed response carries no Authorization / Set-Cookie / Cookie headers |

Prod `https://api-edge-prod.rahulvarghesepullely.workers.dev`:

| Case | Result |
|---|---|
| (a) hit replay | PASS — `x-saas-replay-source: edge-idempotency` on 2nd call, identical body |
| (isolation) | PASS — stage's used key on prod returns fresh response, no replay header (different KV namespaces confirmed) |
| (g) header allowlist | PASS — no auth/cookie leakage on prod replay |

## Phase 9 — Console smoke

- `curl -sI https://stage.sourceplane.ai/` → HTTP/2 307, location: /orgs ✓
- `curl -sI https://prod.sourceplane.ai/`  → HTTP/2 307, location: /orgs ✓

## Open risks

1. **Cloudflare REST listing not executed** — phantom-binding failure mode not directly listed. Mitigation: live (a)/(b)/(d)/(g) replay traffic exercises KV reads/writes end-to-end.
2. **Body-mismatch policy is "key-only / Stripe parity"** — same key + different body returns the cached envelope rather than a 4xx mismatch error. Intentional per Phase-4 sealed behavior; documented for downstream consumers.
3. **5xx-not-cached case (e) unverified live** — relies on Phase-4 unit-test coverage.
4. **Merge required `--admin`** — branch was BEHIND base; standard repo practice per spec.

## Final B3 status: LIVE on stage and prod

Real KV namespaces bound, replay traffic verified end-to-end (hit, miss-then-store, GET passthrough, 4xx cached, header allowlist on both envs, stage/prod isolation).

## Deviations

1. Cloudflare REST API curl skipped — env vars empty in verifier shell.
2. `wrangler kv namespace list` not run — deploy-log evidence used instead.
3. Phase 8 case (e) marked "needs human" — no controlled 500 path on no-auth route.
4. Merge used `--admin` (BEHIND base) — spec-allowed.
