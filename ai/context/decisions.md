# Decisions

Last updated: 2026-05-20

## Active Decisions

- Use `specs/**` as the authoritative spec pack for current work. Do not read or apply `specs-v2/**` unless the task is product-specific Git catalog or CI intelligence work.
- Treat the repo-relative `ai/` directory as the active planning state location for this checkout. The orchestrator text uses `/ai/...` paths conceptually, while `agents/agent-loop.sh` reads `ai/state.json` and `ai/waiting_for_input.md` relative to the workspace.
- Use `specs/orun-golden-path.md` as the short shared Orun context for all implementer and verifier agents.
- Treat `../aws-admin` as the reference repo for Terraform-oriented Orun
  structure and backend behavior, while this repo currently uses the
  Task 0005-verified Orun runtime:
  - Orun runtime version `v2.2.1` in `multi-tenant-saas`;
  - `sourceplane/orun-action@v1.2.0`;
  - `dev`, `stage`, `prod` environments;
  - `parameterDefaults.terraform`;
  - Terraform components with `spec.parameters`, `plan-only` default profile, `apply` profile rules, colocated README docs, and S3 backend shape.
- AWS IAM roles and shared Terraform state buckets are owned by `aws-admin`. `multi-tenant-saas` consumes the repo-scoped role created there.
- Terraform state for `multi-tenant-saas` must use the shared S3 buckets `sourceplane-<env>`, not Cloudflare R2.
- Supabase database credentials and connection details must be generated by Terraform and stored in AWS Secrets Manager under `<org>/<repo>/<component>/<env>`.
- Task 0006 Supabase target is fixed: use Supabase organization `sourceplane`
  with slug/id `dwazxcrywsdbxpuouifa`, provision `stage` and `prod` only, leave
  `dev` unprovisioned, and create separate Supabase projects/databases named
  `multi-tenant-saas-stage` and `multi-tenant-saas-prod`.
- GitHub CI has `SUPABASE_API_KEY` for the Supabase account. Treat it as a
  secret and map it to the selected Supabase Terraform provider's expected
  access-token input without logging it.
- Cloudflare/Supabase human-provided resources discovered in Task 0002 remain useful historical context, but new infrastructure work follows the AWS/S3/Secrets Manager path unless a later spec explicitly says otherwise.
- Composition source is `kind: dir` (local `stack-tectonic/` directory), not the published OCI image. This ensures composition changes are validated in-repo before publishing.
- All component types (terraform, turbo-package, cloudflare-*) use `spec.parameters` instead of `spec.inputs` on Orun v2.

## Pending Decisions

- Exact Supabase project refs are assigned by Supabase at apply time and must be
  recorded as non-secret outputs/report values after Task 0006 creates the
  `stage` and `prod` projects.

## Verified Inputs

- Task 0004 verified and merged the `aws-admin` IAM component for `sourceplane/multi-tenant-saas`.
- Verified plan role ARNs:
  - `arn:aws:iam::306024784101:role/dev-github-sourceplane-multi-tenant-saas-plan`
  - `arn:aws:iam::306024784101:role/stage-github-sourceplane-multi-tenant-saas-plan`
  - `arn:aws:iam::306024784101:role/prod-github-sourceplane-multi-tenant-saas-plan`
- Verified deploy role ARNs:
  - `arn:aws:iam::306024784101:role/dev-github-sourceplane-multi-tenant-saas-production-deploy`
  - `arn:aws:iam::306024784101:role/stage-github-sourceplane-multi-tenant-saas-production-deploy`
  - `arn:aws:iam::306024784101:role/prod-github-sourceplane-multi-tenant-saas-production-deploy`
