# Task 0082.1 — Verifier

Agent: Verifier

## Current Repo Context

- Task 0082 (Implementer) opened PR #125 (`impl/task-0082-web-console-next`)
  introducing greenfield `apps/web-console-next/` (Next.js 15 App Router).
  Verifier first pass FAILED at `verify-build-output` because the orun
  `cloudflare-pages-turbo` contract expects `outputDir: .open-next/assets`
  but plain `next build` only emits `.next/`. See
  `ai/reports/task-0082-verifier.md`.
- Task 0082.1 (Implementer follow-up, commit `e505677`) wired
  `@opennextjs/cloudflare@1.0.4` (with pinned `@opennextjs/aws@3.6.2`),
  added `output: "standalone"` in `next.config.mjs`, added per-package
  `apps/web-console-next/turbo.json` extending `//` with `.open-next/**` +
  `.next/**` outputs, added `open-next.config.ts` (dummy cache) and a
  static `wrangler.jsonc`, and changed the build script to
  `next build && opennextjs-cloudflare build --skipBuild
  --skipWranglerConfigCheck`. Report:
  `ai/reports/task-0082.1-implementer.md`.
- Earlier verifier-applied prerender fix (`875e6e6`) is retained: root
  `apps/web-console-next/src/app/layout.tsx` exports
  `dynamic = "force-dynamic"`.
- **PR #125 CI is now GREEN.** Run at head
  `f38169545ae6aaa2a903297cf4811c78dd56fae3`: `plan = SUCCESS`,
  `web-console-next · {dev,stage,prod} · Verify deploy = SUCCESS`.
  `mergeStateStatus = CLEAN`.
- `apps/web-console` (vanilla Vite console) is byte-identical to
  `origin/main`. Diff outside `apps/web-console-next/**` is limited to
  `ai/reports/**` and `ai/tasks/task-0082.md` (and `pnpm-lock.yaml`).
- ⚠️ **Suspected scope issue to investigate**: `git diff
  --name-only origin/main...impl/task-0082-web-console-next` shows ~173
  files under `apps/web-console-next/.next/**` and
  `apps/web-console-next/.open-next/**` — these are local build
  artifacts. The repo has no `.gitignore` entry covering them for the
  new app. Confirm and remediate (see Acceptance §6).

## Objective

Verify PR #125 end-to-end as the now-complete Task 0082 deliverable
(0082 implementation + 0082 verifier prerender fix + 0082.1 implementer
opennextjs wiring). PASS requires:

- Code-level acceptance criteria from `ai/tasks/task-0082.md` and
  `ai/tasks/task-0082-verifier.md` still hold.
- PR CI is green on the head commit (already true at scoping time —
  re-confirm after any verifier commit).
- No committed build artifacts (`.next/`, `.open-next/`, `node_modules/`).
- No raw secrets, no widening of root `turbo.json`, no changes outside
  `apps/web-console-next/**` (+ `ai/reports/**`, `pnpm-lock.yaml`).

If PASS, merge PR #125, sync local `main` to `origin/main`, leave the
repo clean. Then plan immediately progresses to Task 0083 (Pages cutover
from `apps/web-console` → `apps/web-console-next`).

## PR Boundary

The verifier may commit a single scoped fix to the PR branch confined
to:

1. `apps/web-console-next/.gitignore` (NEW) — to ignore build artifacts.
2. `git rm --cached` the already-committed `apps/web-console-next/.next/**`
   and `apps/web-console-next/.open-next/**` paths.
3. `ai/reports/task-0082.1-verifier.md` (NEW) — this report.

The verifier MUST NOT:

- Modify any worker, contract, db, policy, api-edge, or Terraform file.
- Touch `apps/web-console`.
- Modify root `turbo.json`, root `package.json`, or any other app.
- Change `component.yaml`, `outputDir`, `smokeCommand`, `installCommand`,
  `buildCommand`, or the deploy model.
- Bump `next`, `react`, `@opennextjs/cloudflare`, or `@opennextjs/aws`
  versions.

If those constraints cannot be met to drive the build-artifact cleanup,
the verifier records FAIL with explicit blockers and leaves the PR open.

## Read First

- `agents/orchestrator.md` — Verifier Standard and Merge Protocol.
- `ai/tasks/task-0082.md` — original implementer scope and acceptance.
- `ai/tasks/task-0082-verifier.md` — verifier latitude already granted.
- `ai/reports/task-0082-implementer.md` — first implementer report.
- `ai/reports/task-0082-verifier.md` — first verifier report (FAIL).
- `ai/reports/task-0082.1-implementer.md` — follow-up implementer report.
- `specs/orun-golden-path.md` — component manifest + validation flow.
- PR #125 diff at current head.

## Required Outcomes

- [ ] Re-confirm CI green at PR head: `plan`,
      `web-console-next · {dev,stage,prod} · Verify deploy` all SUCCESS.
- [ ] Confirm scope: diff outside `apps/web-console-next/**` limited to
      `ai/reports/**`, `ai/tasks/task-0082.md`, and `pnpm-lock.yaml`.
- [ ] Confirm `apps/web-console/**` is byte-identical to `origin/main`.
- [ ] Locally run `pnpm --filter @saas/web-console-next install`,
      `typecheck`, `lint`, `build` (must emit `.open-next/assets/**` with
      ≥1 file).
- [ ] Locally run `/Users/irinelinson/.local/bin/kiox -- orun validate
      --intent intent.yaml`, `orun plan --changed --intent intent.yaml
      --output plan.json`, and `orun run --plan plan.json --dry-run
      --runner github-actions`.
- [ ] Confirm `apps/web-console-next/component.yaml` is unchanged vs
      Task 0082 (declared `outputDir: .open-next/assets`).
- [ ] Confirm no secrets, tokens, or live credentials in diff, screenshots,
      or report.
- [ ] Confirm no commit of `.next/` or `.open-next/` build artifacts. If
      already committed: add `.gitignore`, `git rm --cached`, push, wait
      for CI green.
- [ ] PR `mergeStateStatus = CLEAN` before merge.
- [ ] On PASS: squash-merge PR #125, checkout `main`, `git pull --ff-only`,
      delete local branch, `git status --short` clean.
- [ ] Write `/ai/reports/task-0082.1-verifier.md`.

## Non-Goals

- No Task 0083 (Pages project repoint / domain cutover) work.
- No changes to `apps/web-console`.
- No version bumps of Next/React/opennextjs adapters.
- No new policy, db, worker, contract, or infra component changes.
- No widening of root `turbo.json`.

## Constraints

- Verifier-applied commits must be one focused commit each, with one-line
  rationale in the message body.
- Never merge while any required check is FAILURE or PENDING.
- Never merge while `mergeStateStatus != CLEAN`.
- No raw bearer tokens, API keys, or live credentials in commits or
  reports.

## Integration Notes

- `pnpm-lock.yaml` was updated by Task 0082.1 to pin
  `@opennextjs/aws@3.6.2` — confirm `pnpm install --frozen-lockfile`
  exits 0.
- Task 0082.1's per-package `apps/web-console-next/turbo.json` extends
  `//` and adds `.open-next/**` + `.next/**` to `build.outputs`. This is
  what makes `verify-build-output` succeed — confirm root `turbo.json`
  is untouched.
- After merge, the post-merge `main` CI run will exercise the deploy
  profile (not just verify), which will auto-create the per-env Pages
  projects via the existing `ensure-pages-project` step. That run is
  Task 0083's prerequisite, not 0082.1's.

## Acceptance Criteria

1. PR #125 head CI: `plan = SUCCESS`, all three
   `web-console-next · {dev,stage,prod} · Verify deploy = SUCCESS`.
2. PR diff is contained to `apps/web-console-next/**`, `ai/reports/**`,
   `ai/tasks/task-0082.md`, `pnpm-lock.yaml`.
3. No `.next/` / `.open-next/` build artifacts committed.
4. Local build emits `apps/web-console-next/.open-next/assets/**` (≥1 file).
5. Local `orun validate` / `plan --changed` / `run --dry-run` all pass and
   show `web-console-next.{dev,stage,prod}.verify-deploy`.
6. PR `mergeStateStatus = CLEAN`.
7. Verifier report `ai/reports/task-0082.1-verifier.md` written with
   Result, Checks, Issues, Risk Notes, Spec Proposals, Recommended Next
   Move, and final PR Number line.
8. If PASS: PR squash-merged; local `main` fast-forwarded; branch
   `impl/task-0082-web-console-next` deleted locally; `git status
   --short` clean.

## Verification

Follow `agents/orchestrator.md` Verifier Standard + Merge Protocol
verbatim. Run the kiox/orun trio. Inspect `gh run view <runId>
--log` for the most recent run if any check ambiguous.

## PR Creation Requirement

The Implementer has already created PR #125. The verifier's job is to
verify it, optionally commit a single scoped build-artifact cleanup, and
merge on PASS. No new PR is required.

## When Done Report

Write `/ai/reports/task-0082.1-verifier.md` with sections:

- Result: PASS | FAIL
- Summary
- Checks (table)
- CI Log Review
- Verifier Fix (if any)
- Secret Handling Review
- Issues
- Risk Notes
- Spec Proposals
- Live Resource Evidence (verify-deploy outputs and post-merge main run id)
- Recommended Next Move
- PR Number
