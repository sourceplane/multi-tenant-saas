# Task ID

Task 0001.1

# Agent

Verifier

# Current Repo Context

Task 0001 is verified and merged into `main` at `4d38ded`. PR #5 (`codex/human-01` -> `main`) contains follow-up human/orchestrator changes plus the Task 0001.1 implementer commit `83e945c`.

Task 0001.1 was created because PR #5 CI failed in `orun plan --changed --intent intent.yaml --output plan.json` after the repo switched to a committed local Stack Tectonic catalog. The failure was component input schema drift: stale descriptor inputs such as `lintCommand`, `testCommand`, and `outputDirectory` no longer matched `stack-tectonic/compositions/**/compositions.yaml`.

The implementer report is `ai/reports/task-0001.1-implementer.md`. The latest PR #5 CI run `25658811319` now shows:

- `plan`: success
- `matrix.job-name`: skipped because `orun plan --changed` produced `0 components x 3 envs -> 0 jobs`

# Objective

Verify PR #5 for Task 0001.1, focusing on whether the component descriptors now align with the committed local Stack Tectonic schemas and whether the PR is safe to merge.

# PR Boundary

Verify the existing PR #5 branch `codex/human-01`. Do not broaden scope into Task 0002 infrastructure provisioning, product behavior, or local stack schema redesign.

If verification-only fixes are required, keep them minimal, commit them to PR #5, push, and re-check CI before deciding PASS/FAIL.

# Read First

- `agents/orchestrator.md`
- `ai/tasks/task-0001.1.md`
- `ai/reports/task-0001.1-implementer.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/repo.md`
- `specs/components/00-foundation-and-tooling.md`
- `intent.yaml`
- `kiox.yaml`
- `.github/workflows/ci.yml`
- `stack-tectonic/compositions/cloudflare-worker-turbo/compositions.yaml`
- `stack-tectonic/compositions/cloudflare-pages-turbo/compositions.yaml`
- `stack-tectonic/compositions/turbo-package/compositions.yaml`
- Changed `component.yaml` files under `apps/`, `packages/`, `tests/`, and `infra/`.

# Required Outcomes

- Confirm PR #5 preserves the intentional local Stack Tectonic model: committed `stack-tectonic/`, `intent.yaml` with `kind: dir` / `path: stack-tectonic/`, provider-style `kiox.yaml`, committed `kiox.lock`, and Orun-only GitHub Actions.
- Confirm every changed repo `component.yaml` uses only inputs accepted by the relevant local schema.
- Confirm required inputs are present:
  - `apps/web-console/component.yaml`: `outputDir`, `projectName`, `nodeVersion`, `pnpmVersion`, and `productionBranch`
  - `apps/api-edge/component.yaml`: `nodeVersion`, `pnpmVersion`, and `productionBranch`
  - `turbo-package` components: `nodeVersion` and `pnpmVersion`
- Confirm unsupported stale inputs are gone unless the local schema supports them in the same PR: `lintCommand`, `testCommand`, and `outputDirectory`.
- Confirm test intent for `tests/contracts` is preserved in a schema-compatible way.
- Inspect the generated `.orun/component-tree.yaml` and `.orun/compositions.lock.yaml` for deterministic, expected changes only.
- Inspect PR #5 CI logs, not only status summaries. Verify the plan job ran `orun plan --changed --intent intent.yaml --output plan.json`, produced a zero-job plan, and skipped the matrix for that reason.
- Decide whether the remaining lint/test/typecheck-gate gaps are acceptable residual risk for this stabilization task or require a blocking fix/proposal before merge.

# Non-Goals

- Do not create or mutate Cloudflare, Supabase, Hyperdrive, R2, Wrangler, or Terraform resources.
- Do not implement Task 0002 infrastructure provisioning.
- Do not redesign the local Stack Tectonic schemas unless it is the minimal fix required for Task 0001.1 correctness.
- Do not apply product-specific `specs-v2/**`.

# Constraints

- Trust the committed local `stack-tectonic/` schemas over stale descriptor assumptions.
- Preserve user/human changes on PR #5 unless they directly block verification.
- Do not merge with unresolved verification blockers.
- If PASS, merge PR #5, sync local `main` to `origin/main`, and leave the local repo clean except for intentionally separate orchestrator state if it is not part of PR #5.
- If FAIL, leave PR #5 open and make blockers concrete.

# Integration Notes

- The latest successful CI run is `25658811319`, head SHA `83e945c9829b1e5647fbfcc937d397ff41b42312`.
- The current zero-job plan is expected for the PR branch as reported by the implementer, but it means GitHub did not exercise matrix jobs. Treat that as a risk note unless local changed-component simulation or a targeted test-only change proves the matrix path.
- `tests/contracts` maps test execution to `preBuildCommand` because `turbo-package` has no `testCommand` input in the current local schema.
- `web-console` does not have a `typecheckCommand` input because `cloudflare-pages-turbo` does not support it.
- `lintCommand` is unsupported by the current local schemas. This likely remains a future local-stack/schema quality-gate task, not a Task 0001.1 blocker unless the verifier finds that the PR silently drops an acceptance-critical gate with no acceptable fallback.

# Acceptance Criteria

- `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json` passes and produces a parseable plan.
- `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions` passes or records a true no-op result if no jobs are planned.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass locally unless an unrelated blocker is documented with exact output.
- PR #5 CI logs confirm the prior input schema validation failures are gone.
- The verifier report is written at `ai/reports/task-0001.1-verifier.md`.
- PASS requires merging PR #5 and syncing local `main`; FAIL requires clear unresolved blockers.

# Verification

Use both local checks and GitHub Actions evidence:

```bash
git status --short --branch
gh pr view 5 --json number,title,headRefName,baseRefName,headRefOid,mergeable,statusCheckRollup,url
gh run view 25658811319 --json databaseId,conclusion,headSha,jobs,url
gh run view 25658811319 --log --job 75313831890
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

If you merge, follow the verifier merge protocol from `agents/orchestrator.md`.

# When Done Report

Write `ai/reports/task-0001.1-verifier.md` with:

- Result: PASS|FAIL
- Checks
- Issues
- Risk Notes
- Spec Proposals
- Recommended Next Move
