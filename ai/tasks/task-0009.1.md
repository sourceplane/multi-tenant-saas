# Task ID

Task 0009.1

# Agent

Implementer

# Current Repo Context

The reusable SaaS starter spec pack under `specs/**` is authoritative for this
task. `specs-v2/**` is out of scope.

Current repo reality:

- Local `main` is synced with `origin/main` at `d9404ab`
  (`Task 0009 verification PASS: Hyperdrive infrastructure verified stable`).
- Task 0009 is verified PASS. `infra/terraform/cloudflare-hyperdrive/` is the
  current stage/prod Hyperdrive baseline.
- Hyperdrive live evidence:
  - stage: `08f7c6055f544a3890a585d88fd92348`
    (`stg-multi-tenant-saas-stage`)
  - prod: `ab2c21c2db6245a59c91588fcac7107a`
    (`prod-multi-tenant-saas-prod`)
- Code and CI now use Orun `v2.3.0`:
  - `kiox.yaml` pins `ghcr.io/sourceplane/orun:v2.3.0`
  - `kiox.lock` resolves Orun `v2.3.0`
  - `.github/workflows/ci.yml` invokes `sourceplane/orun-action@v1.2.0`
    with `version: v2.3.0`
- Active specs still describe `v2.2.1` as the current runtime reference.
- Task 0009 verifier accepted `ai/proposals/task-0009-spec-update.md` as the
  required follow-up before downstream Worker binding/runtime work resumes.

# Objective

Align the active specs and compact orchestration context with the Task
0009-verified Orun `v2.3.0` runtime baseline.

# PR Boundary

One PR updates documentation/spec/context only for the accepted Orun runtime
spec drift from Task 0009.

This PR may update:

- `specs/orun-golden-path.md`
- `specs/access-and-infra.md`
- `ai/proposals/task-0009-spec-update.md`
- compact orchestration files under `ai/context/**`
- `ai/state.json` and `ai/waiting_for_input.md`
- `ai/reports/task-0009.1-implementer.md`

Do not change runtime code, CI behavior, Terraform resources, component
manifests, Worker apps, package code, or live infrastructure.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/state.json`
- `ai/proposals/task-0009-spec-update.md`
- `ai/reports/task-0009-verifier.md`
- `specs/orun-golden-path.md`
- `specs/access-and-infra.md`
- `kiox.yaml`
- `kiox.lock`
- `.github/workflows/ci.yml`

# Required Outcomes

- Mark `ai/proposals/task-0009-spec-update.md` accepted by Task 0009
  verification / Task 0009.1 implementation.
- Update `specs/orun-golden-path.md` so the current repo runtime reference is
  Orun `v2.3.0`, while preserving `aws-admin` as the Terraform structure,
  backend, and environment-shape reference.
- Update `specs/access-and-infra.md` so the acceptance text no longer names
  `v2.2.1` as the current runtime.
- Update compact context so it no longer says Task 0009 verification is pending
  or that the `v2.3.0` proposal is pending acceptance.
- Keep the missing `dependsOn` edge as a documented non-blocking limitation or
  watch item, not a source change in this PR.
- Leave historical reports/tasks/proposals intact unless they are active
  context files. Historical mentions of earlier runtime versions are fine when
  clearly historical.
- Write `ai/reports/task-0009.1-implementer.md`.

# Non-Goals

- No Worker binding setup.
- No Worker runtime database adapter.
- No `packages/worker` restoration.
- No Wrangler deployment changes.
- No `kiox.yaml`, `kiox.lock`, or `.github/workflows/ci.yml` changes.
- No Terraform source changes.
- No component manifest `dependsOn` changes.
- No live Cloudflare, Supabase, AWS, S3, Secrets Manager, or Hyperdrive
  mutation.
- No broad documentation cleanup outside the Orun runtime drift.
- No changes to product-specific or reference-only folders such as
  `referance/**`.

# Constraints

- Trust current code and verified Task 0009 evidence over stale spec wording.
- Keep edits tightly scoped to the accepted `v2.3.0` spec/context alignment.
- Do not touch generated or ignored outputs such as `.orun/**`, `plan.json`,
  `node_modules/`, `dist/`, TypeScript build info, or Terraform working
  directories.
- Do not commit or log secrets.
- Do not edit historical reports just to remove old version numbers; those are
  evidence of prior tasks.
- A completed report must include a real PR number. `TBD` is not an acceptable
  complete state.

# Integration Notes

- Task 0009 verifier report: `ai/reports/task-0009-verifier.md`.
- Task 0009 proposal: `ai/proposals/task-0009-spec-update.md`.
- PR #44 reverted the later Worker binding/deployment attempts from PRs #37,
  #38, and #39. Do not reintroduce those changes here.
- `stack-tectonic/compositions/publish-stack/jobs/publish-stack-publish.yaml`
  has its own `sourceplane/orun-action@v1.1.0` reference for publishing; do not
  update it unless you prove it is part of this repo runtime contract. It is
  not expected to be in scope for this task.

# Acceptance Criteria

- `specs/orun-golden-path.md` names Orun `v2.3.0` as this repo's current
  runtime baseline and still points to `aws-admin` for Terraform structure and
  backend style.
- `specs/access-and-infra.md` acceptance names the current verified runtime
  generically or as `v2.3.0`, with no stale `v2.2.1` current-runtime wording.
- `ai/proposals/task-0009-spec-update.md` status is accepted.
- `ai/context/current.md`, `ai/context/decisions.md`, and
  `ai/context/open-risks.md` reflect:
  - Task 0009 PASS;
  - Orun `v2.3.0` accepted as current runtime;
  - downstream Worker binding/runtime work is now unblocked after this spec
    alignment PR, subject to normal task creation.
- `ai/state.json` points `task_agent` at this task or the latest report as
  appropriate, and does not leave the proposal as the active task agent.
- `rg -n "v2\\.2\\.1" specs ai/context ai/proposals/task-0009-spec-update.md`
  returns only historical/current-spec-explanation references, not stale active
  runtime guidance.
- Local verification passes:
  - `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  - `git diff --check`
- The PR contains only Task 0009.1-scoped docs/spec/context/report changes.

# Verification

Run the required local checks:

```bash
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
git diff --check
rg -n "v2\\.2\\.1|v2\\.3\\.0" specs ai/context ai/proposals/task-0009-spec-update.md
```

Because this task does not change Orun inputs, CI, Terraform, or component
manifests, a full Orun plan dry-run is optional. If any Orun-facing source is
changed accidentally, run:

```bash
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
```

# PR Creation Requirement

Create or reuse a branch for this task, commit only Task 0009.1-scoped changes,
push the branch, and open a GitHub PR before reporting completion.

The task is not complete if it is only implemented locally. If branch creation,
push, or PR creation is blocked, write the blocker in the report and set
`PR Number: BLOCKED` with the exact command/error.

# When Done Report

Write `ai/reports/task-0009.1-implementer.md` with:

- Summary
- Files Changed
- Checks Run
- Assumptions
- Spec Proposals
- Remaining Gaps
- Next Task Dependencies
- PR Number
