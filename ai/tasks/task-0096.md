# Task 0096

Agent: Implementer

## Current Repo Context

- main tip is `d94bf92` (orchestrator scope commit `chore(task-0095.1):
  orchestrator scope — verifier resumption prompt`, 2026-05-30). Behind
  that, `f7f5d19` is Task 0094 verifier PASS and `71cf34f` is Task 0094
  squash-merge. No source code has changed since Task 0094 closed.
- Tasks 0001–0094 verified and merged. Task 0095 (durable idempotency
  replay store, B3 partial) is mid-flight on PR #143
  (`impl/task-0095-edge-idempotency-replay-store`) and is in a verifier
  FAIL → Task 0095.1 fix-up cycle. Task 0095.1 is on the **same branch**
  as PR #143; the verifier resumption prompt is staged at
  `ai/tasks/task-0095.1-verifier.md` waiting on the implementer's
  fix-up commits.
- Task 0093 (Class-B lint cleanup wave 1, no-unused-vars 39→0) reduced
  the lint surface to **warnings only**. `pnpm -r --no-bail lint` exits
  0 across all 33 workspaces today.
- The residual warning surface is **644 warnings** across 12 workspaces
  (3 production-source workspaces + 9 test workspaces). Two warning
  rules: `@typescript-eslint/no-explicit-any` and `no-console`. Both are
  warning-only by design from `tooling/eslint/index.js` (the shared rule
  baseline).
- This task is **strictly disjoint from PR #143**. PR #143 touches
  `apps/api-edge/wrangler.jsonc`, `infra/terraform/cloudflare-kv/`,
  `apps/api-edge/scripts/verify-bindings.mjs` and reports/state files.
  Task 0096 touches `apps/config-worker/src/`,
  `apps/metering-worker/src/`, and `apps/webhooks-worker/src/` only —
  zero file overlap, zero PR-level merge conflict risk. The two tasks
  proceed in parallel.
- No deferred candidate is being touched (notifications-provider-swap,
  Task 0085b cloudflare-domain v4→v5, notifications-worker-dev-reframe
  stay parked per `/ai/deferred.md`).

## Read First

- `/specs/orun-golden-path.md`
- `/specs/eslint-baseline.md` (if present) — shared rule baseline
  contract; if absent, treat `tooling/eslint/index.js` as the source of
  truth.
- `ai/context/current.md`, `ai/context/task-ledger.md`,
  `ai/context/decisions.md`, `ai/context/open-risks.md`
- `ai/tasks/task-0093.md` (precedent for class-B mechanical cleanup —
  same shape: smallest possible diff, no behavioural change, no
  shared-rule edits, no eslint-disable).

## Architect Brief

- Product bar: **production source has zero lint warnings.** Test
  workspaces are allowed to carry test-harness `any` typing for now
  (separate future wave). Production source under `apps/*/src/**` is
  what runs in stage and prod and is what contributors read first when
  they explore the repo.
- User moment: a contributor runs
  `pnpm --filter "./apps/*" -r lint` and gets a clean green across every
  worker. Today they get 5 warnings across 3 workers (config-worker,
  metering-worker, webhooks-worker) — small enough to fix in one PR but
  large enough to feel sloppy.
- Implementer free choices (no spec proposal needed):
  - Per `as any` cast: replace with the narrowest viable type. The two
    `apps/config-worker/src/handlers/update-feature-flag.ts` casts at
    lines 139:85 and 213:87 are on the `updateFeatureFlag` /
    `createFeatureFlag` repo call argument — read the repo method
    signature in `packages/data/src/repos/feature-flags-repo.ts` (or
    wherever the method lives) and use the existing input type.
    Acceptable alternates: a `satisfies` clause, a `Partial<T>` over the
    repo input type, or restructuring `updateInput` to be typed from the
    start instead of `Record<string, unknown>`. NOT acceptable: `as
    unknown as SomeType` chained, casting to a fresh inline `{}` type, or
    re-declaring the repo input shape (use the canonical export).
  - Per `console.log` warning: replace with the appropriate allowed
    method (`console.warn` for transient/recoverable conditions and
    summary lines, `console.error` for hard failures). The three sites
    (`apps/metering-worker/src/rollups.ts:147`,
    `apps/webhooks-worker/src/index.ts:30` and `:36`) are all summary
    log lines after a scheduled task ran — `console.warn` is the right
    fit. If structured logging via a shared logger is already imported
    in the same file, prefer that (do not introduce a new logger import
    if it isn't already there — log shape is out of scope).
- Implementer must NOT make these decisions without a spec proposal:
  - Editing `tooling/eslint/index.js` (shared rule baseline) or any
    `eslint.config.js` re-export in any workspace.
  - Down-grading or up-grading `no-explicit-any` / `no-console`
    severity anywhere.
  - Touching test workspaces in this PR. The 639 warnings in
    `tests/**` are out of scope and are tracked for a future cleanup
    wave (Task 0096b candidate). Do not edit any file under `tests/**`.
  - Touching `pnpm-lock.yaml`, any `package.json`, `intent.yaml`,
    `component.yaml`, `wrangler.*`, `infra/terraform/**`, `kiox.lock`,
    or `packages/contracts/**`.
  - Touching `apps/api-edge/**` (sealed by ongoing Task 0095/0095.1 PR
    #143 — any edit here will conflict with the in-flight branch and
    invalidate the verifier seal on Phases 1–4).
- Failure modes that invalidate the PR even if `pnpm -r lint` exits 0:
  - Behavioural change to any prod handler (route shape, response body,
    error envelope, feature-flag update payload semantics, scheduled
    log content beyond the log method).
  - Renaming an exported symbol on a public package boundary.
  - Introducing a new dependency or a new import from a workspace not
    already in the file's import graph.
  - Suppression via inline `// eslint-disable-*`,
    `/* eslint-disable */`, `// @ts-ignore`, `// @ts-expect-error`, or
    `as unknown as X` two-step laundering. The goal is "fix the
    underlying warning," not silence it.
  - Editing any file under `tests/**`, `apps/api-edge/**`, or any
    `packages/**`.

## Objective

Drive `pnpm -r --no-bail lint` warning count for **production source
under `apps/*/src/**` (excluding `apps/api-edge`)** from 5 → 0 by
mechanically resolving the residual `no-explicit-any` and `no-console`
warnings in three workspaces:

1. `apps/config-worker/src/handlers/update-feature-flag.ts` lines
   139:85 and 213:87 — `as any` casts on repo update/create input.
2. `apps/metering-worker/src/rollups.ts` line 147:5 — `console.log` in
   a scheduled-rollup summary log line.
3. `apps/webhooks-worker/src/index.ts` lines 30:7 and 36:7 —
   `console.log` in the dispatcher's scheduled summary log lines.

## PR Boundary

One PR contains:

1. Source edits in the three listed workspaces, limited to:
   replacing `as any` with a real type and replacing `console.log` with
   `console.warn` (or the shared logger if already imported in that
   file).
2. The implementer report at `ai/reports/task-0096-implementer.md`
   summarising the diff per file with line:col before/after.
3. Nothing else. No `package.json`, no `pnpm-lock.yaml`, no
   `eslint.config.js`, no test files, no infra, no contracts.

## Required Outcomes

- `pnpm --filter "./apps/config-worker" lint` exits 0 with **0
  warnings** (was 2).
- `pnpm --filter "./apps/metering-worker" lint` exits 0 with **0
  warnings** (was 1).
- `pnpm --filter "./apps/webhooks-worker" lint` exits 0 with **0
  warnings** (was 2).
- `pnpm -r --no-bail lint` total warning count drops by exactly 5
  (644 → 639). All 639 remaining warnings are in `tests/**` and stay
  unchanged.
- `pnpm -r typecheck` exits 0 (Task 0091 baseline holds).
- `pnpm -r --no-bail test` for the three touched workspaces is green.
- `git diff --stat origin/main..HEAD` shows exactly 3 source files
  changed plus 1 report file (4 files total). No file under `tests/**`,
  `apps/api-edge/**`, `packages/**`, `infra/**`, `tooling/**`, or any
  `*.json` / `*.yaml` / `*.jsonc` / `*.lock` file appears in the diff.
- Zero `+eslint-disable*`, `+@ts-ignore`, `+@ts-expect-error`, or
  `+as unknown as` introductions in the diff (verify with
  `git diff origin/main..HEAD | grep -E '^\+.*eslint-disable|^\+.*@ts-ignore|^\+.*@ts-expect-error|^\+.*as unknown as'`
  exits non-zero / empty).

## Repo Hygiene

- Branch: `impl/task-0096-class-b-warning-cleanup-wave-1`. Create from
  `origin/main` at `d94bf92` or later. Do NOT branch from PR #143's
  branch.
- Commits: one or two commits is fine. Commit message convention:
  `Task 0096: class-B warning cleanup wave 1 (apps source no-explicit-any + no-console)`.
- Push the branch and open a GitHub PR titled
  `Task 0096: Class-B warning cleanup wave 1 (apps source)`. PR body
  must list the five warning sites fixed (file:line) with before/after
  code in fenced blocks.
- Implementer report at `ai/reports/task-0096-implementer.md` with:
  PR URL, head SHA, per-file diff summary, repo-method type used to
  replace each `as any` cast (with import path), confirmation that
  `pnpm -r --no-bail lint` warning count is exactly 639, confirmation
  that `pnpm -r typecheck` exit 0, confirmation that touched-workspace
  test suites are green, and a one-line statement of what was NOT
  changed (tests/**, api-edge, packages, infra).

## Out of Scope

- Test workspaces (`tests/**`) — 639 warnings across 9 workspaces
  reserved for a future Task 0096b wave once this PR lands.
- `apps/api-edge` — sealed by in-flight PR #143.
- Any `packages/**` source.
- Shared rule baseline (`tooling/eslint/index.js`) and any
  `eslint.config.js` re-export.
- Adding new dependencies, loggers, or import graphs.
- Behavioural changes of any kind to feature-flag handlers, metering
  rollups, or webhook dispatchers.

## Blocker Protocol

If a repo-method input type for the feature-flag casts cannot be located
in `packages/data/**` (or wherever the repo lives — search by the method
name `updateFeatureFlag` / `createFeatureFlag`), do NOT invent one.
Stop, write the partial state to `ai/reports/task-0096-implementer.md`
under a `## Blocker` section naming the search paths attempted and the
exact file:line of the unresolved cast, push the branch with a `wip:`
prefix, open a draft PR, and exit. The orchestrator will revisit. Do
not fall back to `as unknown as Record<string, unknown>` or similar
laundering — that defeats the purpose of the task.

## Acceptance

- Branch pushed, PR opened, PR description matches the boundary above.
- All five warnings cleared with real type fixes (no suppression).
- Lint count drops from 644 → 639 globally, from 5 → 0 in the three
  named apps workspaces.
- Typecheck and touched-workspace tests green.
- Implementer report committed.
- Diff stays inside the boundary (3 source + 1 report = 4 files).
