# Task ID

Task 0039

# Agent

Implementer

# Current Repo Context

- Active spec pack is the reusable SaaS starter under `specs/**`; do not use
  `specs-v2/**` for this task.
- Task 0038 merged via PR #79 at `e746d8414b97f9634e91ef51203cb3f555962864`.
  Organization bootstrap now atomically emits `organization.created` and
  initial `membership.added` event/audit rows.
- Latest `origin/main` is `e746d84`.
- PR #79 CI run `26445264007` passed for 8 selected Orun checks on the PR
  branch. No open PRs were found during orchestration.
- The local worktree contains untracked historical `ai/tasks/**`,
  `ai/reports/**`, and `.vscode/` files. Do not stage, delete, or rewrite
  unrelated untracked files.
- Task 0038 moved `POST /v1/organizations` out of
  `createOrganizationService` and into `handleCreateOrganization` so the live
  path can share one transaction across membership bootstrap and event/audit
  writes.
- `apps/membership-worker/src/services/organization.ts` still exposes
  `createOrganization` and `listOrganizations` service methods, and
  `tests/membership-worker/src/membership-worker.test.ts` still has direct unit
  coverage for those stale service methods. The live create/list handlers no
  longer use them.
- `handleGetOrganization` still uses `createOrganizationService(...).getOrganization`
  for the policy-gated organization read path. Do not break that route.

# Objective

Remove stale organization-service create/list code and retarget the associated
tests so membership-worker organization tests reflect the live handler/repository
boundaries after Task 0038.

# PR Boundary

One housekeeping PR only:

- membership-worker organization service cleanup;
- membership-worker tests that currently exercise stale service create/list
  methods;
- optional small type/import cleanup directly caused by the removed service
  surface.

This PR must not add routes, change public response shapes, change policy
actions, change event/audit semantics, add migrations, change infrastructure,
start identity security events, implement API keys, config/secrets, webhooks,
notifications, metering, billing, UI, SDK, CLI, or touch `specs-v2/**`.

# Read First

- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/state.json`
- `ai/reports/task-0038-verifier.md`
- `specs/constitution.md`
- `specs/repo.md`
- `specs/orun-golden-path.md`
- `specs/components/04-organizations-membership.md`
- `specs/components/09-events-audit-observability.md`

Then inspect current code before editing:

- `apps/membership-worker/src/services/organization.ts`
- `apps/membership-worker/src/handlers/create-organization.ts`
- `apps/membership-worker/src/handlers/get-organization.ts`
- `apps/membership-worker/src/handlers/list-organizations.ts`
- `tests/membership-worker/src/membership-worker.test.ts`
- `tests/api-edge/src/org-facade.test.ts`

# Required Outcomes

1. Remove stale service methods.
   - Remove `createOrganization` from `createOrganizationService`.
   - Remove `listOrganizations` from `createOrganizationService` if no live
     runtime path uses it.
   - Remove now-unused types such as `CreateOrgInput`, `CreateOrgResult`, and
     `ListOrgsResult` if they become dead.
   - Keep `getOrganization` behavior intact or replace it with an equally
     focused read helper used by `handleGetOrganization`.

2. Align tests with live boundaries.
   - Delete tests that only prove the stale service create/list methods work.
   - Preserve or add focused tests for the live `handleCreateOrganization` path
     where needed so public response shape, validation, conflict handling, and
     Task 0038 event/audit expectations remain covered.
   - Preserve policy-gated `getOrganization` coverage. If service tests are
     reduced, ensure the remaining tests still cover allow, deny-as-404,
     missing organization, fail-closed-without-authorizer, and role-list failure
     behavior for the read path.

3. Preserve behavior.
   - `POST /v1/organizations` must keep the Task 0038 atomic event/audit
     behavior and public response shape.
   - `GET /v1/organizations` must keep cursor pagination behavior.
   - `GET /v1/organizations/{orgId}` must keep policy-gated 404-on-deny
     behavior.
   - No event types, payload fields, audit descriptions, policy actions, route
     matching, or public API envelopes should change.

# Non-Goals

- No identity security-event implementation.
- No spec changes or proposals unless the cleanup reveals real contract drift.
- No database migrations.
- No new public or internal routes.
- No changes to invitation/member/project/environment behavior.
- No Cloudflare, Supabase, AWS, Terraform, or Orun composition changes.
- No README or broad documentation cleanup.

# Constraints

- Trust code reality over stale tests: remove or rewrite tests that protect code
  paths not used by runtime handlers.
- Keep the diff small and mechanical. This should not become a redesign of
  membership-worker services.
- Do not weaken authorization, enumeration protections, event/audit guarantees,
  or transactionality.
- Do not stage unrelated untracked historical task/report files.

# Acceptance Criteria

- No runtime code imports or calls removed organization-service create/list
  methods.
- Membership-worker tests no longer exercise stale create/list service behavior
  as if it were the live organization runtime path.
- Existing organization create/list/get public behavior remains covered at the
  handler or facade level.
- Task 0038 event/audit tests still pass.
- The PR contains no unrelated product or infrastructure changes.

# Verification

Run targeted checks:

```bash
pnpm --filter @saas/membership-worker typecheck
pnpm --filter @saas/membership-worker-tests test
pnpm --filter @saas/membership-worker-tests typecheck
pnpm --filter @saas/api-edge-tests test
pnpm --filter @saas/api-edge-tests typecheck
```

Then run Orun verification:

```bash
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
```

# Branch, Commit, PR, And Report Requirements

- Start from latest `origin/main` (`e746d84` or newer) and create or reuse a
  task-scoped branch such as `codex/task-0039-membership-org-service-cleanup`.
- Commit only task-scoped changes.
- Push the branch.
- Open a GitHub PR against `main`.
- Write `ai/reports/task-0039-implementer.md` with:
  - Summary
  - Files Changed
  - Checks Run
  - Assumptions
  - Spec Proposals
  - Remaining Gaps
  - PR Number
- The task is not complete until the branch is pushed and the PR is open.
