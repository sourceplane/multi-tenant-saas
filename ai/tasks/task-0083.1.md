# Task 0083.1 — Hotfix: thread CONSOLE_CUSTOM_DOMAIN to Terraform

Agent: Implementer

## Current Repo Context

- Task 0083 (PR #129, squash `927c5179`) merged 2026-05-29T12:30:01Z.
  PR-CI and post-merge main-CI both 9/9 SUCCESS.
- Code-level cutover landed correctly: `cloudflare_pages_domain.console`
  was swapped for `cloudflare_workers_domain.console` (cloudflare
  provider v4, pin `~> 4.52`); `apps/web-console/` removed; api-edge
  CORS narrowed; spec/README sweep complete; `intent.yaml`
  `stage.promotion.dependsOn=[dev]` dropped; orun toolchain bumped
  `v2.3.0 → v2.9.0`.
- **Post-merge soak FAILED (Task 0083 verifier).** Both stage and
  prod `cloudflare-domain · Terraform · apply` jobs ran with
  `var.CONSOLE_CUSTOM_DOMAIN = ""`. Result:
  `0 added, 0 changed, 0 destroyed`. The `cloudflare_workers_domain.console`
  resource is gated by `local.has_custom_domain = var.CONSOLE_CUSTOM_DOMAIN != ""`
  (`infra/terraform/cloudflare-domain/terraform/main.tf:159`),
  so `count=0` and the resource is never created.
- Live state: `https://stage.sourceplane.ai/` and
  `https://prod.sourceplane.ai/` both resolve **NXDOMAIN**.
  Rollback hatch is healthy — both
  `sourceplane-web-console-next-{stage,prod}.rahulvarghesepullely.workers.dev`
  serve HTTP 200 with body `Sourceplane Console`.

## Objective

Wire the `CONSOLE_CUSTOM_DOMAIN` value already declared in
`intent.yaml` (`stage.sourceplane.ai`, `prod.sourceplane.ai`) through
to the `cloudflare-domain` Terraform apply step as
`TF_VAR_CONSOLE_CUSTOM_DOMAIN`, so the
`cloudflare_workers_domain.console` resource is actually planned and
applied on the next `github-push-main` run for stage and prod.

This is the same class of bug fixed in Task 0082.2 — a value reaches
the runner as a bare env var but is never re-emitted with a `TF_VAR_`
prefix, so Terraform's variable resolution silently sees the default
`""`.

## Architect Brief

- Product bar: Stripe-quality infra correctness. No silent
  `0 added, 0 changed, 0 destroyed` for the resource this PR exists
  to create.
- User moment: apex URLs `stage.sourceplane.ai` and
  `prod.sourceplane.ai` go from NXDOMAIN to a 200 console page,
  closing out the cutover Task 0083 started.
- Free latitude: pick the wiring shape. Acceptable options include
  (a) modifying the `cloudflare-domain-validate` job template to
  re-emit selected env values as `TF_VAR_<name>`, (b) adding an
  explicit declarative env→TF_VAR mapping to
  `infra/terraform/cloudflare-domain/component.yaml`, or (c)
  promoting `CONSOLE_CUSTOM_DOMAIN` to a per-environment
  `parameterDefault` so it flows through the existing
  `.parameters` → `TF_VAR_<key>` loop. Reference
  `infra/terraform/cloudflare-hyperdrive/` for the working pattern
  (note that hyperdrive sidesteps this by using a `parameter`, not
  an env var). Whatever shape is chosen, record a one-line
  rationale in the implementer report.
- Must not change: the existing `cloudflare_workers_domain.console`
  Terraform resource shape, the Cloudflare provider pin, the
  intent.yaml values themselves (the literal hostnames must remain
  `stage.sourceplane.ai` / `prod.sourceplane.ai`), the api-edge
  CORS list, or any other component. No new public route, no spec
  change beyond touching the README of `cloudflare-domain/` if the
  wiring shape changes.
- Failure modes that invalidate this PR even if CI is green:
  - post-merge `cloudflare-domain · {stage,prod} · apply` logs do
    not show `cloudflare_workers_domain.console: Creation complete`
  - apex curl probes still NXDOMAIN or non-200 after the next
    `github-push-main` apply
  - any unrelated topology / dependency change rides along
- Recommended pattern: option (a) is the least intrusive — add an
  explicit `TF_VAR_CONSOLE_CUSTOM_DOMAIN=${CONSOLE_CUSTOM_DOMAIN}`
  line to the `terraform-env` step of
  `stack-tectonic/compositions/cloudflare-domain/jobs/cloudflare-domain-validate.yaml`
  so the wiring is visible in the composition, not buried in a
  loop. Deviate if you find a better-bounded shape.

## PR Boundary

One PR, one outcome. Acceptable changes:

1. The wiring change (one file, possibly two) that causes
   `TF_VAR_CONSOLE_CUSTOM_DOMAIN` to reach the Terraform apply step
   with the value declared in `intent.yaml`'s per-environment `env`
   block.
2. A README touch in `infra/terraform/cloudflare-domain/` if the
   wiring shape changes the documented contract.
3. The implementer report itself.

Non-Goals:

- No Terraform `main.tf` changes (the variable already exists and
  the gating logic is correct — the value is what's missing).
- No `intent.yaml` topology changes beyond optionally moving the
  `CONSOLE_CUSTOM_DOMAIN` value into `parameterDefaults.terraform`
  (only if you pick option c — record the rationale).
- No changes to `cloudflare-hyperdrive`, `web-console-next`,
  `api-edge`, or any other component.
- No provider version bumps, no orun toolchain bumps.
- No deletion of `pagesProjectPrefix` (still in soak per Task 0083).
- No new spec proposal unless the wiring shape forces a
  documented contract change.

## Read First

- `agents/orchestrator.md` — Implementer Standard, PR-Sized Task
  Standard, Architect Brief expectations.
- `ai/context/current.md` — Task 0083.1 framing and the FAIL trace.
- `ai/reports/task-0083-verifier.md` — full FAIL details, including
  the exact apply-log evidence of `0 added, 0 changed, 0 destroyed`.
- `ai/state.json` — `notes` field records the root cause and the
  rollback hatch URLs.
- `specs/orun-golden-path.md` — env / parameter / TF_VAR contract.
- `infra/terraform/cloudflare-hyperdrive/component.yaml` — working
  reference for the `parameter` → `TF_VAR_<key>` pattern via the
  job template loop.
- `stack-tectonic/compositions/cloudflare-domain/jobs/cloudflare-domain-validate.yaml`
  — the `terraform-env` step is where env→TF_VAR mapping lives.
- `infra/terraform/cloudflare-domain/terraform/main.tf:147-160` —
  `var.CONSOLE_CUSTOM_DOMAIN`, `local.console_custom_domain`,
  `local.has_custom_domain` (do not change these).
- `intent.yaml:80-128` — per-environment `env.CONSOLE_CUSTOM_DOMAIN`
  values for dev (`""`), stage, prod.

## Required Outcomes

- [ ] After the change, `terraform plan` for `cloudflare-domain` in
  the `stage` and `prod` environments shows
  `cloudflare_workers_domain.console` will be created (count=1, not
  0).
- [ ] Implementer report explicitly records the wiring shape
  chosen and a one-line rationale for that choice over the other
  two options.
- [ ] Implementer report includes the exact `kiox -- orun
  plan --intent intent.yaml --output plan.json` artifact path and
  confirms the cloudflare-domain plan jobs are still present in the
  9-job matrix.
- [ ] PR opened (mandatory) with branch
  `impl/task-0083.1-console-domain-tfvar` (or a clearly
  task-scoped name) and the implementer report committed to the PR
  branch.

## Constraints

- No secrets in any committed file; the Cloudflare API token /
  account ID continue to flow through `secrets.CLOUDFLARE_*` in the
  workflow.
- The wiring change must work for all three environments without
  regression: dev (`CONSOLE_CUSTOM_DOMAIN=""` must continue to gate
  the resource to count=0 and not error), stage, prod.
- No breaking change to the existing `terraform-env` step contract
  for other components (the cloudflare-domain job template is
  cloudflare-domain-specific, so option (a) is local; verify before
  editing if any other component reuses it).
- Verifier merge protocol applies: the post-merge soak is the real
  acceptance test.

## Integration Notes

- `intent.yaml` already carries the values
  (`dev: ""`, `stage: stage.sourceplane.ai`,
  `prod: prod.sourceplane.ai`).
- `.github/workflows/ci.yml` already exposes
  `TF_VAR_cloudflare_api_token` and `TF_VAR_cloudflare_account_id`
  at the workflow env layer; CONSOLE_CUSTOM_DOMAIN is per-env, so
  it must be wired through the per-env `.env` flow, not workflow
  secrets.
- The `terraform-env` step in
  `stack-tectonic/compositions/cloudflare-domain/jobs/cloudflare-domain-validate.yaml`
  writes to `$ORUN_ENV`, which is then picked up by every
  subsequent Terraform step (`init`, `plan`, `apply`).

## Acceptance Criteria

Pre-merge (PR CI):

- ✅ `kiox -- orun validate --intent intent.yaml` exits 0.
- ✅ `kiox -- orun plan --changed --intent intent.yaml --output plan.json`
  exits 0 and the resulting matrix includes
  `cloudflare-domain.{stage,prod} · Terraform · plan` jobs.
- ✅ `kiox -- orun run --plan plan.json --dry-run --runner github-actions`
  exits 0; the rendered `terraform-env` step contains a literal
  `TF_VAR_CONSOLE_CUSTOM_DOMAIN=` export for stage and prod.
- ✅ PR CI all checks green on the PR head.

Post-merge (mandatory soak — Verifier owns this):

- ✅ `cloudflare-domain · stage · Terraform · apply` log contains
  `cloudflare_workers_domain.console: Creation complete` and a
  non-zero `added` count.
- ✅ `cloudflare-domain · prod · Terraform · apply` log contains
  the same.
- ✅ `curl -sfL https://stage.sourceplane.ai/` returns 200, body
  contains `Sourceplane Console`.
- ✅ `curl -sfL https://prod.sourceplane.ai/` returns 200, body
  contains `Sourceplane Console`.
- ✅ Rollback hatch still healthy:
  `https://sourceplane-web-console-next-{stage,prod}.rahulvarghesepullely.workers.dev`
  return 200.

## Verification

- Local Orun trio above.
- `terraform -chdir=infra/terraform/cloudflare-domain/terraform fmt -check`
  passes if any terraform file is touched (none expected for this
  task).
- Inspect the rendered job YAML (`orun run --dry-run`) to confirm
  the export line is present before pushing.
- Verifier will run the post-merge soak per
  `references/post-merge-deploy-profile-gap.md`.

## PR Creation Requirement

You must create a branch, commit, push, and open a GitHub PR before
reporting complete. PR Number: TBD is not acceptable in the final
report. After `gh pr create`, update the implementer report and any
files containing `#[PR]` / `TBD` placeholders with the real PR
number, then commit+push that final update.

## When Done Report

Write `/ai/reports/task-0083.1-implementer.md` with sections:

- Summary (3-5 bullets, including chosen wiring shape and
  rationale)
- Files Changed (grouped, expect 1–3 files)
- Checks Run (exact commands + results; include the rendered
  TF_VAR_CONSOLE_CUSTOM_DOMAIN export snippet)
- Assumptions (durable only)
- Spec Proposals (link only if needed; not expected)
- Remaining Gaps (post-merge soak owned by Verifier)
- Next Task Dependencies
- PR Number (real number, not TBD)
