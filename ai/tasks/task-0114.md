# Task 0114

Agent: Implementer

## Current Repo Context

- Task 0113 (B5 webhook endpoint re-enable vertical slice) verified PASS + merged 2026-05-31 as `f5cda64`. Contract `EnableWebhookEndpointRequest`/`Response`, SDK `client.webhooks.enableEndpoint(orgId, endpointId, body?, opts?)`, api-edge matcher, `webhooks-worker` `POST /v1/organizations/:orgId/webhooks/endpoints/:endpointId/enable`, db `enableEndpoint`, and console "Re-enable" button + dialog are all live on main. Audit pattern matches Task 0024 atomicity (both repos from same `txExec` inside `executor.transaction(...)`, throw-to-rollback on event-append fail, audit description `"Webhook endpoint re-enabled"`).
- Repo health: green. main HEAD `f5cda64` (post-verifier bookkeeping at `bc15418`, working tree clean).
- B5 endpoint-CRUD arc (create / edit / disable / delete / re-enable) is closed end-to-end **everywhere except CLI**. The CLI gap is the symmetric counterpart for re-enable.
- Established CLI cadence for webhook subcommands: 0103 (`webhook create`) → 0107 (`webhook verify` / `webhook sign`) → 0110 (`webhook secrets rotate`) → 0111 (cli-helpers extraction `helpers.ts`). The Task 0110 file `packages/cli/src/commands/webhook-secrets-rotate.ts` is the canonical template (pure SDK consumer, no fetch, no header building, helpers-extracted org resolution + idempotency-key reading).
- Deferred candidates parked in `/ai/deferred.md` (provider-swap, cloudflare-domain v4→v5, optional spec-13 commands, notifications-worker-dev-reframe) — do **not** reach into them.

## Objective

Ship the symmetric CLI subcommand `sourceplane webhook enable <endpointId>` as a pure consumer of the locked `client.webhooks.enableEndpoint` SDK shape, closing the B5 endpoint-CRUD CLI gap for the re-enable surface and matching the Task 0110 cadence (mirror the existing console "Re-enable" button + dialog from Task 0113).

## PR Boundary

This PR adds **exactly** these files:

1. `packages/cli/src/commands/webhook-enable.ts` (NEW) — single-call SDK adapter, mirrors `webhook-secrets-rotate.ts` shape; uses `resolveOrgId(ctx, /* allowOverride */ false)` and `readIdempotencyKey(ctx)` from `./helpers.js`; `assertOutputModeValid` gate; human + json output modes.
2. `packages/cli/src/cli-runner.ts` (MODIFIED) — exactly one new `import` line + exactly one new `r.register(["webhook", "enable"], ...)` line + exactly one new help-output usage line. No other edits.
3. `packages/cli/src/__tests__/webhook-enable.test.ts` (NEW) — vitest suite, ≥ 8 cases (see Acceptance Criteria for the floor), mirrors `webhook-secrets-rotate.test.ts` fake-SDK-injection harness.
4. `ai/reports/task-0114-implementer.md` (NEW) — committed on the PR branch with the **real** PR number (no `TBD`, no Task-0106 fix-up).

**Forbidden zones** (touch any of these → auto-FAIL):

- `packages/contracts/**`, `packages/sdk/**`, `packages/db/**`, `packages/webhook-verifier/**`, `packages/notifications-client/**`, `packages/shared/**`, `packages/eslint-config/**`, `packages/tsconfig/**`
- `apps/**` (no api-edge, no webhooks-worker, no web-console-next, no policy-worker, no membership-worker, no projects-worker, no events-worker, no notifications-worker, no identity-worker, no metering-worker, no billing-worker, no usage-worker, no policy-worker — none of them)
- `tests/**` outside the new `packages/cli/src/__tests__/webhook-enable.test.ts`
- `infra/**`, `tooling/**`, `stack-tectonic/**`, root `package.json`, `pnpm-lock.yaml`, `kiox.lock`
- All other `packages/cli/src/commands/*.ts` files (`writes.ts`, `cross-reads.ts`, `webhook-sign.ts`, `webhook-verify.ts`, `webhook-secrets-rotate.ts`, `helpers.ts`, `index.ts`)
- `packages/cli/package.json` and `packages/cli/component.yaml` (turbo-package shape is already correct)

**Non-goals**:

- No `webhook disable` subcommand (`disableEndpoint` SDK method exists but is out of scope; defer to a future task).
- No re-shaping of `helpers.ts` (Task 0111 extraction is final for this PR).
- No edits to existing `webhook-*` test files.
- No console / api-edge / webhooks-worker / contract / SDK / db / Terraform changes.
- No new flags beyond `--idempotency-key=KEY` and `--output=human|json` (mirrors rotate exactly).
- No live network calls in tests.

## Read First

- `agents/orchestrator.md` — Implementer Standard, PR Creation Requirement, PR-Sized Task Standard.
- `packages/cli/src/commands/webhook-secrets-rotate.ts` — canonical template to mirror.
- `packages/cli/src/__tests__/webhook-secrets-rotate.test.ts` — canonical test harness to mirror (fake-SDK injection via `sdkFactory`).
- `packages/cli/src/commands/helpers.ts` — `resolveOrgId(ctx, allowOverride)` + `readIdempotencyKey(ctx)` (Task 0111 extraction; use `allowOverride: false` to mirror rotate semantics).
- `packages/cli/src/cli-runner.ts` lines 22–25 (imports), 166–169 (existing webhook registrations), and the help-output block ~lines 200–206.
- `packages/sdk/src/webhooks.ts` — locate `enableEndpoint` (method around line 170). Note the body is `EnableWebhookEndpointRequest = {}` (empty contract).
- `packages/contracts/src/webhooks.ts` — `EnableWebhookEndpointRequest` (empty interface), `EnableWebhookEndpointResponse` (`{ endpoint: PublicWebhookEndpoint }`).
- `apps/web-console-next/src/components/webhooks/enable-endpoint-dialog.tsx` — reference for human-mode language ("Webhook endpoint re-enabled") to keep CLI/console phrasing consistent.

## Required Outcomes

- [ ] `packages/cli/src/commands/webhook-enable.ts` is a thin one-call adapter:
  - Parses `<endpointId>` positional → `UsageError` (exit 2) if missing/empty.
  - Calls `assertOutputModeValid(ctx)` (mirror rotate's exact gate, including the `--output requires human|json` and `--output must be human or json, got: ${raw}` messages, with subcommand prefix `webhook enable:`).
  - `orgId = await resolveOrgId(ctx, /* allowOverride */ false)`.
  - `idempotencyKey = readIdempotencyKey(ctx)`.
  - One SDK call: `await sdk.webhooks.enableEndpoint(orgId, endpointId, {}, idempotencyKey !== undefined ? { idempotencyKey } : {})`.
  - Output:
    - `json`: `ctx.stdout(JSON.stringify(response))` once, exit 0.
    - `human`: header line `Webhook endpoint re-enabled: ${endpointId} in ${orgId}`, blank line, indented key/value block (`status:`, `secretVersion:`, `updatedAt:`), exit 0. No reveal-once warning (re-enable carries no secret material — keep block minimal).
- [ ] `cli-runner.ts` registers `["webhook", "enable"]` with description `"Re-enable a previously disabled webhook endpoint"`. Help-output adds **one** line:
  `"  sourceplane webhook enable <endpointId> [--idempotency-key=KEY] [--output=human|json]"`.
  Place the new import alphabetically near the existing `webhook-secrets-rotate` import; place the new `r.register` line directly after the existing `webhook secrets rotate` registration.
- [ ] `webhook-enable.test.ts` ≥ 8 vitest cases covering at minimum:
  1. Success default human mode prints header + status block; SDK called with `(orgId, endpointId, {}, {})`.
  2. Success `--output=json` prints SDK response as single-line JSON; SDK called as above.
  3. `--idempotency-key=key-123` is forwarded verbatim to SDK opts.
  4. Missing positional `<endpointId>` → `UsageError`, exit 2 (and helper usage string is correct).
  5. `--output=yaml` (invalid) → `UsageError`, exit 2.
  6. `--output` (boolean true / no value) → `UsageError`, exit 2.
  7. SDK rejection (4xx-shaped Error) propagates through standard error path (exit non-zero, no secret/payload leaks into stderr).
  8. Logged-out / no active-org context → `MissingOrgContextError` (or whatever `resolveOrgId(ctx, false)` raises) propagates without a fake-SDK call being made (assert `enableCalls.length === 0`).
  - Re-use the rotate test's `MemoryTokenStore` + `ContextStore` + `runCli` harness; do NOT re-implement.
- [ ] No new runtime dependencies, no new `devDependencies`, no edits to `packages/cli/package.json`.
- [ ] No `node:*` imports in `webhook-enable.ts` (test file may use `node:fs|os|path` for fixture I/O, mirroring rotate test).
- [ ] No `eslint-disable` / `@ts-ignore` / `@ts-expect-error` / `as any` / `as unknown as` in production source (test boundary `as unknown as Sourceplane` is acceptable mirroring rotate test).
- [ ] Implementer report at `ai/reports/task-0114-implementer.md` with sections: Summary, Files Changed, Checks Run, Assumptions, Spec Proposals (none expected), Remaining Gaps, Next Task Dependencies, **PR Number** (real number, not TBD).

## Non-Goals

- No `webhook disable` (separate future task).
- No `webhook endpoints list` / `get` / `delete` / `update` — those need their own scoped slices.
- No SDK / contract / api-edge / worker / console / Terraform / migration changes.
- No reshape of existing webhook command help strings beyond adding the single new usage line.
- No new components, no `component.yaml` changes (turbo-package shape unchanged).

## Constraints

1. Pure SDK consumer — zero `fetch(`, zero header building, zero auth handling in this command.
2. Mirror `webhook-secrets-rotate.ts` structure byte-by-byte where semantically equivalent (file header comment, `assertOutputModeValid`, helper imports). Diverge only where re-enable semantics differ (no reveal-once warning, no secret plaintext, no `secretLastRotatedAt` line — re-enable returns only `endpoint`).
3. Org id resolution: `resolveOrgId(ctx, /* allowOverride */ false)` — no `--org` flag plumbing.
4. Idempotency-key: forward verbatim when supplied via `--idempotency-key=KEY`, omit when absent. Never auto-mint.
5. Test floor ≥ 8 cases; harness mirrors `webhook-secrets-rotate.test.ts`.
6. Branch: `impl/task-0114-cli-webhook-enable`. One commit (or two — code + report-with-real-PR fixup), no merge-of-main in branch history.
7. Turbo-package shape: only `cli·{dev,stage,prod}·Verify` lanes should be selected by Orun changed-plan (3 jobs total).
8. No secret material in any output path (re-enable response carries none, but defence-in-depth: don't `.toString()` the response into errors or interpolate it into wider objects).
9. Implementer report is committed on the PR branch with the **real** PR number — `TBD` is not an acceptable end state (recurring 0106 missing-report fix-up pattern).
10. BEHIND-main rebase is the verifier's responsibility, not the implementer's. Do **not** rebase or merge main into the branch.

## Integration Notes

- The `cli-runner.ts` import block currently has these webhook imports (lines 22–26):
  ```ts
  import { webhookCreateCommand } from "./commands/writes.js";
  import { makeWebhookVerifyCommand, type WebhookVerifyOptions } from "./commands/webhook-verify.js";
  import { makeWebhookSignCommand, type WebhookSignOptions } from "./commands/webhook-sign.js";
  import { webhookSecretsRotateCommand } from "./commands/webhook-secrets-rotate.js";
  ```
  Add: `import { webhookEnableCommand } from "./commands/webhook-enable.js";` after the `webhookSecretsRotateCommand` import.
- The `r.register` block currently ends webhook registrations at line 169. Add directly after:
  ```ts
  r.register(["webhook", "enable"], "Re-enable a previously disabled webhook endpoint", webhookEnableCommand);
  ```
- The help-output block (around lines 203–206) currently lists four webhook usage lines. Add fifth line directly after the `webhook secrets rotate` line:
  ```ts
  "  sourceplane webhook enable <endpointId> [--idempotency-key=KEY] [--output=human|json]",
  ```
- `client.webhooks.enableEndpoint` SDK signature (locked by Task 0113):
  ```ts
  enableEndpoint(orgId, endpointId, body: EnableWebhookEndpointRequest = {}, opts?): Promise<EnableWebhookEndpointResponse>
  ```
  Pass `{}` for the body (contract is empty by design). Pass `{ idempotencyKey }` for opts only when defined.
- `EnableWebhookEndpointResponse.endpoint` is a `PublicWebhookEndpoint`; for the human block emit `status`, `secretVersion`, and `updatedAt` (three lines, indented two spaces). Do **not** emit `disabledReason`/`disabledAt` (they will be `null` after re-enable — printing them is noise).
- Audit description on the worker side is `"Webhook endpoint re-enabled"`. The CLI human header reuses that exact phrasing for cross-surface consistency.

## Acceptance Criteria

✅ PR is exactly 4 files (1 NEW command, 1 MODIFIED runner, 1 NEW test, 1 NEW report). Diff `+/-` lines stay reasonable (~ +250/-0 ballpark; rotate was +720/-0 for a more complex command).

✅ `pnpm install --frozen-lockfile` clean (no lockfile drift).

✅ `pnpm -r typecheck` = 0 across all workspace projects.

✅ `pnpm -r --no-bail lint` warning count does NOT increase vs main HEAD `f5cda64`.

✅ `pnpm --filter @saas/cli test` runs the new suite green; net new passing tests ≥ +8 over the current `packages/cli/src/__tests__/` baseline.

✅ Local Orun gates green:
  ```bash
  /Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
  /Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
  /Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
  ```
  Plan selects ONLY `cli·dev·Verify`, `cli·stage·Verify`, `cli·prod·Verify` (3 lanes).

✅ Branch `impl/task-0114-cli-webhook-enable` pushed; PR opened with title `feat(cli): webhook enable subcommand` (or `Task 0114: webhook enable CLI subcommand`).

✅ PR-CI green at HEAD (4/4 SUCCESS: plan + 3 cli·env·Verify lanes).

✅ Implementer report at `ai/reports/task-0114-implementer.md` is committed on the PR branch with the **real** PR number, not `TBD`.

✅ Forbidden-zone scan (run before pushing): zero hits across `packages/(contracts|sdk|db|webhook-verifier|notifications-client|shared|eslint-config|tsconfig)/`, `apps/`, `tests/`, `infra/`, `tooling/`, `stack-tectonic/`, root `package.json`, `pnpm-lock.yaml`, `kiox.lock`, and the four sibling `packages/cli/src/commands/*.ts` files (`writes`, `cross-reads`, `webhook-sign`, `webhook-verify`, `webhook-secrets-rotate`, `helpers`, `index`).

✅ Hazard scan in production source under PR diff: zero new `eslint-disable` / `@ts-ignore` / `@ts-expect-error` / `as any` / `as unknown as` / `node:*` / `fetch(` / `Math.random` (crypto.randomUUID is fine if used).

✅ MergeStateStatus is `CLEAN` (or `BEHIND` only — verifier handles `gh pr update-branch <PR#>` if needed; implementer does not rebase).

## Verification

The verifier (Task 0114-verifier, scoped after this implementer completes) will run:

- 8-phase verifier shape mirroring Task 0110/0111 (turbo-package, no deploy lane, no live URL):
  - Phase 0: working dir + readiness (gh pr checkout, impl report on PR branch with real `#`).
  - Phase 1: PR sanity (4-file allowlist EXACTLY; forbidden-zone grep auto-FAIL on any hit).
  - Phase 2: hazard scan (eslint-disable / ts-ignore / `as any` / `node:*` / fetch / Math.random in production source under PR diff).
  - Phase 3: quality gates (`pnpm install --frozen-lockfile`, `pnpm -r typecheck`, `pnpm -r --no-bail lint` no new warnings, `pnpm --filter @saas/cli test`).
  - Phase 4: behaviour-equivalence diff vs `webhook-secrets-rotate.ts` template (assertOutputModeValid, helpers imports, single-call SDK consumer shape).
  - Phase 5: Orun local gates + PR-CI run via `gh run view --log` (not just summary).
  - Phase 6: BEHIND-main rebase via `gh pr update-branch` if needed; re-watch PR-CI on rebased HEAD.
  - Phase 7: merge protocol — squash, delete branch, fast-forward main locally, post-merge main-CI run check.

## PR Creation Requirement

You **must** create a branch (`impl/task-0114-cli-webhook-enable`), commit the four scoped changes, push the branch, and open a GitHub PR before reporting complete.

After `gh pr create` returns the real PR number, update `ai/reports/task-0114-implementer.md` to replace any `TBD`/`#[PR]` placeholder with the real number, then commit + push that fixup as a final commit on the same branch. `PR Number: TBD` is not an acceptable completed-implementer end state.

If PR creation fails (network, auth, or `gh` error), mark the report `PR Number: BLOCKED` with the exact failing command and error, do NOT silently end with TBD.

## When Done Report

`/ai/reports/task-0114-implementer.md` with sections:

- **Summary** (3–5 bullets)
- **Files Changed** (grouped: command source, runner registration, test, report)
- **Checks Run** (exact commands + result, including the four pnpm gates and three Orun gates and `gh run view <run-id> --log` excerpts proving CLI lanes ran)
- **Assumptions** (e.g., human-mode field choice `status`/`secretVersion`/`updatedAt`)
- **Spec Proposals** (none expected; if you discover a contract/SDK gap, write `/ai/proposals/task-0114-spec-update.md` and pause)
- **Remaining Gaps** (e.g., `webhook disable` symmetric counterpart still absent — explicit non-goal here)
- **Next Task Dependencies** (none — this closes the B5 endpoint-CRUD CLI gap for re-enable)
- **PR Number** (real number, `BLOCKED` only if `gh pr create` failed)
