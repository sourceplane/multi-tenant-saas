# Task 0116

Agent: Implementer

## Current Repo Context

- Task 0115 (`sourceplane webhook disable <endpointId> [--reason=TEXT]`)
  verified PASS + merged 2026-05-31 as `558d8d5` (squash of PR #170). The B5
  endpoint-CRUD CLI arc (create / disable / re-enable, plus sign / verify /
  secrets-rotate) is now closed end-to-end. main HEAD `558d8d5`, repo health
  green, working tree clean.
- **Carry-forward gap (surfaced in both Task 0114 implementer + verifier
  reports, and again in Task 0115):** `packages/sdk/src/index.ts` re-exports the
  ENTIRE webhook type surface — `Create*`, `Get*`, `List*`, `Update*`,
  `Disable*`, `Delete*`, `RotateWebhookSecretResponse` — but is **missing**
  `EnableWebhookEndpointRequest` and `EnableWebhookEndpointResponse`. Both types
  are defined in `packages/contracts/src/webhooks.ts` (lines 80 and 85) and are
  already imported + used inside `packages/sdk/src/webhooks.ts`
  (`enableEndpoint(orgId, endpointId, body, opts)` at line ~170). They simply
  never got added to the public index re-export block (`index.ts:165–186`).
- Consequence: `packages/cli/src/__tests__/webhook-enable.test.ts` cannot import
  `EnableWebhookEndpointResponse` from `@saas/sdk`, so it locally reconstructs
  the shape via a hand-rolled `interface EnableResponse { endpoint:
  PublicWebhookEndpoint }` (lines 10–14, 30–32). The Task 0115 disable test, by
  contrast, imports `DisableWebhookEndpointResponse` directly because that type
  IS re-exported — the asymmetry is purely this missing pair of lines.
- This task closes the gap at its source (SDK index) and removes the now-needless
  local reconstruction in the enable test.

## Objective

Re-export `EnableWebhookEndpointRequest` and `EnableWebhookEndpointResponse`
from the `@saas/sdk` package index so the enable webhook response/request types
are publicly consumable (matching every other webhook-endpoint operation), and
refactor `webhook-enable.test.ts` to import `EnableWebhookEndpointResponse`
directly from `@saas/sdk` instead of locally reconstructing it — bringing the
enable test into shape-symmetry with the disable test.

## PR Boundary

This PR changes **exactly** these files:

1. `packages/sdk/src/index.ts` (MODIFIED) — add `EnableWebhookEndpointRequest`
   and `EnableWebhookEndpointResponse` to the existing webhook-endpoint
   `export type { … } from "@saas/contracts/webhooks"` block (the block at
   lines ~165–186 that already lists `Disable*`, `Create*`, etc.). Place the two
   new names adjacent to the other `*WebhookEndpoint*` request/response pairs
   (e.g. directly before `DisableWebhookEndpointRequest`/`Response` to mirror
   the contract ordering). Additive only — no removals, no reordering of
   unrelated names.
2. `packages/cli/src/__tests__/webhook-enable.test.ts` (MODIFIED) — replace the
   local `interface EnableResponse { endpoint: PublicWebhookEndpoint }`
   reconstruction with a direct `import type { EnableWebhookEndpointResponse }
   from "@saas/sdk"`, and use that type for the `RESPONSE` fixture, the
   `opts.response` field, and the fake `enableEndpoint` return type. Delete the
   now-stale "not (yet) re-exported" comment block (lines ~10–14). Keep the
   `PublicWebhookEndpoint` import (still needed for `FIXTURE_ENDPOINT`). Do NOT
   change any test assertions, case count, or behaviour — this is a type-source
   swap only.
3. `ai/reports/task-0116-implementer.md` (NEW) — committed on the PR branch with
   the **real** PR number (no `TBD`).

**Forbidden zones** (touch any → auto-FAIL):

- `packages/contracts/**` (the types are already correctly defined there — do
  NOT edit the source of truth), `packages/db/**`, `packages/webhook-verifier/**`,
  `packages/notifications-client/**`, `packages/shared/**`,
  `packages/eslint-config/**`, `packages/tsconfig/**`.
- `packages/sdk/src/webhooks.ts` and every other `packages/sdk/**` file except
  `packages/sdk/src/index.ts` (the method already imports/uses the types
  correctly; only the public index re-export is missing).
- `packages/cli/**` except the single test file named above — no command source,
  no `cli-runner.ts`, no other test, no `helpers.ts`, no `package.json`.
- `apps/**`, `infra/**`, `tooling/**`, `stack-tectonic/**`, root `package.json`,
  `pnpm-lock.yaml`, `kiox.lock`, `packages/sdk/package.json`,
  `packages/sdk/component.yaml`, `packages/cli/package.json`,
  `packages/cli/component.yaml`.

**Non-goals**:

- No change to the enable command source (`webhook-enable.ts`) — it already
  works; this is a type-surface + test cleanup only.
- No new webhook CLI subcommands, no `list`/`get`/`delete`/`update` CLI surface.
- No contract changes — `EnableWebhookEndpoint*` are already defined in
  `packages/contracts/src/webhooks.ts`; this only re-exports them through SDK.
- No reordering or stylistic churn of the existing SDK index export block beyond
  inserting the two new names.
- No behaviour or assertion change in `webhook-enable.test.ts`.

## Read First

- `agents/orchestrator.md` — Implementer Standard, PR Creation Requirement,
  PR-Sized Task Standard.
- `packages/sdk/src/index.ts` (lines ~165–186) — the webhook-endpoint
  `export type { … } from "@saas/contracts/webhooks"` block to extend.
- `packages/contracts/src/webhooks.ts` (lines 80–88) —
  `EnableWebhookEndpointRequest` / `EnableWebhookEndpointResponse` definitions
  (the names to re-export; do NOT edit this file).
- `packages/sdk/src/webhooks.ts` (lines ~10–11, ~170–178) — confirms the SDK
  method already imports + returns these types internally.
- `packages/cli/src/__tests__/webhook-enable.test.ts` (lines 10–14, 22, 30–32,
  50, 69, 106, 114) — the local reconstruction to replace.
- `packages/cli/src/__tests__/webhook-disable.test.ts` (lines ~23–24, 48) — the
  canonical "import the response type directly from `@saas/sdk`" pattern to
  match.

## Required Outcomes

- [ ] `packages/sdk/src/index.ts` re-exports `EnableWebhookEndpointRequest` and
      `EnableWebhookEndpointResponse` from `@saas/contracts/webhooks` in the
      existing webhook-endpoint type block. `import type {
      EnableWebhookEndpointResponse } from "@saas/sdk"` now type-checks from any
      consumer.
- [ ] `webhook-enable.test.ts` imports `EnableWebhookEndpointResponse` from
      `@saas/sdk` and uses it for the `RESPONSE` fixture + `opts.response` +
      fake-SDK return type; the local `interface EnableResponse` and the
      "not (yet) re-exported" comment are removed. `PublicWebhookEndpoint` import
      retained for `FIXTURE_ENDPOINT`.
- [ ] No test assertion, case count, or behaviour change in
      `webhook-enable.test.ts` (still the same N passing cases as on main HEAD).
- [ ] No new runtime/dev dependencies; no edits to any `package.json` or
      `component.yaml`.
- [ ] No `eslint-disable` / `@ts-ignore` / `@ts-expect-error` / `as any` /
      `as unknown as` / `node:*` introduced in the SDK index change (the test's
      existing `node:fs`/`node:os`/`node:path` imports and the boundary
      `as unknown as Sourceplane` cast are pre-existing and may remain).
- [ ] Implementer report at `ai/reports/task-0116-implementer.md` with sections:
      Summary, Files Changed, Checks Run, Assumptions, Spec Proposals (none
      expected), Remaining Gaps, Next Task Dependencies, **PR Number** (real
      number, not TBD).

## Constraints

1. Additive SDK re-export only — do not remove or reorder unrelated exported
   names; insert the two new names adjacent to the sibling `*WebhookEndpoint*`
   pairs.
2. The contracts package is the source of truth and is a forbidden zone — only
   re-export, never redefine.
3. Test refactor is a pure type-source swap: zero change to assertions, fixtures
   values, harness wiring, or the number of `it(...)` cases.
4. Branch: `impl/task-0116-sdk-enable-response-reexport`. One commit (or two —
   code + report-with-real-PR fixup), no merge-of-main in branch history.
5. Orun changed-plan will select BOTH the `sdk` and `cli` component lanes
   (since both packages have a changed file). That is expected and correct —
   this is NOT a single-component turbo-package PR. Acceptance is the union of
   `sdk·{dev,stage,prod}·Verify` + `cli·{dev,stage,prod}·Verify` lanes (plus
   `plan`). Do NOT try to force it down to one component.
6. No secret material anywhere (none in scope).
7. Implementer report committed on the PR branch with the **real** PR number —
   `TBD` is not acceptable.
8. BEHIND-main rebase is the verifier's responsibility, not the implementer's.
9. Build artifacts: do NOT commit `plan.json`; revert any `kiox.lock` mutation
   from Orun runs before pushing.

## Acceptance Criteria

✅ PR is exactly 3 files (1 MODIFIED SDK index, 1 MODIFIED enable test, 1 NEW
report). Diff is small (`~ +6/-12` ballpark — two export lines added, the local
interface + comment removed, import lines adjusted).

✅ `pnpm install --frozen-lockfile` clean (no lockfile drift).

✅ `pnpm --filter @saas/sdk typecheck` = 0 AND `pnpm --filter @saas/cli
typecheck` = 0.

✅ `pnpm --filter @saas/sdk lint` and `pnpm --filter @saas/cli lint` warning
counts do NOT increase vs main HEAD.

✅ `pnpm --filter @saas/cli test` green; `webhook-enable.test.ts` passes with
the same case count as on main HEAD (no net test-count change expected — this is
a refactor, not a feature). `pnpm --filter @saas/sdk test` green if the SDK
package has tests.

✅ Local Orun gates green (validate / plan --changed / run --dry-run): plan
selects `sdk·{dev,stage,prod}·Verify` + `cli·{dev,stage,prod}·Verify` lanes
(2 components × 3 envs = 6 jobs + plan).

✅ Branch pushed; PR opened with title `refactor(sdk): re-export
EnableWebhookEndpoint request/response types`.

✅ PR-CI green at HEAD (all selected lanes SUCCESS).

✅ Implementer report committed on the PR branch with the **real** PR number,
not `TBD`.

✅ Forbidden-zone scan: zero hits — in particular zero edits to
`packages/contracts/**`, `packages/sdk/src/webhooks.ts`, any other
`packages/sdk/**` file, or any `packages/cli/**` file other than the named test.

✅ Hazard scan in the SDK index change: zero new `eslint-disable` / `@ts-ignore`
/ `@ts-expect-error` / `as any` / `as unknown as`.

✅ MergeStateStatus is `CLEAN` (or `BEHIND` only — verifier handles
`gh pr update-branch`).

## Verification

The verifier (Task 0116-verifier, scoped after this implementer completes) will
run an 8-phase verifier shape ADAPTED for a 2-component turbo-package PR (sdk +
cli, no deploy lane, no live URL):

- Phase 0: working dir + readiness (gh pr checkout, impl report on PR branch with
  real `#`).
- Phase 1: PR sanity (3-file allowlist EXACTLY; forbidden-zone grep auto-FAIL on
  any hit — especially `packages/contracts/**`, `packages/sdk/src/webhooks.ts`,
  and sibling cli files). Push the scope commit to origin/main BEFORE judging PR
  file count (recompute merge-base — Task 0114/0115 lesson).
- Phase 2: hazard scan in the SDK index change; confirm additive-only re-export
  (diff the export block against main — only two names added).
- Phase 3: quality gates (install / sdk+cli typecheck / lint no-new-warnings /
  cli test same case count / sdk test).
- Phase 4: confirm the test refactor is behaviour-neutral — `webhook-enable.test`
  case count unchanged vs main HEAD, assertions untouched, only the type source
  swapped; confirm `EnableWebhookEndpointResponse` now imports cleanly.
- Phase 5: Orun local gates (expect sdk + cli lanes) + PR-CI run via
  `gh pr checks` / `gh run view --log`.
- Phase 6: BEHIND-main rebase via `gh pr update-branch` if needed; re-watch
  PR-CI on rebased HEAD.
- Phase 7: merge protocol — squash, delete branch, fast-forward main locally,
  post-merge bookkeeping; watch post-merge main-CI to SUCCESS (sdk + cli lanes,
  no deploy lane — turbo-package shape).

## PR Creation Requirement

You **must** create a branch (`impl/task-0116-sdk-enable-response-reexport`),
commit the three scoped changes, push the branch, and open a GitHub PR before
reporting complete. After `gh pr create` returns the real PR number, update
`ai/reports/task-0116-implementer.md` to replace any `TBD`/`#[PR]` placeholder
with the real number, then commit + push that fixup as a final commit on the
same branch. `PR Number: TBD` is not an acceptable completed-implementer end
state. If PR creation fails, mark the report `PR Number: BLOCKED` with the exact
failing command and error.

## When Done Report

`/ai/reports/task-0116-implementer.md` with sections:

- **Summary** (3–5 bullets)
- **Files Changed** (grouped: SDK index, enable test, report)
- **Checks Run** (exact commands + result, including pnpm gates for both sdk and
  cli, and the three Orun gates)
- **Assumptions** (e.g., the enable test case count is unchanged; export
  placement adjacent to the Disable pair)
- **Spec Proposals** (none expected)
- **Remaining Gaps** (e.g., no CLI read-side surface yet — separate scope)
- **Next Task Dependencies** (none — this closes the enable re-export carry-
  forward gap)
- **PR Number** (real number, `BLOCKED` only if `gh pr create` failed)
