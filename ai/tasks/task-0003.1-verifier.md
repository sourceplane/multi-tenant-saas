# Task ID

Task 0003.1 Verifier

# Agent

Verifier

# Current Repo Context

Task 0003.1 has open PR #26:

- URL: https://github.com/sourceplane/multi-tenant-saas/pull/26
- Branch: `task-0003.1-delete-legacy-terraform`
- Base: `main` at `f645d41`
- Current PR state: open, not draft
- Current merge status: `UNSTABLE` because CI is still in progress
- Current PR CI run: `26114594102`
- Previous failing `main` CI run to compare against: `26113133649`

Task 0003.1 is the deletion-only stabilization follow-up to Task 0003. Its
scope is intentionally narrow:

- delete `infra/terraform/tf-state-r2/` from active repo source;
- delete `infra/terraform/core/` from active repo source;
- remove active references that would keep those components discoverable or
  implied as current infra;
- do not clean up, import, destroy, rename, or otherwise mutate live
  Cloudflare, R2, Hyperdrive, Supabase, AWS, or Terraform state resources.

The implementer report says local verification passed and PR #26 is open. The
repo contains unrelated local modifications in files such as `agents/agent-loop.sh`,
future task prompts, specs, and `ai/state.json`. Preserve unrelated local work
and keep it out of verifier changes.

# Objective

Independently verify PR #26 against Task 0003.1. If it passes, merge PR #26,
confirm post-merge `main` CI no longer attempts to run the deleted Terraform
components, sync local `main` to `origin/main`, and leave the local repo clean
except for unrelated pre-existing local work.

# PR Boundary

This verifier task covers PR #26 only.

Allowed verifier changes are limited to a concise verifier report and any small
verification-only correction required to complete Task 0003.1. Do not start
Task 0004, add replacement Terraform components, reintroduce `infra/`, or make
live cloud changes.

# Read First

- `agents/orchestrator.md`
- `ai/tasks/task-0003.1.md`
- `ai/reports/task-0003.1-implementer.md`
- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/orun-golden-path.md`
- `specs/access-and-infra.md`
- `specs/components/00-foundation-and-tooling.md`
- `intent.yaml`
- `.github/workflows/ci.yml`
- PR #26 diff, commits, and CI logs
- GitHub Actions runs `26114594102` and `26113133649`

# Required Outcomes

- Confirm PR #26 maps to exactly one PR-sized task: delete the two legacy
  Terraform component directories and related active references only.
- Inspect the PR diff and current PR head, not just the local checkout.
- Confirm `infra/terraform/tf-state-r2/` and `infra/terraform/core/` are absent
  from active repo source in the PR.
- Confirm Orun discovery no longer includes `tf-state-r2`,
  `infra-tf-state-r2`, or `infra-terraform-core`.
- Confirm the rendered plan and CI behavior no longer schedule Terraform jobs
  for the deleted components.
- Confirm `.github/workflows/ci.yml` remains Orun-only and does not introduce
  direct pnpm, Terraform, Wrangler, Supabase, or AWS apply logic outside Orun.
- Confirm the deletion did not perform or require live Cloudflare, R2,
  Hyperdrive, Supabase, AWS, or Terraform state mutations.
- Inspect GitHub Actions logs for the current PR CI run and, if the PR merges,
  inspect the post-merge `main` CI logs to confirm the prior `409 Conflict`
  failure mode is gone.
- Write `ai/reports/task-0003.1-verifier.md`.

# Non-Goals

- No Task 0004 implementation in `aws-admin`.
- No S3 backend migration.
- No Supabase project or database creation.
- No live resource cleanup for the orphaned R2 bucket or Hyperdrive scaffold.
- No re-adding `infra/` discovery before a new in-scope Terraform component
  exists.
- No unrelated cleanup in `agents/agent-loop.sh`, future task files, or specs.

# Constraints

- Trust repo code, rendered Orun output, PR diff, and CI logs over stale docs.
- Do not commit generated `.orun/**`, `plan.json`, `.terraform/**`, state files,
  caches, or secrets.
- Preserve unrelated local/user changes.
- If PR CI is still running when verification starts, wait for the run to finish
  before making a PASS/FAIL decision.
- If verification requires a tiny fix, keep it strictly scoped to Task 0003.1,
  commit it on the PR branch, push, and wait for CI again before merging.
- Do not merge with unresolved blockers or without checking successful CI logs.

# Integration Notes

- The current roadmap remains blocked on Task 0003.1 until PR #26 is verified
  and merged.
- Task 0004 in `../aws-admin` should start only after PR #26 merges and `main`
  CI is green.
- The implementer removed `infra/` from `intent.yaml` discovery because no
  `infra/` directory remains in active repo source. That is acceptable for this
  deletion-only task if Orun validation and plans remain green.
- Historical references to the removed components may remain in prior reports,
  prompts, or clearly marked context notes. They must not make the components
  active again.

# Acceptance Criteria

- Local verification passes or exact blockers are reported:
  - `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  - `/Users/irinelinson/.local/bin/kiox -- orun component --intent intent.yaml`
  - `/Users/irinelinson/.local/bin/kiox -- orun plan --intent intent.yaml --output plan.json`
  - `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions`
  - `pnpm lint`
  - `pnpm typecheck`
  - `pnpm test`
  - `pnpm build`
- Rendered component output does not list `tf-state-r2`, `infra-tf-state-r2`, or
  `infra-terraform-core`.
- Current PR CI run `26114594102` finishes acceptably, and its logs show the
  expected Orun plan/run behavior with no Terraform jobs for the deleted
  components.
- If merged, post-merge `main` CI no longer attempts to recreate the existing R2
  bucket or run the deleted core component.
- No secrets, Terraform state, generated credentials, or generated Orun files
  are committed.
- PR #26 does not create or mutate live AWS, Cloudflare, or Supabase resources.
- If PASS, PR #26 is merged, local `main` is fast-forwarded from `origin/main`,
  and `git status --short` is clean except for unrelated pre-existing local work
  that is explicitly called out in the verifier report.
- If FAIL, PR #26 remains open and the report lists concrete blockers.

# Verification

Use the verifier merge protocol from `agents/orchestrator.md`:

- inspect prompt, PR, report, diff, and CI logs;
- run local gates;
- inspect successful GitHub Actions logs, not only summaries;
- verify that the legacy Terraform components are gone from active source and no
  live cleanup was attempted;
- write `ai/reports/task-0003.1-verifier.md`;
- merge only on PASS;
- sync local `main` after merge.

# When Done Report

Write `ai/reports/task-0003.1-verifier.md` with:

- Result: PASS or FAIL
- Checks
- Issues
- Risk Notes
- Spec Proposals
- Recommended Next Move
