# Task 0122

Agent: Implementer

## Current Repo Context

- Task 0121 (B7 audit-log filtering + export) is **VERIFIED PASS + MERGED** (PR
  #176, squash `2b98507`). Main is green, 0 open PRs, working tree clean as of
  bookkeeping commit `2b52d2b`.
- The **account security-events read backend is already fully shipped on main**
  (do NOT rebuild it):
  - DB: `packages/db/src/identity/repository.ts` `querySecurityEventsByUser`
    (cursor-keyset paginated, `SecurityEventPageQueryParams` / `…PagedResult`).
  - Worker/route: api-edge `apps/api-edge/src/auth-facade.ts` proxies
    `GET /v1/auth/security-events` (**actor-scoped**, backed by identity-worker;
    NOT org-scoped). Response envelope is `{ data: { securityEvents: [...] },
    meta: { requestId, cursor? } }` — the continuation cursor lives in
    `meta.cursor`, mirroring webhook deliveries / audit.
  - Contracts: `packages/contracts/src/security-events.ts` — `PublicSecurityEvent`
    (id, eventType, outcome, occurredAt, requestId, correlationId, ip, userAgent,
    metadata) + `SecurityEventListResponse { securityEvents: PublicSecurityEvent[] }`.
  - SDK: `packages/sdk/src/securityEvents.ts` `SecurityEventsClient.list()` EXISTS
    but is **flat** — it threads no `limit`/`cursor` query and discards
    `meta.cursor`, so it cannot paginate yet.
- **Consumer gap (the milestone):** the new Next.js console
  (`apps/web-console-next`) has **no account/security page** (its nav is entirely
  org-scoped: projects/members/invitations/api-keys/webhooks/config/audit/billing),
  and `packages/cli` has **no security-events command**. The old vanilla console
  surfaced this (Tasks 0046/0054) but that surface never carried to web-console-next.
- This milestone mirrors **Task 0120 (webhook delivery-history surfaces: SDK +
  Console + CLI in one combined PR)** byte-for-byte in shape. Use it as the
  prior-art template.

## Objective

Make the already-recorded **account security events** visible and usable to end
users by surfacing the existing read backend through the SDK (with cursor
pagination), the CLI, and the new Next.js Console — closing the B7 security-events
consumer leg end to end. No backend, contract-shape, or route changes.

## Milestone

**B7 — account security-events consumer surfaces (SDK pagination + CLI + Console).**

Deliver, end to end:
1. **SDK** — extend `SecurityEventsClient` so it can paginate: thread an optional
   `{ limit?, cursor? }` query into the `GET /v1/auth/security-events` request and
   surface the server `meta.cursor` to callers (mirror the Task 0120
   `listDeliveryAttemptsPage` / audit `iterAuditEntries` cursor seam — read
   `meta.cursor ?? null`, forward opaque cursor verbatim, never parse/construct
   it). Keep the existing flat `list()` working (additive, no breaking change) or
   add a sibling page method — implementer's call. Export any new types from
   `packages/sdk/src/index.ts`.
2. **CLI** — add a `sourceplane security events` (or equivalently-named) read-only
   command: human table + `--output=json`, `--limit`/`--cursor`, and `--all`
   cursor-follow loop with a seen-cursor guard, mirroring
   `packages/cli/src/commands/webhook-deliveries.ts` exactly. Pure SDK consumer
   (no fetch/header/auth/env). Actor-scoped: **no `--org` flag** (this surface is
   not org-scoped — do NOT call `resolveOrgId`). Register in `cli-runner.ts` with
   a help line.
3. **Console** — add an account-scoped **Security** page in `apps/web-console-next`
   surfacing the security-event history (eventType / outcome / occurredAt / ip /
   userAgent, safe metadata only), with a designed empty state (U4), skeleton
   (U8), and cursor **Load-more**. Dependency-free pure helper
   (`components/.../security-events.ts` shape) + the page, SDK-only via the
   existing `wrap()` pattern (**zero `fetch`**), mirroring the Task 0121
   `audit/page.tsx` + `components/audit/audit-log.ts` pair. Add a nav entry. Since
   the surface is **account-scoped not org-scoped**, place it at an account route
   (e.g. `/account/security` or `/security`) and add an appropriate nav affordance
   — implementer chooses the exact route, but it must NOT be nested under
   `/orgs/[orgSlug]/` since the data is not org-scoped.

## Milestone Boundary

In scope: SDK pagination plumbing for the existing security-events client; one CLI
command; one Console account-security page + helper + nav entry; tests for each
changed surface.

Out of scope / **hard exclusions**:
- NO changes to the security-events **contract shape** (`PublicSecurityEvent` /
  `SecurityEventListResponse` are byte-stable), the **api-edge route**
  (`auth-facade.ts` security-events entry unchanged), the **identity-worker**, or
  the **DB** layer. This is a read-consumer milestone only.
- NO org-scoping of the surface (it is actor/account-scoped by design) and NO new
  org-scoped route. Do NOT call `resolveOrgId` in the CLI command.
- NO `querySecurityEventsByUser` SQL / cursor-encoding / DEFAULT_LIMIT / MAX_LIMIT
  change.
- NO audit-log surface change (Task 0121 is closed); NO webhook surface change.
- NO touching `ai/deferred.md` zones or `infra/terraform/cloudflare-domain/**` /
  the cloudflare provider pin.
- NO new audit-entry fields, write/mutation paths, or analytics.

## Read First

- `ai/tasks/task-0120.md` + `ai/reports/task-0120-implementer.md` — the byte-for-byte
  shape template (SDK page method + Console panel + CLI cursor-loop in one PR).
- `ai/tasks/task-0121.md` + `apps/web-console-next/src/app/(app)/orgs/[orgSlug]/audit/page.tsx`
  + `apps/web-console-next/src/components/audit/audit-log.ts` — Console
  page+helper pattern (SDK-only, wrap(), Load-more, empty state, skeleton).
- `packages/cli/src/commands/webhook-deliveries.ts` — the CLI cursor-pagination
  prior-art to mirror (human table / `--output=json` / `--limit` / `--cursor` /
  `--all` seen-cursor guard).
- `packages/sdk/src/securityEvents.ts`, `packages/sdk/src/transport.ts`
  (`requestWithMeta` / `meta.cursor` seam, `query` field), `packages/sdk/src/index.ts`.
- `packages/contracts/src/security-events.ts` — the locked shape.
- `apps/web-console-next/src/components/shell/sidebar.tsx` — nav registration.
- `specs/roadmap.md` B7; `specs/orun-golden-path.md` (component manifests /
  changed-plan); `agents/orchestrator.md` (Implementer Standard, PR Creation
  Requirement).

## Required Outcomes

- [ ] SDK `SecurityEventsClient` can paginate (`limit`/`cursor` query threaded;
      `meta.cursor` surfaced as `nextCursor ?? null`); new types exported from
      `packages/sdk/src/index.ts`; existing `list()` behaviour preserved.
- [ ] CLI security-events read command registered + help line; human + json
      output; `--limit`/`--cursor`/`--all` with seen-cursor guard; pure SDK
      consumer, no `--org`/`resolveOrgId`.
- [ ] Console account-security page (non-org route) + dependency-free helper + nav
      entry; SDK-only via `wrap()` (zero `fetch`); empty state + skeleton +
      cursor Load-more.
- [ ] Tests for each changed surface: SDK page method (cursor read + verbatim
      forward), CLI command (success human/json, `--all` loop, seen-cursor guard,
      usage errors, `--limit` validation), Console helper (row mapping, cursor
      accumulation, empty state).
- [ ] `ai/reports/task-0122-implementer.md` written and committed **on the PR
      branch** with the real PR number.

## Non-Goals

- No backend/route/contract/DB change of any kind.
- No org-scoping; no new org-scoped page.
- No webhook or audit surface change.
- No new component package (reuse existing `sdk`, `cli`, `web-console-next`
  components — no new top-level discovery-root directory, so no new
  `component.yaml` needed; if you DO add a new workspace package, it MUST ship a
  `component.yaml` mirroring an adjacent peer).

## Constraints

1. Cursor is **opaque**: read `meta.cursor ?? null`, forward verbatim, never
   parse or construct it (mirror Task 0120/0121).
2. Console page is **SDK-only** via `wrap()` — zero `fetch(`, no bespoke API
   client, no signing-secret/credential/raw-token render (security metadata is
   pre-redacted by the worker; render only the safe `PublicSecurityEvent` fields).
3. No new `eslint-disable` / `@ts-ignore` / `@ts-expect-error` / `as any` /
   `as unknown as` in production source.
4. Revert any `kiox.lock` mutation; do NOT commit `plan.json`.
5. The web-console-next leg is **deploy-gated** (`cloudflare-pages-turbo`,
   `verify` on PR / `deploy` on push-main): PR-CI proves build/typecheck only;
   the deploy + live-URL is the **verifier's** post-merge gate. BEHIND-main rebase
   is the **verifier's** responsibility (recurring 0103–0121).
6. Real PR number required in the report (`TBD` / `PR #[PR]` = BLOCKED). After
   `gh pr create`, update the report + any placeholder with the real number,
   commit + push.

## Integration Notes

- Multi-component change: Orun changed-plan will select `sdk` + `cli` (turbo
  packages) + `web-console-next` (cloudflare-pages turbo, deploy-gated) + any
  test harness component touched. That union is expected/correct — do NOT force it
  to one component.
- May land as one combined PR (Task 0120 precedent) or an SDK-before-consumers
  sequence — implementer's call. The milestone is not complete until the full
  "Required Outcomes" list is satisfied and all PR(s) are open/merged on GitHub.
- Carry-forward nit you MAY fold in if you touch `cross-reads.ts` (optional, not
  required): its `parseAuditFilterFlags` doc-comment says malformed input
  "surfaces as a 400" — the worker actually returns 422. Comment-only.

## Acceptance Criteria

✅ `pnpm install --frozen-lockfile` clean; `pnpm -r typecheck` = 0.
✅ `pnpm --filter @saas/sdk test`, `pnpm --filter @saas/cli test`, and the
   web-console-next test harness all green, with **net-new** passing cases for
   each changed surface (no case-count regressions elsewhere).
✅ `pnpm -r --no-bail lint` adds **0 new** warnings (baseline warnings in
   `tests/api-edge/**` are pre-existing and acceptable).
✅ Local Orun gates green:
   - `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
   - `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json`
     (selects sdk + cli + web-console-next [+ test harness] lanes)
   - `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions`
✅ CLI command works end to end against a stubbed SDK in tests: human table, json,
   `--all` cursor loop terminating on `null`, seen-cursor guard, usage errors.
✅ Console page renders the security-event list with empty state + skeleton +
   Load-more; zero `fetch(` in the new console source (SDK-only via `wrap()`).
✅ PR CI green (`gh run view --log` shows the lanes actually ran, not a no-op).
✅ Commit history clean; `kiox.lock` reverted; no `plan.json` committed.

## Verification

The Verifier will (8-phase, deploy-gated adaptation per
`references/post-merge-deploy-profile-gap.md`): confirm the file boundary matches
this milestone; scan forbidden zones (contracts security-events shape, api-edge
auth-facade, identity-worker, DB identity layer all UNCHANGED); confirm SDK reads
`meta.cursor` and forwards opaque cursor verbatim; confirm CLI is a pure SDK
consumer with no `--org`/`resolveOrgId`; confirm Console is SDK-only (zero
`fetch`) and renders only safe redacted fields; run quality + Orun gates; inspect
PR-CI logs; **wait for post-merge main-CI + curl the live Console security route**
before PASS; rebase BEHIND-main as needed; merge in dependency order; sync main.

## PR Creation Requirement

You MUST create a branch (suggested `impl/task-0122-security-events-surfaces`),
commit, push, and open at least one GitHub PR before reporting complete.
`PR Number: TBD` is not acceptable. After `gh pr create`, update
`ai/reports/task-0122-implementer.md` and any placeholder PR numbers with the real
number, then commit + push that final update.

## When Done Report

Write `ai/reports/task-0122-implementer.md` with: Summary (3–5 bullets), Files
Changed (grouped by subsystem), Checks Run (exact commands + results), Assumptions
(durable only), Spec Proposals (links + one-line reason, if any), Remaining Gaps
(actionable residual risk only), Next Task Dependencies, PR Number (real GitHub PR
number).
