# Task ID

Task 0005

# Agent

Implementer

# Current Repo Context

Task 0004 is verified and merged in `../aws-admin`. The repo-scoped IAM
component for `sourceplane/multi-tenant-saas` now exists in AWS and the exact
role identifiers are known.

Verified AWS account and role ARNs:

- Account: `306024784101`
- Plan roles:
  - `arn:aws:iam::306024784101:role/dev-github-sourceplane-multi-tenant-saas-plan`
  - `arn:aws:iam::306024784101:role/stage-github-sourceplane-multi-tenant-saas-plan`
  - `arn:aws:iam::306024784101:role/prod-github-sourceplane-multi-tenant-saas-plan`
- Deploy roles:
  - `arn:aws:iam::306024784101:role/dev-github-sourceplane-multi-tenant-saas-production-deploy`
  - `arn:aws:iam::306024784101:role/stage-github-sourceplane-multi-tenant-saas-production-deploy`
  - `arn:aws:iam::306024784101:role/prod-github-sourceplane-multi-tenant-saas-production-deploy`

Task 0003.1 should already have deleted the old `tf-state-r2` and
`infra-terraform-core` component source. There is no active Terraform component
left in this repo today, and `infra/` was intentionally removed from
`intent.yaml` discovery when the old components were deleted. This task must
restore the `infra/` discovery seam and establish the AWS/S3 Terraform path for
new components before Supabase infrastructure is created fresh.

# Objective

Reintroduce repo-owned Terraform execution in `multi-tenant-saas` on the AWS
golden path: restore infra discovery, wire AWS OIDC credential assumption into
the Orun Terraform composition, add one bounded bootstrap Terraform component
that uses the shared S3 backend, and verify S3 plus AWS Secrets Manager access.

# PR Boundary

One PR in `multi-tenant-saas`.

This task owns the minimum repo changes needed to make Terraform components in
this repo runnable through Orun with AWS credentials and the shared S3 backend.
That includes `intent.yaml`, the local Terraform composition/profile/job
contract, one bootstrap Terraform component under `infra/terraform/**`, related
docs, and CI-visible execution behavior. It does not create the Supabase
database/project.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/tasks/task-0003.1.md`
- `ai/reports/task-0003.1-verifier.md`
- `ai/tasks/task-0003.md`
- `ai/tasks/task-0004.md`
- `ai/reports/task-0004-verifier.md`
- `specs/orun-golden-path.md`
- `specs/access-and-infra.md`
- `specs/repo.md`
- `intent.yaml`
- `.github/workflows/ci.yml`
- `kiox.yaml`
- `stack-tectonic/compositions/terraform/**`
- `infra/terraform/**`
- `../aws-admin/intent.yaml`
- `../aws-admin/domains/access/github-repositories/sourceplane-multi-tenant-saas/**`

# Required Outcomes

- Re-add `infra/` to `intent.yaml` discovery roots so repo-owned Terraform
  components under `infra/terraform/**` are discoverable again.
- Keep Terraform execution inside Orun. Wire AWS credential setup into the local
  Terraform composition/job path so the rendered plan makes the AWS assumption
  step visible before `terraform init`.
- Prefer the same credential pattern documented by `aws-admin`, using
  `aws-actions/configure-aws-credentials@v4` or an equivalent explicit Orun job
  step rather than hidden workflow shell.
- Ensure PR/plan jobs use the verified environment plan roles and apply jobs use
  the verified deploy roles, consistent with current `aws-admin` trust subjects.
- Add one minimal bootstrap Terraform component under `infra/terraform/**` whose
  purpose is to prove backend and Secrets Manager access without creating the
  Supabase project yet. Keep it tightly scoped to Task 0005.
- Configure the bootstrap component and local Terraform composition to use AWS
  S3 backend settings compatible with `aws-admin`:
  - bucket: `sourceplane-<env>`;
  - key: `multi-tenant-saas/<component>/terraform.tfstate`;
  - `workspace_key_prefix = "env"`;
  - `encrypt = true`;
  - `use_lockfile = true`;
  - region from `AWS_REGION` or Terraform parameters, default `us-east-1`.
- Update the repo-local Terraform composition or schema only as needed for AWS
  credential assumption and backend support. Keep the change minimal and aligned
  with `../aws-admin/stacks/aws-admin-terraform/**`.
- Verify Task 0003.1 left no active R2-specific backend components or docs that
  imply Cloudflare R2 Terraform state is still current for active repo usage.
- Update Terraform component manifests and READMEs to document the S3 backend,
  AWS role dependency, and secret namespace.
- Verify access to AWS through the assumed role:
  - `aws sts get-caller-identity`;
  - read/list the expected S3 state bucket for at least `dev`;
  - read and write a scoped Secrets Manager smoke secret under
    `sourceplane/multi-tenant-saas/access-smoke/dev`, then clean it up or record
    why it intentionally remains.
- Update `ai/context/*` and write the implementer report.

# Non-Goals

- No AWS IAM role or policy creation. That was Task 0004 in `aws-admin`.
- No Supabase database/project creation. That is Task 0006.
- No broad multi-component infra rollout. One minimal bootstrap Terraform
  component is enough if it proves the backend and secret path.
- No Cloudflare Hyperdrive wiring unless required only to remove stale R2
  references.
- No domain code, migrations, product behavior, or `specs-v2/**` work.

# Constraints

- Do not commit AWS credentials, secret values, Terraform state, `.terraform/**`,
  `.orun/**`, or `plan.json`.
- Treat role ARNs as non-secret but avoid burying account-specific values in
  multiple locations if the composition can derive them cleanly from typed
  parameters or environment context.
- Keep PR and push behavior visible in the rendered Orun plan.
- If an S3 bucket does not exist, do not create it here. It is owned by
  `aws-admin`; record the blocker and stop before widening scope.
- If Secrets Manager write verification would leave a production artifact, use
  `dev` only and document the exact cleanup behavior.
- Do not widen `multi-tenant-saas` CI into direct Terraform shell outside Orun.
- The deploy trust subject currently uses GitHub environment `production`; do
  not change that in this repo. If it blocks apply behavior, document the gap
  precisely instead of guessing a new trust model.

# Integration Notes

- `aws-admin` uses `parameterDefaults.terraform` for `awsRegion`, `orgName`,
  `namespace`, `owner`, and `repo`; mirror that shape in this repo.
- The effective state object path should be:
  `env/<environment>/multi-tenant-saas/<component>/terraform.tfstate`.
- Task 0003.1 intentionally removed `infra/` from discovery; Task 0005 must add
  it back as part of reintroducing a new in-scope Terraform component.
- `stack-tectonic/compositions/terraform/**` is the active local execution seam;
  if AWS role assumption needs to happen before `terraform init`, encode it
  there so the job DAG shows it.
- The S3 backend setup is a prerequisite for the Supabase Terraform
  component in Task 0006.

# Acceptance Criteria

- No active Terraform component or README claims Cloudflare R2 is the Terraform
  backend.
- `intent.yaml` once again discovers `infra/` and Orun discovers the new
  bootstrap Terraform component.
- Terraform backend blocks use AWS S3 with native lockfile support.
- Rendered Orun plans show AWS credential setup before Terraform init/plan/apply
  for Terraform components.
- `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  passes.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --intent intent.yaml --view dag`
  passes and shows expected Terraform dependencies.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent
  intent.yaml --output plan.json` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run
  --runner github-actions` passes or records a true no-op.
- Targeted Terraform `fmt`, `init`, `validate`, and `plan` pass for migrated
  components with S3 backend access.
- AWS STS, S3 state bucket, and Secrets Manager smoke checks are recorded with
  no secret values.
- PR CI passes.

# Verification

The verifier must inspect the rendered Orun plan, CI logs, backend files,
component manifests, README updates, and AWS read/write smoke evidence. PASS
requires proving the repo has a working Terraform seam again under `infra/`, is
no longer on R2 for active Terraform backend usage, and can use the
`aws-admin` roles for S3 plus Secrets Manager.

# When Done Report

Write `ai/reports/task-0005-implementer.md` with:

- Summary
- Files Changed
- Backend Setup Notes
- AWS Role And Access Verification
- Orun Plan Impact
- Checks Run
- Assumptions
- Remaining Gaps
- Next Task Dependencies
- PR Number
