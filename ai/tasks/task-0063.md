# Task 0063

Agent: Implementer

## Current Repo Context

- Task 0062 verified PASS and merged via PR #105 at `3200d3c`; follow-up docs/state commit `b546e77` is on `main` and main CI run `26580938104` passed.
- The repo is green with no open GitHub PRs at orchestrator handoff time.
- Local checkout is `main` at `b546e77` with unrelated untracked historical `ai/tasks/*` and `ai/reports/*` carryover files from prior cycles. Do not stage, delete, or modify unrelated carryover files.
- `apps/web-console` is still a plain TypeScript/DOM Vite app. Task 0062 added a Figma-inspired app shell: sidebar navigation, topbar context badges, dark theme tokens, and responsive shell behavior.
- The live app already supports sign-in/session, organization create/select, members, invitations, projects/environments, API keys, audit, account profile/security, and config settings/feature flags/secret metadata through the public `api-edge` contracts.
- The Figma reference bundle remains at `referance/figma/` (misspelled in repo). It is a React/Tailwind prototype and must be used as visual/layout reference only.

## Objective

Polish the existing web-console workspace and account page internals using `referance/figma` as the visual reference, without adding new backend behavior or fake product surfaces.

Task 0062 improved the outer shell. Task 0063 should make the live workflow pages themselves feel like the Figma console: page headers, action bars, cards, list/table rows, tabs, section copy, loading/empty/error states, and responsive spacing for the existing SaaS starter functionality.

## PR Boundary

One PR should update only the frontend presentation layer and minimal supporting DOM structure in `apps/web-console`.

In scope:

1. Restyle and lightly restructure the existing live pages/tabs:
   - auth/login and manual-token import;
   - organization create/select;
   - workspace tabs: Members, Invitations, Projects, API Keys, Audit, Config;
   - account pages: Profile and Security Events.
2. Add consistent Figma-inspired page primitives in the current DOM code, such as:
   - page header/title/subtitle/action-row patterns;
   - cards/panels for forms and lists;
   - toolbar/action layouts for create and destructive flows;
   - table-like/list-row metadata layouts for members, invitations, projects, API keys, audit events, security events, settings, feature flags, and secret metadata;
   - coherent loading, empty, success, error, debug-token, and one-time-secret states;
   - responsive two-column/card-grid behavior where it improves readability.
3. Align page-level visual treatment with these exact reference files:
   - `referance/figma/src/app/pages/SettingsPage.tsx` — tabs, member/API-key/webhook-style cards, settings sections, danger-zone styling.
   - `referance/figma/src/app/pages/CatalogPage.tsx` — page header, filter/action bars, cards/tables, status/score metadata density.
   - `referance/figma/src/app/pages/ComponentDetailPage.tsx` — detail header, metric cards, tabbed content, status sections.
   - `referance/figma/src/app/components/TopBar.tsx` and `Sidebar.tsx` — keep page internals consistent with the shell already landed in Task 0062.
   - `referance/figma/src/styles/theme.css` — colors/radius/typography/status token cues.
4. Keep the production app's existing TypeScript/DOM architecture. Use small helper functions/classes if they reduce duplication and keep safe rendering readable.
5. Preserve all existing public API calls, request bodies, response handling, auth/session behavior, org/project selection, and tab/account navigation semantics.
6. Preserve the exact secret-safety constraints already established:
   - one-time API key secret appears only immediately after create and clears on tab/org navigation;
   - secret metadata remains read-only;
   - config values render safely and mutation drafts are not persisted to localStorage/sessionStorage;
   - dynamic values render through text nodes or safe form values, never unsafe HTML.
7. Write `ai/reports/task-0063-implementer.md` with files changed, Figma references used, behavior preserved, checks run, and the real PR number.

Explicit non-goals:

- No backend changes in Workers, contracts, database, migrations, Terraform, Cloudflare, Supabase, Orun components, or API routes.
- No React migration, router migration, Tailwind setup, shadcn/Radix/lucide import, package dependency change, or framework overhaul.
- No new Catalog/Runs/Deployments/Repositories product features from the Figma prototype.
- No fake data surfaces or non-functional command/search/filter chrome unless attached to an existing live workflow.
- No secret metadata create/edit/reveal/rotate/revoke UI.
- No plaintext secret display except the existing one-time API key creation result and existing debug delivery tokens where the backend explicitly returns them.
- No `innerHTML`, `insertAdjacentHTML`, `eval`, `Function`, dynamic markdown/HTML rendering, or user-controlled class/script injection.
- No unrelated cleanup of pre-existing untracked historical `ai/` files.

## Read First

- `agents/orchestrator.md` — implementer standard and PR creation/reporting requirements.
- `ai/context/current.md`, `ai/context/task-ledger.md`, `ai/context/decisions.md`, `ai/context/open-risks.md` — current repo state and constraints.
- `ai/tasks/task-0062.md`, `ai/reports/task-0062-verifier.md` — shell design baseline and visual-reference constraints.
- `ai/tasks/task-0058.md`, `ai/reports/task-0058-verifier.md` — Config tab read-only/secret-metadata constraints.
- `ai/tasks/task-0061.md`, `ai/reports/task-0061-verifier.md` — config mutation UI and safe-rendering constraints.
- `specs/components/12-web-console.md` — public-API-only constraint and visible scope requirement.
- `specs/product-overview.md` — UX baseline and required starter capabilities.
- `specs/constitution.md` — API/UI parity, secure-by-default, extraction seams.
- `specs/orun-golden-path.md` — Orun validation workflow.
- `apps/web-console/src/main.ts` — current DOM rendering and workflow behavior.
- `apps/web-console/src/style.css` — Task 0062 theme/shell styles.
- `apps/web-console/src/api.ts` and `apps/web-console/src/state.ts` — ensure this task does not alter public API behavior or persistence rules.
- Figma references:
  - `referance/figma/README.md`
  - `referance/figma/src/styles/theme.css`
  - `referance/figma/src/app/pages/SettingsPage.tsx`
  - `referance/figma/src/app/pages/CatalogPage.tsx`
  - `referance/figma/src/app/pages/ComponentDetailPage.tsx`
  - `referance/figma/src/app/components/Sidebar.tsx`
  - `referance/figma/src/app/components/TopBar.tsx`

## Required Outcomes

- [ ] Existing live workflows remain reachable and behaviorally unchanged.
- [ ] Workspace/account page internals use consistent Figma-inspired headers, cards, rows, tabs, forms, and action bars.
- [ ] Members/invitations/projects/API keys/audit/config/account/security pages have clear empty/loading/error/success states.
- [ ] Organization/project/environment/auth context remains visible through the Task 0062 topbar while page sections make their own scope clear where relevant.
- [ ] API key one-time secret lifecycle remains safe and is visually clearer.
- [ ] Secret metadata remains read-only; no secret value reveal or mutation controls are introduced.
- [ ] Dynamic values still use safe DOM text/form values, not unsafe HTML.
- [ ] Config mutation drafts are not persisted to localStorage/sessionStorage.
- [ ] No public API route, request, response, contract, backend, package, or infrastructure behavior changes.
- [ ] Layout is usable at desktop/tablet/mobile widths; controls do not become unreachable.
- [ ] Implementer report is committed to the PR branch after the real PR number is known.

## Implementation Guidance

- Treat `referance/figma` as a visual language reference. Do not copy its React code, mock product data, routes, or dependency stack into the production app.
- Prefer CSS and small DOM-class additions over broad TypeScript rewrites.
- If introducing helper functions, keep them UI-only and safe: e.g. `pageHeader()`, `card()`, `toolbar()`, `statusBadge()`, `metadataGrid()`, or `emptyState()`.
- Keep existing `h()` behavior and text-node construction. Do not introduce an HTML templating shortcut for dynamic values.
- Remove or reduce duplicate navigation chrome only if the sidebar/topbar already provides the same live navigation and the workflow remains reachable.
- Figma `SettingsPage.tsx` includes mock webhooks and product-specific text; use only card/list/tab/action styling cues, not its mock data or unimplemented webhook management behavior.
- Figma `CatalogPage.tsx` and `ComponentDetailPage.tsx` are product-direction references. Use only page density/card/table/status patterns; do not introduce catalog/runtime features.

## Acceptance Criteria

Targeted web-console checks pass locally:

```bash
pnpm --filter @saas/web-console typecheck
pnpm --filter @saas/web-console build
pnpm --filter @saas/web-console lint
```

Run Orun validation and changed-plan/dry-run for the touched web-console files:

```bash
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun component --intent intent.yaml --long
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
```

The changed plan should include only the web-console verification/deploy path for this frontend-only change, or the report must explain any narrower/empty plan evidence.

PR CI must pass before reporting complete.

## Verification

The Verifier will:

- Review the PR diff against this prompt and confirm it is web-console presentation/UI-only.
- Confirm Figma reference files were used for visual/layout cues only, with no framework migration or Figma-only product behavior.
- Confirm all current live workflows remain reachable and current API client behavior is unchanged.
- Confirm account/workspace tabs, creates, edits, destructive actions, pagination/load-more, and org/project selection still behave coherently.
- Confirm secret metadata remains read-only and no secret reveal/mutation behavior was introduced.
- Confirm one-time API key secret and debug-token displays remain constrained to existing backend-returned paths.
- Confirm no unsafe dynamic rendering path or draft persistence regression was introduced.
- Run targeted web-console checks and Orun validation/dry-run.
- Inspect PR CI with `gh`, not only local checks.
- If PASS and CI passes, merge the PR, sync local `main`, and inspect post-merge main CI enough to confirm the web-console path remains green.

## PR Creation Requirement

Create or reuse a task branch, commit only task-scoped changes, push the branch, and open a GitHub PR before reporting complete. If PR creation is blocked, mark the implementer report as BLOCKED with the exact command/error; do not report the task complete locally without a PR.

After creating the PR, update `ai/reports/task-0063-implementer.md` with the real PR number, commit that report to the PR branch, and push it. `PR Number: TBD` is not acceptable for a completed task.

## When Done Report

Write `ai/reports/task-0063-implementer.md` with these sections:

- Summary
- Files Changed
- Figma References Used
- UI Behavior Preserved
- Secret Handling and Safe Rendering Review
- Checks Run
- Orun / CI Evidence
- Assumptions
- Spec Proposals
- Remaining Gaps
- Next Task Dependencies
- PR Number
