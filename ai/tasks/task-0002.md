# Task ID

Task 0002

# Agent

Implementer

# Current Repo Context

`main` is green at `b68794c`. Task 0001 and Task 0001.1 are verified and merged. The repo has the initial pnpm/Turbo/Orun scaffold, committed local `stack-tectonic/`, provider-style `kiox.yaml`/`kiox.lock`, and Orun-only CI that now passes plan plus matrix jobs.

Human intervention established an important operational baseline that must be preserved:

- `intent.yaml` uses local `stack-tectonic/` via `kind: dir` / `path: stack-tectonic/`.
- `.orun/` is generated local state and is ignored.
- Cloudflare account ID is `f9270f828799775bebf9315248fdf717`.
- Primary Hyperdrive resource name is `sourceplane-db`.
- A V1 Supabase Postgres database already exists behind that Hyperdrive resource.

# Objective

Discover, document, and begin Terraform adoption for the existing Cloudflare/Supabase infrastructure baseline without blindly recreating the database or `sourceplane-db`.

# PR Boundary

One PR adds the first real infra baseline adoption scaffold under `infra/terraform/**` and updates only directly related docs/reports.

This is not a domain feature PR and not a broad local-stack redesign.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/constitution.md`
- `specs/repo.md`
- `specs/access-and-infra.md`
- `specs/components/00-foundation-and-tooling.md`
- `infra/terraform/state/README.md`
- `infra/terraform/core/README.md`
- `infra/terraform/state/component.yaml`
- `infra/terraform/core/component.yaml`
- `intent.yaml`
- `kiox.yaml`
- `stack-tectonic/compositions/terraform/compositions.yaml`

# Required Outcomes

- Preserve the local `stack-tectonic/` model. Do not switch `intent.yaml` back to OCI and do not commit generated `.orun/` files.
- Perform read-only discovery of the Cloudflare baseline with `wrangler` or the Cloudflare API:
  - confirm the account ID in use,
  - inspect whether `sourceplane-db` Hyperdrive exists,
  - inspect whether the Terraform state R2 bucket exists or must be created later.
- Inspect Supabase only as far as available authenticated tooling allows. Do not log secrets or database passwords.
- Add minimal Terraform files under `infra/terraform/state` and `infra/terraform/core` so both components validate through the existing Orun Terraform jobs.
- Represent existing resources conservatively. If a resource already exists, prefer import/adoption notes or Terraform import blocks when the provider supports them safely; do not define a create-new resource that would collide with `sourceplane-db`.
- Keep non-secret IDs, names, and account config in committed files only when safe. Keep secrets in CI/platform config.
- Update the infra READMEs with the observed baseline, adoption status, and exact next apply/import step.
- Write the implementer report at `ai/reports/task-0002-implementer.md`.

# Non-Goals

- No identity, organization, membership, project, billing, audit, webhook, notification, runtime, resource, or admin/support behavior.
- No database migrations or schema creation.
- No destructive Terraform apply, import, or state migration without first proving the exact target resources and recording the commands.
- No creation of a second Supabase database, second primary Hyperdrive resource, or duplicate R2 state bucket.
- No product-specific `specs-v2/**` work.

# Constraints

- Treat live Cloudflare/Supabase inspection as high signal. Trust observed resource state over stale docs.
- Use Orun as the only CI path. Do not add direct Terraform or Wrangler GitHub Actions workflow steps.
- Keep the PR small: infra baseline adoption scaffold, read-only verification, and docs/report only.
- Do not commit `plan.json`, `.orun/**`, `.workspace/**`, Terraform state files, credentials, or generated caches.
- If exact resource IDs or Supabase details are not discoverable with available credentials, do not guess. Record the blocker and leave a precise follow-up or human-input note.

# Integration Notes

- Current Terraform composition runs `terraform fmt -check`, `terraform init -backend=false`, and `terraform validate` in each component workdir.
- `infra/terraform/state/component.yaml` and `infra/terraform/core/component.yaml` already use `type: terraform` and subscribe to `dev`.
- `infra/terraform/core` depends on `infra-terraform-state`; preserve that edge unless inspection proves it is wrong.
- The existing Hyperdrive name `sourceplane-db` is the primary database binding name for future Workers.

# Acceptance Criteria

- `infra/terraform/state` and `infra/terraform/core` contain real Terraform config that passes `terraform fmt -check`, `terraform init -backend=false`, and `terraform validate`.
- Read-only Cloudflare discovery results are recorded in `infra/terraform/**/README.md` and `ai/reports/task-0002-implementer.md`.
- `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions` passes or records a true no-op result if no jobs are planned.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` still pass locally unless an unrelated external blocker is documented with exact output.
- PR CI passes through the Orun workflow.
- No live resource is duplicated or destroyed.

# Verification

The verifier should inspect the PR diff, implementer report, local Orun/Terraform results, and GitHub Actions logs. If the PR adds or changes live Cloudflare/Supabase resources, the verifier must independently inspect those resources; for a read-only adoption scaffold, the verifier should independently confirm the recorded discovery where credentials allow.

# When Done Report

Write `ai/reports/task-0002-implementer.md` with:

- Summary
- Files Changed
- Checks Run
- Observed Cloudflare/Supabase Baseline
- Assumptions
- Spec Proposals
- Remaining Gaps
- Next Task Dependencies
- PR Number
