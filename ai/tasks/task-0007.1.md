# Task ID

Task 0007.1

# Agent

Implementer

# Current Repo Context

Human input resolved the block: continue from the existing local Task 0007
draft work and drive it to a PR. Do not discard or regenerate the Task 0007
implementation from the last merged state.

The local worktree is on `main` at
`29cd21c125423adfd7a17b1a1b12707d63f7982d` with uncommitted Task 0007 draft
artifacts:

- `README.md`
- `pnpm-lock.yaml`
- `ai/tasks/task-0007.md`
- `ai/reports/task-0007-implementer.md`
- `packages/db/**`
- `tests/db/**`
- compact context/state updates under `ai/context/**`, `ai/state.json`, and
  `ai/waiting_for_input.md`

The draft appears to add `packages/db` and `tests/db`, but it has not been put
on a task branch and no GitHub PR exists. It also produced ignored generated
artifacts under `packages/db/dist/`, `packages/db/node_modules/`,
`tests/db/node_modules/`, and TypeScript build info files. Those ignored
artifacts must not be staged or committed.

The orchestrator contract now requires every implementer task to create a
GitHub PR before it can be considered complete. `PR Number: TBD` is not an
acceptable completed report value.

# Objective

Finish the existing local Task 0007 database migration harness work and open
one GitHub PR for it.

# PR Boundary

One PR in `multi-tenant-saas` that completes Task 0007.

Allowed scope:

- finish and correct the existing `packages/db` migration harness;
- finish and correct the existing `tests/db` migration verifier;
- update package metadata and lockfile only as required by those packages;
- update `README.md` only for current infrastructure and DB migration harness
  facts;
- update `ai/reports/task-0007-implementer.md` with final commands and the PR
  number;
- keep the existing Task 0007 prompt/context artifacts if they are part of the
  local orchestration history.

Non-task generated outputs such as `dist`, `node_modules`, `.orun`, local
Terraform directories, `plan.json`, and TypeScript build info files must stay
uncommitted.

# Read First

- `agents/orchestrator.md`
- `ai/tasks/task-0007.md`
- `ai/reports/task-0007-implementer.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/constitution.md`
- `specs/repo.md`
- `specs/domain-model.md`
- `specs/contracts/tenancy-and-rbac.md`
- `specs/components/00-foundation-and-tooling.md`
- `specs/orun-golden-path.md`
- `packages/db/**`
- `tests/db/**`

# Required Outcomes

- Create or switch to a task branch before committing, using the repo branch
  convention such as `codex/task-0007.1-db-migration-harness`.
- Preserve the existing Task 0007 implementation direction unless verification
  exposes a concrete issue.
- Ensure `packages/db` is Worker-safe at its public exports and does not import
  Node-only modules from its default public entry points.
- Ensure the migration manifest convention is deterministic and the verifier
  catches ordering, duplicate IDs, missing files, checksum drift, missing owner
  context, and project-scoped `org_id + project_id` invariant issues.
- Review `packages/db/component.yaml` and `tests/db/component.yaml` against the
  local Stack Tectonic schema. Do not add unsupported component parameters.
- If `dependsOn` is supported for this component relationship, add the smallest
  correct dependency edge from `packages/db` to the DB test component. If it is
  not supported or would be speculative, document that in the report.
- Re-run focused package checks and Orun validation/changed planning.
- Open a GitHub PR for the task branch.
- Update `ai/reports/task-0007-implementer.md` so `PR Number` contains the real
  PR number and URL. If PR creation fails, mark the report `BLOCKED` with the
  exact command/error instead of calling the task done.

# Non-Goals

- No live Supabase schema apply.
- No direct database credentials, Secrets Manager payload values, or connection
  strings.
- No Cloudflare Hyperdrive Terraform component.
- No Worker database adapter implementation.
- No identity, membership, projects, policy, events, billing, or UI feature
  behavior.
- No changes to `specs-v2/**`.
- No broad Orun composition or CI refactor.

# Constraints

- Do not revert user or existing draft work unless it is clearly generated
  output or a concrete task-scope defect.
- Do not commit ignored build artifacts or dependency directories.
- Do not commit `.orun/**`, `.workspace/**`, Terraform working directories, or
  local `plan.json`.
- Keep the PR reviewable as a Task 0007 completion PR, not a new feature.
- If the draft implementation meaningfully changes persistence ownership,
  migration strategy, or bounded-context scope beyond Task 0007, write
  `ai/proposals/task-0007-spec-update.md` and keep the implementation
  conservative.

# Integration Notes

- `dev` has no Supabase project; tests must not need a live database.
- The root README should describe current state accurately: Supabase `stage`
  and `prod` exist; Hyperdrive and live migration apply do not.
- `ai/waiting_for_input.md` has been unblocked by the user's answer. Keep it as
  a no-input-requested note.
- Task 0007.1 is not a new product feature; it is the path to complete and PR
  the already-started Task 0007 work.

# Acceptance Criteria

- A task branch exists and contains only Task 0007/0007.1-scoped committed
  changes.
- A GitHub PR exists for that branch.
- `packages/db` and `tests/db` pass their focused build/typecheck/lint/test
  checks.
- Orun validates the repo and the changed plan/dry-run reflects the DB package
  and DB test component changes.
- No ignored generated outputs are staged or included in the PR diff.
- `ai/reports/task-0007-implementer.md` contains final checks and the real PR
  number/URL.

# Verification

Run at minimum:

```bash
git status --short --ignored
pnpm install
pnpm --filter @saas/db build
pnpm --filter @saas/db typecheck
pnpm --filter @saas/db lint
pnpm --filter @saas/db-tests test
pnpm --filter @saas/db-tests typecheck
pnpm --filter @saas/db-tests lint
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
gh pr view <created-pr-number> --json number,title,state,url,headRefName,baseRefName
```

If a command must be adjusted because package names or local tooling differ,
record the exact command and reason in the report.

# PR Creation Requirement

This task is not complete until a GitHub PR exists. Push the task branch and
open a PR against `main`. The implementer report must contain the PR number and
URL. If GitHub access or branch state prevents PR creation, stop and report the
blocker with the failing command output.

# When Done Report

Update `ai/reports/task-0007-implementer.md` with:

- Summary
- Files Changed
- Migration Layout
- Ownership And Boundary Notes
- Generated Artifact Review
- Checks Run
- Assumptions
- Spec Proposals
- Remaining Gaps
- Next Task Dependencies
- PR Number
