# Task 0117

Agent: Implementer

## Current Repo Context

- Task 0116 (PR #171, merged `c860053`) closed the `@saas/sdk`
  webhook-endpoint type re-export gap. The B5 endpoint-CRUD console + CLI arc
  is fully shipped; repo is green on every component lane.
- **Standing baseline failure on main:** `tests/db/src/migrations.test.ts`
  has a red test. The suite hard-codes a local `VALID_CONTEXTS: BoundedContext[]`
  array (lines 52–62) listing 9 contexts, but the canonical `BoundedContext`
  union in `packages/db/src/types.ts` includes a 10th —
  `"notifications"` — and a real migration declares it
  (`packages/db/src/manifest.ts:116`, `context: "notifications"`). The
  `"each migration declares a valid bounded context"` assertion at
  `migrations.test.ts:66` therefore fails (`@saas/db-tests` → `jest` →
  1 failed / 515 passed). This was flagged as a known baseline failure across
  Tasks 0113/0115 reports and is recommended-next-focus #2 in `current.md`.

## Objective

Stabilize main by fixing the single red test: bring the test's local
`VALID_CONTEXTS` array into sync with the authoritative `BoundedContext`
type union, so the migration manifest's existing `notifications` migration
validates. Repo-failing → stabilize first (orchestrator Task Selection Logic).

## PR Boundary

EXACTLY 2 files:

1. `tests/db/src/migrations.test.ts` (MODIFIED) — add `"notifications"` to the
   `VALID_CONTEXTS` array (lines 52–62), placed adjacent to `"metering"` to
   mirror the ordering of the `BoundedContext` union in `types.ts`. No other
   change to this file: no new test cases, no assertion edits, no harness
   changes.
2. `ai/reports/task-0117-implementer.md` (NEW) — real PR# (no TBD).

Non-goals:
- Do NOT edit `packages/db/src/types.ts` (already correct — it is the source
  of truth and already includes `"notifications"`).
- Do NOT edit `packages/db/src/manifest.ts` or any migration file.
- Do NOT add `"notifications"` anywhere else, refactor the test to derive the
  array from the type, or "improve" the test beyond the one-line sync.
- No new behaviour, no new migrations, no schema change.

## Read First

- `tests/db/src/migrations.test.ts` (lines 51–69 — the failing block)
- `packages/db/src/types.ts` (the `BoundedContext` union — source of truth)
- `packages/db/src/manifest.ts` (line 116 — the `notifications` migration)
- `ai/context/current.md` (recommended-next-focus #2)

## Required Outcomes

- [ ] `"notifications"` added to `VALID_CONTEXTS` in `migrations.test.ts`,
      ordered to match the `BoundedContext` union.
- [ ] `pnpm --filter @saas/db-tests test` → 516/516 pass (was 515/516).
- [ ] No other file in `packages/db/**` or `tests/db/**` modified.
- [ ] Implementer report committed on the PR branch with the real PR number.

## Non-Goals

- No deriving the array programmatically from the type (out of scope; a
  one-line literal sync is the intended fix).
- No touching any other test file or db source.
- No lockfile / package.json / component.yaml change.

## Constraints

1. Test-only change. Zero production-source edits.
2. The added literal must be exactly `"notifications"` (matches the union
   member and the migration's declared context byte-for-byte).
3. No new `eslint-disable` / `ts-ignore` / `as any`.
4. Revert any `kiox.lock` mutation produced by local Orun runs; do not commit
   `plan.json`.
5. Real PR number required in the report (TBD = BLOCKED).

## Integration Notes

- Single-component turbo PR: Orun changed-plan selects ONLY `db-tests`
  (subscribes `dev` · `quick-check` only) → 1 component × 1 env = 1 job + plan.
  Do NOT expect stage/prod lanes for this component.
- BEHIND-main rebase (if main advances before merge) remains the verifier's
  responsibility.

## Acceptance Criteria

✅ `tests/db/src/migrations.test.ts` diff is a single added array element.
✅ `pnpm --filter @saas/db-tests test` → all suites pass (516/516).
✅ `pnpm --filter @saas/db typecheck` → 0 errors (unchanged type surface).
✅ `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml` passes.
✅ `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --base origin/main --intent intent.yaml --output plan.json` selects `db-tests · dev` (1 job + plan).
✅ `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions` → 1 selected, green.
✅ PR opened against `main`; PR-CI green (plan + db-tests · dev · quick-check).
✅ Working tree clean (kiox.lock reverted, no plan.json committed).

## Verification

Verifier confirms: 2-file boundary exactly, the added literal matches the
union member, the full `@saas/db-tests` suite is green (516/516, the previously
red `migrations.test.ts:66` now passes), typecheck clean, Orun gates select
only `db-tests · dev`, PR-CI green, no forbidden-zone touches.

## PR Creation Requirement

You must create a branch (`impl/task-0117-migrations-test-notifications-context`),
commit, push, and open a GitHub PR before reporting complete. `PR Number: TBD`
is not acceptable. After `gh pr create`, update the implementer report with the
real PR number, then commit + push that update.

## When Done Report

Write `ai/reports/task-0117-implementer.md` with: Summary, Files Changed,
Checks Run (exact commands + results, including before/after test counts),
Assumptions, Spec Proposals, Remaining Gaps, Next Task Dependencies, PR Number.
