# Task 0101

Agent: Implementer

## Current Repo Context

- Track B4 first half CLOSED 2026-05-31. `@saas/sdk` ships the full
  11-client public-API surface (PR #153 squash `93ebe0e`). SDK is a
  first-class Orun component (`packages/sdk/component.yaml`,
  `domain: starter-sdk`).
- Track B4 second-half FOUNDATION CLOSED 2026-05-31. Task 0100 / PR #154
  squash `5cf36d9`, post-merge main-CI `26697417691` = 4/4 SUCCESS.
  `packages/cli` ships the workspace scaffold + command framework +
  auth (token-paste validated by `client.organizations.list()`) +
  KeychainTokenStore (lazy `keytar`) / FileTokenStore (POSIX 0600) +
  org-context store (`~/.config/sourceplane/config.json`) +
  `--output=human|json` formatter + pilot read-only commands
  (`login`, `logout`, `whoami`, `org list`, `org use`, `org members`,
  `project list`). Hand-rolled router under `src/router.ts`.
  `packages/cli/component.yaml` mirrors the SDK component shape
  (turbo-package, quick-check on `{dev,stage,prod}`,
  `domain: starter-cli`, `surface: cli`). 51 it() across 6 test files.
  Hazard scan packages/cli/** = 0 hits.
- Track B (B1, B2, B3 first+second halves) is live in production.
  Edge is hardened: Idempotency-Key contract (Task 0094), idempotency
  replay store (Task 0095/0095.1), per-org + per-identity rate limiting
  (Task 0097). Notifications V1 stays internal-only.
- Parked candidates (do NOT reach into them): `0085b` (cloudflare-domain
  v4→v5), `notifications-provider-swap` (provider choice pending),
  `notifications-worker-dev-reframe` (dev deploy lane design pending).
- Task 0096f (`tests/api-edge` class-B drain, 45→0 no-explicit-any)
  remains scoped at `ai/tasks/task-0096f.md`, parallel-safe with this
  task — zero file overlap (0096f owns `tests/api-edge/**`, this task
  owns `packages/cli/**` only).

## Objective

Fan out the remaining `packages/cli` commands per
`specs/components/13-cli-and-sdk.md` to close Track B4.
Ship all the spec-13 write commands plus the cross-resource read
commands, all wired through `@saas/sdk` end-to-end. This is the
mirror of how Task 0099 fanned out resource clients on top of the
Task 0098 SDK foundation.

## PR Boundary

In scope (one PR):
1. Write commands (each forwards a caller-supplied `--idempotency-key`
   verbatim, never auto-generates):
   - `org invite <email> [--role=ROLE] [--idempotency-key=KEY]`
     → `client.invitations.create({ email, role, idempotencyKey? })`
       on the active org (or a `--org=ORG_ID` override).
   - `project create <name> [--idempotency-key=KEY]`
     → `client.projects.create({ name, idempotencyKey? })`.
   - `env create <project-id> <name> [--idempotency-key=KEY]`
     → `client.environments.create({ projectId, name, idempotencyKey? })`.
     (Use whatever resource-client surface `@saas/sdk` already exposes
     for environment create — call it through the published path; do
     NOT add a new SDK client. If the SDK exposes envs as a sub-resource
     on projects, route through that.)
   - `api-key create <name> [--scope=SCOPE] [--idempotency-key=KEY]`
     → `client.apiKeys.create({ name, scope?, idempotencyKey? })`.
   - `webhook create <url> [--event=EVENT ...] [--idempotency-key=KEY]`
     → `client.webhooks.create({ url, events, idempotencyKey? })`.
2. Cross-resource read commands (no idempotency key, no writes):
   - `usage summary [--from=ISO] [--to=ISO]`
     → `client.metering.summarize(...)` (or whichever metering method
     the SDK exposes for org-level usage rollup).
   - `billing summary` → `client.billing.summary(...)` (or current
     period read).
   - `audit list [--limit=N] [--cursor=...] [--category=CAT]`
     → `client.events.listAuditEntries({ by: 'org', orgId, ... })`.
     Stream all pages via `--all` flag; default to first page.
3. Tests under `packages/cli/src/__tests__/`:
   - Extend `commands.test.ts` (or split into `commands.write.test.ts`
     and `commands.crossread.test.ts` — implementer's choice) so the
     full command suite has ≥ 30 new it() (combined with Task 0100's
     51 → ≥ 81 total in packages/cli).
   - Cover for each new command: happy path (SDK called with right
     args), `--idempotency-key` passthrough on writes (Stripe parity:
     CLI passes the key, never generates one), JSON output envelope,
     non-zero exit on `SourceplaneError` subclass with request ID,
     `--org=` override flag where applicable, and a `MissingAuthError`
     path for at least one new command.
4. README touch-up under `packages/cli/README.md` documenting the new
   commands. Stays terse — table of commands + one example per write
   command.

Out of scope (defer):
- Optional spec-13 commands (`component list`, `resource create`,
  `resource get`, `deployment get`) — defer behind a new task
  (`Task 0102`) only if the user asks. Spec 13 marks them optional;
  not blocking B4 closure.
- Console U10 refactor to consume the SDK (separate B4 follow-up
  candidate; out of CLI scope).
- Publishing config (npm tarball, `oclif` manifest, brew tap), shell
  completions, and `--profile` multi-account UX — explicit post-B4
  follow-ups.
- Device-flow swap in `auth/login.ts` — gated on `apps/api-edge`
  exposing the endpoint. Token-paste fallback stays.
- Any change to `@saas/sdk`, `packages/contracts/**`, `apps/**`,
  `infra/**`, or `tests/api-edge/**`.

## Read First

- `specs/components/13-cli-and-sdk.md` (CLI command list + output rules)
- `specs/contracts/api-guidelines.md` (Idempotency-Key + error envelope)
- `packages/cli/src/cli-runner.ts`, `router.ts`, `commands/index.ts`,
  `output/index.ts`, `errors.ts` — the foundation Task 0100 shipped
- `packages/sdk/src/index.ts` and the resource-client files for the
  SDK methods you'll be calling (organizations, invitations, projects,
  environments, apiKeys, webhooks, metering, billing, events)
- `packages/cli/src/__tests__/commands.test.ts` and
  `packages/cli/src/__tests__/helpers.ts` — DI seam + fake SDK
  pattern to mirror

## Required Outcomes

- All listed write + cross-read commands available, dispatched through
  the SDK only, with stable JSON output envelopes and request-ID-aware
  error reporting.
- `--idempotency-key` is forwarded verbatim on every write command.
  No transparent CLI-side generation. Verified by tests.
- `pnpm --filter @saas/cli` typecheck/lint/test/build all exit 0.
- Test count ≥ 81 it() repo-wide in `packages/cli/src/__tests__/**`
  (Task 0100 baseline 51 + ≥ 30 new). Implementer report records the
  exact post-PR count.
- `pnpm -r typecheck` exit 0.
- `pnpm -r --no-bail lint` ≤ 45 residual warnings, all in
  `tests/api-edge` (Task 0096f territory). CLI must contribute 0.
- Hazard scan `packages/cli/`: 0 hits
  (`eslint-disable | @ts-ignore | @ts-expect-error | as unknown as | as any`).
- Orun: `validate` / `component` / `plan --changed` / `run --dry-run`
  exit 0; CI runs `cli · {dev,stage,prod} · Verify` lanes green on
  the PR.

## Non-Goals

- No new SDK clients or methods. If a needed method is missing on the
  SDK, write a proposal under `ai/proposals/task-0101-spec-update.md`
  and pick an alternative (or scope a sub-task). Do not edit
  `packages/sdk/**`.
- No changes to `apps/**`, `packages/contracts/**`, `packages/db/**`,
  `infra/**`, or `tests/api-edge/**`.
- No publishing config, shell completions, multi-account profile UX,
  or device-flow swap.
- No CLI-side timestamps, request-IDs, or idempotency-key generation
  injected into SDK calls or output envelopes.

## Constraints

- Same hazard ban as Task 0100 / 0099 / 0098 / 0097 / 0096b–f: zero new
  `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `as unknown as`,
  `as any` under `packages/cli/**`.
- Public-API only — no `apps/**`, no `packages/db/**`, no Worker imports.
- `keytar` stays in `optionalDependencies`; do NOT introduce another
  optional native dep.
- JSON output stays deterministic; no CLI-injected timestamps or
  request-IDs.
- Stripe parity preserved: caller-owned `idempotencyKey`. CLI passes
  what the user gave; never auto-generates.
- POSIX 0600 credentials file invariant unchanged.
- `--output=human|json` honored on every new command.
- Use the existing DI seam (cli-runner factory args + helpers.ts fake
  SDK) — do NOT widen the runner contract beyond what's needed for the
  new commands.

## Integration Notes

- Active-org resolution: every write command except `org invite --org=`
  override falls back to the persisted `activeOrgId` from
  `~/.config/sourceplane/config.json`. If unset, exit non-zero with
  `MissingAuthError`-equivalent message ("no active org; run
  `sourceplane org use <id>`").
- `--idempotency-key` is a top-level flag at the command level; parse
  it via the existing `parseArgv` shape. If the SDK method does not
  accept an idempotency key (it should — verify per resource), surface
  a clear `UsageError`.
- `audit list` pagination: surface SDK cursor in the JSON envelope as
  `next_cursor`. `--all` mode loops until cursor is undefined and emits
  one JSON document per page (newline-delimited) when `--output=json`,
  or a flat human table when `--output=human`. Document the choice.
- `usage summary` / `billing summary`: pick the SDK method that
  matches; record the picked method + signature in the implementer
  report. If the SDK exposes only a list-style metering call, treat
  `usage summary` as a thin convenience wrapper that aggregates the
  current period.
- Errors: every command must surface request IDs from
  `SourceplaneError` subclasses through `formatCliError` and exit
  non-zero. Tests must assert.

## Acceptance Criteria

1. PR #ZZZ opened on branch `impl/task-0101-cli-command-fanout`,
   diff scoped to `packages/cli/**` (+ `pnpm-lock.yaml` only if a new
   devDep is needed — prefer none) + `ai/reports/task-0101-implementer.md`.
2. All write commands + cross-resource read commands listed above
   ship and pass tests.
3. `pnpm --filter @saas/cli typecheck/lint/test/build` exit 0.
4. `pnpm -r typecheck` exit 0.
5. `pnpm -r --no-bail lint` exit 0 with ≤ 45 residual warnings (all
   `tests/api-edge`). CLI contributes 0.
6. Hazard scan packages/cli/** = 0 hits.
7. it() count under packages/cli/src/__tests__/** ≥ 81 (record exact
   number in report).
8. `kiox -- orun validate / plan --changed / run --plan ... --dry-run`
   exit 0.
9. PR-CI green on `plan` + `cli · {dev,stage,prod} · Verify`.
10. Implementer report at `ai/reports/task-0101-implementer.md` with
    real PR Number (no `TBD`, no `BLOCKED` unless genuinely blocked).

## Verification

After local checks pass and PR is opened, the verifier will run a
sealed prompt that:
1. Confirms PR sanity (state OPEN, branch, base, mergeable).
2. Hazard + boundary scan on the diff.
3. Local gates (cli filter typecheck/lint/test/build + repo-wide
   typecheck + repo-wide lint warning count = ≤ 45 all in
   tests/api-edge).
4. Orun validate/plan/run dry-run.
5. PR-CI log inspection — confirm `cli · {dev,stage,prod} · Verify`
   actually executed Verify steps, not just SUCCESS-no-op.
6. Spec compliance against `specs/components/13-cli-and-sdk.md`
   (every required CLI command in spec is present; idempotency-key
   passthrough on writes; JSON envelope shape; request-ID surfacing).
7. Idempotency parity — confirm via tests + grep that the CLI never
   calls `crypto.randomUUID()` / `Math.random()` / equivalent to
   construct an idempotency key. The SDK is also forbidden from
   generating one (Stripe parity). Tests must assert the verbatim
   passthrough.

If PASS: squash-merge, sync local main, confirm post-merge main-CI
SUCCESS, write verifier report, close Track B4.
If FAIL: leave PR open with blockers.

## PR Creation Requirement

Create branch `impl/task-0101-cli-command-fanout`. Push the branch
and open the PR before reporting complete. If the PR cannot be
created, the implementer report's `PR Number:` line must be `BLOCKED`
with the failing command/error. `TBD` is not acceptable.

## When Done Report

Write `ai/reports/task-0101-implementer.md`:

- Summary (3-5 bullets, including the it() count, framework choice
  rationale only if it changed, and the SDK methods picked for
  `usage summary` / `billing summary`)
- Files Changed (grouped: commands, tests, README, lock if any)
- Checks Run (typecheck/lint/test/build/hazard/orun lanes)
- Assumptions (e.g., SDK method picks, audit `--all` pagination shape)
- Spec Proposals (any gaps surfaced in the SDK or spec 13)
- Remaining Gaps (Task 0102 candidates: optional spec-13 commands;
  console U10; publishing; shell completions; `--profile`)
- Next Task Dependencies
- PR Number (real number; no `TBD`)
