# Task ID

Task 0001.1

# Agent

Implementer

# Current Repo Context

Task 0001 is verified and merged into `main` at `4d38ded`. Human intervention is now on the open PR #5 branch `codex/human-01`.

The human changes are intentional and must be preserved:

- `stack-tectonic/` is committed as the repo-owned local Stack Tectonic catalog.
- The local stack was fetched with `/Users/irinelinson/.local/bin/kiox -- orun fetch ghcr.io/sourceplane/stack-tectonic:0.12.0 --overwrite`.
- `intent.yaml` uses `kind: dir` and `path: stack-tectonic/`.
- `kiox.yaml` uses the provider-style Orun pin and `kiox.lock` is committed.
- `.github/workflows/ci.yml` now starts in GitHub Actions; the earlier org-policy diagnosis is stale.

Current CI failure:

- PR #5 run `25657854944` fails in `orun plan --changed --intent intent.yaml --output plan.json`.
- GitHub log: `contracts-tests` fails schema validation for `turbo-package` because `lintCommand` and `testCommand` are unsupported inputs.
- Local plan also fails before matrix creation because `web-console` fails `cloudflare-pages-turbo` validation: missing `outputDir`, `projectName`, and `productionBranch`.

# Objective

Fix the PR #5 CI failure by aligning all discovered repo `component.yaml` descriptors with the committed local Stack Tectonic composition input schemas.

# PR Boundary

Use the existing PR #5 branch `codex/human-01`. Do not create a new PR unless the current branch is no longer available.

This task is only a CI/composition-input stabilization task. It must not implement domain behavior, live infrastructure provisioning, or product UI.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/repo.md`
- `specs/components/00-foundation-and-tooling.md`
- `README.md`
- `intent.yaml`
- `kiox.yaml`
- `stack-tectonic/compositions/cloudflare-worker-turbo/compositions.yaml`
- `stack-tectonic/compositions/cloudflare-pages-turbo/compositions.yaml`
- `stack-tectonic/compositions/turbo-package/compositions.yaml`
- All discovered repo component descriptors under `apps/`, `packages/`, `tests/`, and `infra/`.

# Required Outcomes

- Keep `intent.yaml` pointed at the local `stack-tectonic/` directory. Do not revert to the prior OCI composition source.
- Inspect every discovered repo `component.yaml` against the relevant local schema in `stack-tectonic/compositions/**/compositions.yaml`.
- Update `apps/web-console/component.yaml` to use the local `cloudflare-pages-turbo` input contract, including `outputDir`, `projectName`, `nodeVersion`, `pnpmVersion`, and `productionBranch`, and removing unsupported stale inputs.
- Update `apps/api-edge/component.yaml` to use the local `cloudflare-worker-turbo` input contract, including `productionBranch` and no unsupported inputs.
- Update `packages/*/component.yaml`, `tests/contracts/component.yaml`, and `infra/terraform/*/component.yaml` to use only supported `turbo-package` inputs. Preserve lint/test intent through supported inputs such as `preBuildCommand`, `buildCommand`, or `typecheckCommand` where appropriate.
- Regenerate any Orun generated files only if the relevant command changes them, and commit generated changes only when they are deterministic and necessary.
- Leave `.github/workflows/ci.yml` Orun-only.
- Write the implementer report at `ai/reports/task-0001.1-implementer.md`.

# Non-Goals

- No changes to domain contracts or SaaS product behavior.
- No live Cloudflare, Supabase, Hyperdrive, R2, Wrangler deploy, or Terraform resource creation.
- No broad Stack Tectonic redesign beyond what is required for the current local schema/input mismatch.
- No product-specific `specs-v2/**` work.
- No reverting the human local-stack change.

# Constraints

- Trust the committed local `stack-tectonic/` schemas over stale descriptor assumptions.
- If preserving a lint/test gate requires a composition behavior that cannot be expressed with the current schema, prefer the smallest descriptor-compatible mapping and record the remaining gap in the report. Do not silently add unsupported inputs.
- Keep the change small and focused on getting Orun plan/run and PR #5 CI healthy.
- Do not commit secrets, generated cache output, `.workspace/`, or `plan.json`.

# Integration Notes

- Current local `cloudflare-pages-turbo` requires `outputDir`, not `outputDirectory`.
- Current local `turbo-package` accepts `nodeVersion`, `pnpmVersion`, `preBuildCommand`, `buildCommand`, and `typecheckCommand`; it rejects `lintCommand` and `testCommand`.
- Current local `cloudflare-worker-turbo` requires `productionBranch` and rejects `lintCommand`.
- A reasonable production branch value for deployable scaffold components is `main`.
- Use stable Cloudflare project names for Pages inputs, for example `sourceplane-web-console`, unless repo context clearly establishes another name.

# Acceptance Criteria

- `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json` passes and produces a parseable plan.
- `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions` passes or records a true no-op result if no jobs are planned.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` still pass locally unless an unrelated external blocker is documented with exact output.
- PR #5 CI is rerun and the plan job no longer fails on component input schema validation. If a later non-input failure appears after this fix, document it clearly in the report and update `ai/context/open-risks.md`.
- No live Cloudflare/Supabase/Terraform resources are created or mutated.

# Verification

The verifier should inspect PR #5 diff, `ai/reports/task-0001.1-implementer.md`, local Orun validation/plan/dry-run output, and GitHub Actions logs. Verification must confirm that the branch preserves the local `stack-tectonic/` model and that the prior component input schema errors are gone.

# When Done Report

Write `ai/reports/task-0001.1-implementer.md` with:

- Summary
- Files Changed
- Checks Run
- Assumptions
- Spec Proposals
- Remaining Gaps
- Next Task Dependencies
- PR Number
