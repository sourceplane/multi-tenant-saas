# Task ID

Task 0006.1

# Agent

Implementer

# Current Repo Context

Task 0006 merged as PR #30 (`chore: update supabase-infra`) into `main` at
`fc795e4d974ae57d3e262084c393c04e18076f90`. PR CI passed on run
`26185038583`, but the post-merge `main` workflow run `26185145757` failed in
the live `apply` path for both `supabase.stage.terraform` and
`supabase.prod.terraform`.

Observed failure causes from the `supabase.stage.terraform` job:

- Supabase Management API rejected `instance_size` for the `sourceplane`
  free-plan organization:

```text
Unable to create project, got status 402:
{"message":"Instance size cannot be specified for free plan organizations."}
```

- AWS Secrets Manager creation was denied for the repo-scoped AWS plan role:

```text
AccessDeniedException:
User arn:aws:sts::306024784101:assumed-role/stage-github-sourceplane-multi-tenant-saas-plan/GitHubActions
is not authorized to perform: secretsmanager:CreateSecret
on resource: sourceplane/multi-tenant-saas/supabase/stage
because no identity-based policy allows the secretsmanager:CreateSecret action
```

The repo model still requires Orun-planned Terraform execution, S3 backend
state, `stage` and `prod` only, and AWS Secrets Manager storage under
`sourceplane/multi-tenant-saas/supabase/<env>`.

# Objective

Land the smallest safe remediation PR in `multi-tenant-saas` that makes the
Supabase Terraform component compatible with the current free-plan organization
and records the exact `aws-admin` IAM delta still required for successful live
apply.

# PR Boundary

One PR in `multi-tenant-saas` only.

This remediation may update the Supabase Terraform component, its README, and
the implementer report for the retry. It must not include direct IAM resource
changes, new infrastructure domains, product behavior, or unrelated cleanup.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/tasks/task-0006.md`
- `specs/access-and-infra.md`
- `specs/orun-golden-path.md`
- `specs/repo.md`
- `infra/terraform/supabase/**`
- `.github/workflows/ci.yml`
- GitHub Actions run `26185145757`
- Supabase Terraform provider docs for the pinned provider version

# Required Outcomes

- Update `infra/terraform/supabase/terraform/main.tf` so the current
  `sourceplane` organization path does not send `instance_size` during project
  creation.
- Keep the existing Task 0006 scope intact:
  - organization `dwazxcrywsdbxpuouifa`
  - region `ap-southeast-1` unless the provider/API now proves it invalid
  - project names `multi-tenant-saas-stage` and `multi-tenant-saas-prod`
  - `stage` and `prod` only
  - no `dev` provisioning
- Inspect partial-apply recovery state before retry recommendations:
  - Terraform state for the `stage` workspace
  - Supabase for a partial `multi-tenant-saas-stage` project
  - AWS Secrets Manager for `sourceplane/multi-tenant-saas/supabase/stage`
- Do not implement AWS IAM changes in this repo. Instead, record the minimum
  required `aws-admin` policy delta with exact secret ARN patterns and actions
  proven necessary by the failed apply path.
- Re-run targeted local Terraform and Orun checks for the changed surface.
- Open a PR for the repo-scoped fix.
- Write `ai/reports/task-0006.1-implementer.md`.

# Non-Goals

- No direct `aws-admin` code changes in this PR.
- No new Supabase environments, project names, organizations, or regions unless
  a provider/API error forces a stop.
- No Hyperdrive rollout, Cloudflare wiring, schema migrations, UI changes, SDK
  work, or workflow redesign.
- No manual dashboard setup as the shipped solution.

# Constraints

- Keep the change minimal and bounded to this repo.
- Do not create IAM users, roles, or policies from `multi-tenant-saas`.
- Do not log `SUPABASE_API_KEY`, `SUPABASE_ACCESS_TOKEN`, generated passwords,
  service keys, connection strings, or secret payloads.
- Do not silently switch region, org, or environment scope.
- If inspection shows a partial live resource that would require import,
  destroy, or replacement, stop and report the exact state instead of guessing.

# Integration Notes

- `specs/access-and-infra.md` already defines AWS Secrets Manager read/write for
  `<org>/<repo>/<component>/<env>` as an `aws-admin` responsibility; treat the
  current denial as an implementation gap, not a reason to weaken the boundary.
- The PR CI path is still plan-only for pull requests. A passing PR does not by
  itself prove post-merge apply is unblocked.
- The implementer report must clearly separate:
  - the repo-local fix landed in this PR
  - the remaining cross-repo `aws-admin` dependency

# Acceptance Criteria

- `infra/terraform/supabase/terraform/main.tf` no longer sends
  `instance_size` for the current free-plan `sourceplane` organization path.
- `terraform plan` for the Supabase component remains non-destructive unless an
  inspected partial resource requires an explicitly justified alternative.
- Local checks pass for the changed surface:
  - `terraform fmt -check`
  - `terraform init -backend=false`
  - `terraform validate -no-color`
  - `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  - `/Users/irinelinson/.local/bin/kiox -- orun plan --intent intent.yaml --view dag`
  - `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json`
  - `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions`
- The PR stays within one coherent remediation scope in `multi-tenant-saas`.
- The implementer report includes:
  - the failed-job root cause
  - partial-apply inspection evidence
  - exact `aws-admin` policy delta still required
  - confirmation that no `dev` Supabase project or secret was added

# Verification

The verifier should confirm the remediation PR is minimal, re-check the failed
merge run evidence, inspect the repo diff, and validate the local Terraform and
Orun commands. Because the IAM fix lives outside this repo boundary, PASS for
this PR depends on proving the repo-local bug is fixed and the remaining
`aws-admin` blocker is documented precisely enough for the next task.

# When Done Report

Write `ai/reports/task-0006.1-implementer.md` with:

- Summary
- Root Cause
- Files Changed
- Supabase Free-Plan Fix
- AWS Secrets Manager Permission Fix
- Partial Apply / State Review
- Orun Plan Impact
- Checks Run
- Sensitive Data Review
- Remaining Risks
- Next Task Dependencies
- PR Number
