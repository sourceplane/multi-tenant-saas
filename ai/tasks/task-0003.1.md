# Task ID

Task 0003.1

# Agent

Implementer

# Current Repo Context

Task 0003 is verified and merged in PR #25, but the repo is not stable yet.

- `main` is at `f645d41`
- PR #25 merged successfully
- PR CI run `26110433274` passed
- post-merge `main` CI run `26113133649` failed

The failure is in the legacy Terraform path:

- `tf-state-r2` selected the `apply` profile on `github-push-main`
- CI attempted to create the existing Cloudflare R2 bucket `sourceplane-tf-state`
- Cloudflare returned `409 Conflict` because the bucket already exists
- downstream `infra-terraform-core` jobs failed because they depend on `tf-state-r2`

Human direction for this intervention:

- remove both legacy Terraform components from the repo for now:
  - `tf-state-r2`
  - `infra-terraform-core`
- do not clean up, import, destroy, rename, or otherwise mutate any live
  Cloudflare, Supabase, R2, Hyperdrive, AWS, or Terraform state resources;
- later tasks will use S3 state from buckets created in `aws-admin`;
- Supabase must be created fresh in a new Terraform component later.

This repo must be green before the orchestrator advances to the cross-repo
`aws-admin` IAM task.

# Objective

Stabilize `multi-tenant-saas` `main` by deleting the two legacy Terraform
components from repo source so Orun no longer discovers or runs them.

# PR Boundary

One PR in `multi-tenant-saas` only.

This is a source cleanup task. It removes stale component directories and
updates directly related docs/context so Task 0004 can start from a green base
branch.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/reports/task-0003-implementer.md`
- `ai/reports/task-0003-verifier.md`
- `specs/orun-golden-path.md`
- `specs/access-and-infra.md`
- `specs/components/00-foundation-and-tooling.md`
- `intent.yaml`
- `.github/workflows/ci.yml`
- `stack-tectonic/compositions/terraform/**`
- `infra/terraform/tf-state-r2/**`
- `infra/terraform/core/**`
- GitHub Actions run `26113133649`

# Required Outcomes

- Delete the repo source for the legacy Terraform components:
  - `infra/terraform/tf-state-r2/**`
  - `infra/terraform/core/**`
- Remove any active component references, dependency edges, README entries, or
  context text that imply these two components should still be planned or
  applied.
- Keep historical references in old task prompts/reports only when rewriting
  them would destroy useful history.
- Ensure Orun discovery no longer includes `tf-state-r2`, `infra-tf-state-r2`,
  or `infra-terraform-core`.
- Ensure PR and push-to-main behavior no longer attempts to mutate the legacy R2
  bucket, Hyperdrive adoption scaffold, or dependent Terraform resources.
- Preserve Orun-only CI and the existing plan/run workflow shape.
- Update `ai/context/*` so the next orchestrator step is Task 0004 once this
  PR verifies and merges.
- Write an implementer report describing the deletion-only choice and why no
  live cleanup was performed.

# Non-Goals

- No `aws-admin` changes.
- No repo-scoped IAM role creation.
- No S3 backend migration.
- No Supabase project/database creation.
- No new replacement Terraform component in this task.
- No Cloudflare, Supabase, R2, Hyperdrive, AWS, or Terraform state cleanup.
- No `terraform apply`, `terraform destroy`, Terraform import, Wrangler
  mutation, Supabase mutation, or AWS mutation.
- No domain, API, UI, database migration, or product work.
- No `specs-v2/**` work.

# Constraints

- Keep the fix minimal and reversible.
- Use `git rm`/normal repo deletion for source files only.
- Do not introduce direct non-Orun CI logic.
- Do not commit `.orun/**`, `plan.json`, `.terraform/**`, state files, or
  secrets.
- Do not assume Task 0004 or Task 0005 has already landed.
- Do not attempt to reconcile live resources left behind by the deleted source.

# Integration Notes

- The old R2 bucket and Hyperdrive adoption scaffold are historical Task 0002
  artifacts. They are no longer the target infrastructure path.
- Task 0004 creates repo-scoped AWS IAM access in `aws-admin`.
- Task 0005 will consume that access and use S3 state from the
  `sourceplane-<env>` buckets created in `aws-admin`.
- Task 0006 will create fresh Supabase infrastructure in a new Terraform
  component with AWS Secrets Manager storage.
- The desired outcome is a green base branch with no active legacy Terraform
  components.

# Acceptance Criteria

- The following paths are removed from the repo:
  - `infra/terraform/tf-state-r2/`
  - `infra/terraform/core/`
- `rg -n "tf-state-r2|infra-tf-state-r2|infra-terraform-core" infra intent.yaml .github specs README.md ai/context ai/state.json` returns no references that make these components active or describe them as current infrastructure. Explicit deletion notes, blockers, or historical context are acceptable when clearly marked.
- `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  passes.
- `/Users/irinelinson/.local/bin/kiox -- orun component --intent intent.yaml`
  does not list `tf-state-r2`, `infra-tf-state-r2`, or `infra-terraform-core`.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --intent intent.yaml
  --output plan.json` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run
  --runner github-actions` passes or records a true no-op if no jobs are
  planned.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass unless an
  unrelated external blocker is documented with exact output.
- PR CI passes.
- After merge, `main` CI passes without attempting to recreate the existing R2
  bucket or run the deleted core component.

# Verification

The verifier must inspect the diff, rendered Orun component list, rendered plan
behavior, PR CI logs, and post-merge `main` CI logs. PASS requires confirming
the two legacy Terraform components are gone from active repo source and no live
resource cleanup was attempted.

# When Done Report

Write `ai/reports/task-0003.1-implementer.md` with:

- Summary
- Files Changed
- Deleted Components
- Orun Plan Impact
- Checks Run
- Live Resource Cleanup
- Assumptions
- Remaining Gaps
- Next Task Dependencies
- PR Number
