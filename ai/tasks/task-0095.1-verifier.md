# Task 0095.1 — Verifier (resumption of Task 0095 verification)

Agent: Verifier

## Current Repo Context

This is a **resumption** of the Task 0095 verification, not a fresh
verification. The verifier already ran on PR #143 head
`e47248e3ae4b81020204eeeaa200429b1e1a0a7f` on 2026-05-30 and produced
`ai/reports/task-0095-verifier.md` (FAIL — request changes).

What the prior run sealed (do NOT redo, do NOT relitigate):

- **Phase 1 — Repo/PR sanity:** PASSed. PR #143 OPEN, MERGEABLE/CLEAN,
  base `f7f5d19`. Three task doc files (`task-0094.md`,
  `task-0094-verifier.md`, `task-0095.md`) bleed-through noted as
  hygiene-only, not a blocker.
- **Phase 2 — Diff hazard + boundary scan:** PASSed. Zero
  `eslint-disable*` / `@ts-ignore` / `@ts-expect-error` / `as any` in
  the diff. Edits scoped to `apps/api-edge/`, `tests/api-edge/`,
  `infra/terraform/cloudflare-kv/` (new), and `ai/tasks/*.md` doc
  files. No edits to `cloudflare-domain`, `notifications-worker`,
  `web-console-next`, `tooling/eslint`, non-`@saas/contracts`
  `package.json`, `pnpm-lock.yaml`, `intent.yaml`, `kiox.lock`.
- **Phase 3 — Local install + gates:** PASSed. `pnpm install
  --frozen-lockfile` no-op. `pnpm --filter @saas/api-edge typecheck`
  green. `pnpm --filter @saas/api-edge lint` green. `pnpm --filter
  @saas/api-edge-tests test` = **282 passed, 0 failed, 11 suites.**
  `terraform fmt -check -recursive` and `terraform init -backend=false
  && terraform validate` on `infra/terraform/cloudflare-kv/terraform`
  both green.
- **Phase 4 — `replayOrExecute` code path:** PASSed in full. Envelope
  v1 + header allowlist + base64 fallback + 4xx cached / 5xx
  not-cached + 24h TTL + identity-agnostic key + KV-failure
  degrade-open. The 12 cases in
  `tests/api-edge/src/idempotency-replay.test.ts` cover all of these.
  **The following files are SEALED by Phase 4 and must not change in
  the 0095.1 fix-up commits:**
    - `apps/api-edge/src/idempotency.ts`
    - `apps/api-edge/src/env.ts`
    - the seven facades (`apps/api-edge/src/{auth,billing,config,
      metering,org,project,webhooks}-facade.ts`)
    - any test file under `tests/api-edge/src/idempotency-*.test.ts`

What the prior run BLOCKED on (Phase 5 — wrangler binding inspection,
hard FAIL): `apps/api-edge/wrangler.jsonc` shipped sentinel placeholder
KV IDs (`0000000000000000000000000000000a` for stage,
`0000000000000000000000000000000b` for prod) and **no substitution
mechanism existed anywhere in the deploy plumbing**
(`.github/workflows/ci.yml`, the `cloudflare-worker-turbo-verify-deploy`
composition, `apps/api-edge/component.yaml`'s `preDeployCommand`,
`apps/api-edge/scripts/verify-bindings.mjs` all checked). Consequence
if merged as-is: KV-bound replay ops throw → `logKvFailure` degrades
open → entire B3 replay store silently inert in prod while dashboards
say green. Full evidence in `ai/reports/task-0095-verifier.md` § Phase 5.

The Implementer was given `ai/tasks/task-0095.1.md` to land an additive
commit (or two) on `impl/task-0095-edge-idempotency-replay-store` that
mirrors the existing Hyperdrive precedent in the same `wrangler.jsonc`:
apply the `cloudflare-kv` Terraform slice once on stage + prod,
hardcode the real 32-char hex namespace IDs into wrangler, extend
`verify-bindings.mjs` with an `EXPECTED_KV` block, and commit the
`ai/context/open-risks.md` lines 83–91 closure that the original
implementer report claimed but the diff omitted. **PR #143 stays the
same PR. The verifier resumption picks up at Phase 5.**

## Objective

Resume Task 0095 verification from the Phase-5 blocker, validate the
0095.1 fix-up commits, and run Phases 6–10 in full per the existing
`ai/tasks/task-0095-verifier.md` if Phase 5 now PASSes. End state must
be one of:

- **PASS** — PR #143 merged at the verifier's hand, post-merge main-CI
  green, live KV replay evidence captured on stage and prod, KV
  namespaces verified directly via `wrangler kv namespace list`,
  open-risks closure committed, state files updated, Task 0095 marked
  completed.
- **FAIL (second pass)** — PR #143 stays OPEN with the new blocker
  documented; no merge. The orchestrator scopes Task 0095.2 (or
  rejects the approach and pivots).

## PR Boundary

PR #143 (`impl/task-0095-edge-idempotency-replay-store`) only. Do NOT
touch any other branch or PR. Do not relitigate Phases 1–4 in detail —
only delta-scan against the new commits added by 0095.1.

## Read First

1. `ai/reports/task-0095-verifier.md` — the prior FAIL report. Anchor
   for what's sealed and what's still open.
2. `ai/tasks/task-0095.1.md` — the implementer fix-up prompt. Defines
   IN/OUT scope for the new commits.
3. `ai/tasks/task-0095-verifier.md` — the original 10-phase verifier
   prompt. Phases 6–10 are reused verbatim if Phase 5 PASSes.
4. `ai/reports/task-0095-implementer.md` — including the `## Task
   0095.1 Follow-Up` section the implementer is required to append.
5. `apps/api-edge/wrangler.jsonc` (post-fix) — the Hyperdrive
   precedent at `env.{stage,prod}.hyperdrive[0]` is your reference for
   what "good" looks like for KV.
6. `apps/api-edge/scripts/verify-bindings.mjs` (post-fix) — the new
   `EXPECTED_KV` block must mirror the existing `EXPECTED_HYPERDRIVE`
   shape.
7. `infra/terraform/cloudflare-kv/terraform/{main.tf,outputs.tf}` — the
   real namespace IDs must come from this slice, applied stage + prod.
8. `ai/context/open-risks.md` — lines 83–91 must move under "Resolved
   Risks" with a one-line PR #143 reference.

## Required Outcomes (delta vs prior FAIL run)

### Phase 5-delta — Wrangler binding inspection (the previously blocking phase)

- [ ] `apps/api-edge/wrangler.jsonc` `env.stage.kv_namespaces[0].id`
      matches `^[0-9a-f]{32}$` AND is not
      `0000000000000000000000000000000a`. ID equals the `stage` output
      from the applied `cloudflare-kv` Terraform slice (cross-check
      with `wrangler kv namespace list` or `terraform output`).
- [ ] `apps/api-edge/wrangler.jsonc` `env.prod.kv_namespaces[0].id`
      matches `^[0-9a-f]{32}$` AND is not
      `0000000000000000000000000000000b`. ID equals the `prod` output
      from the applied slice.
- [ ] `apps/api-edge/wrangler.jsonc` `env.dev` either has no
      `kv_namespaces` block at all, or the dev block does not bind
      `IDEMPOTENCY_KV` to a stage/prod ID. Dev stays verify-only per
      Task 0095 boundary.
- [ ] `node apps/api-edge/scripts/verify-bindings.mjs` exits 0
      locally. Inspect the script: an `EXPECTED_KV` block exists,
      mirrors the `EXPECTED_HYPERDRIVE` pattern, and asserts:
      (a) binding name `IDEMPOTENCY_KV`, (b) ID matches
      `^[0-9a-f]{32}$`, (c) ID is not `…000a` / `…000b`, (d) ID
      matches what's in `wrangler.jsonc` for the requested env.
- [ ] **(If the implementer added the optional sentinel-string CI
      guard)** Re-running the script with the sentinel hex string
      injected into `wrangler.jsonc` makes it exit non-zero. (Do this
      in a scratch checkout — do not push the sabotage.)

### Phase 2-delta — Hazard + boundary delta scan on 0095.1 commits ONLY

Compare commits added since `e47248e` (the prior verifier head):

- [ ] `git diff e47248e..HEAD --name-only` shows ONLY paths in IN-scope
      from `ai/tasks/task-0095.1.md`:
      `apps/api-edge/wrangler.jsonc`,
      `apps/api-edge/scripts/verify-bindings.mjs`,
      `ai/context/open-risks.md`,
      `ai/reports/task-0095-implementer.md`. Plus, if the implementer
      took the manual-apply path, no new infra files; if main-CI
      apply, a small no-op trigger commit is acceptable.
- [ ] `git diff e47248e..HEAD` contains zero `+eslint-disable*`,
      `+@ts-ignore`, `+@ts-expect-error`, `+as any` source additions.
- [ ] No diff lines under `apps/api-edge/src/idempotency.ts`,
      `apps/api-edge/src/env.ts`, the seven facades, any
      `tests/api-edge/src/idempotency-*.test.ts`,
      `apps/notifications-worker/**`, `apps/web-console-next/**`,
      `tooling/eslint/index.js`, `pnpm-lock.yaml`, any non-
      `@saas/contracts` `package.json`,
      `infra/terraform/cloudflare-domain/**`, `kiox.lock`,
      `intent.yaml`. (These are the SEALED + OUT-of-scope paths.)
- [ ] Cloudflare provider pin still `~> 4.52` if any
      `infra/terraform/cloudflare-kv/terraform/*.tf` was touched.

### Phase 3-delta — Local gates re-run (cheap, do it anyway)

- [ ] `pnpm install --frozen-lockfile` no-op (pnpm-lock unchanged).
- [ ] `pnpm --filter @saas/api-edge typecheck` green.
- [ ] `pnpm --filter @saas/api-edge lint` green.
- [ ] `pnpm --filter @saas/api-edge-tests test` still **282 passed**
      (any change in count or any new failure is a blocker — the
      implementer should not have touched test or src files).
- [ ] If `cloudflare-kv` slice was touched: `terraform fmt -check
      -recursive` and `terraform validate` green.
- [ ] `kiox -- orun validate --intent intent.yaml`,
      `kiox -- orun plan --changed --intent intent.yaml --output
      plan.json`,
      `kiox -- orun run --plan plan.json --dry-run --runner
      github-actions` all green.

### Phase 6 — Merge decision

If Phase 5-delta + Phase 2-delta + Phase 3-delta all PASS, AND PR-CI
on PR #143 is green for all required checks (`plan`,
`api-edge · {dev,stage,prod} · Verify deploy`, `api-edge-tests · dev
· Verify`, `cloudflare-kv · {stage,prod} · Terraform`) at the latest
head:

- [ ] `gh pr merge 143 --squash --auto=false --delete-branch=false`
      (squash is the repo standard).
- [ ] `git checkout main && git pull --ff-only origin main`. Confirm
      `main` advanced.
- [ ] `git status --short` clean. No leftover verifier-created
      changes. Branch deleted at the remote.

### Phase 7 — Post-merge main-CI

Per `references/post-merge-deploy-profile-gap.md` from the orun-saas-
verifier skill: PR-time `verify` profile is necessary but not
sufficient for deploy-gated components (api-edge subscribes to
`verify` on PRs but `deploy` on `triggerRef: github-push-main`).

- [ ] `gh run watch <main-CI-run-id-after-merge>` to completion.
      Required jobs ALL = SUCCESS:
      - `api-edge · {dev,stage,prod} · Verify deploy`
      - `cloudflare-kv · {stage,prod} · Terraform` (apply)
      - `api-edge-tests · dev · Verify`
- [ ] Inspect the `Verify deploy` job logs for stage and prod (with
      `gh run view --log`): the new `verify-bindings.mjs` ran and the
      `EXPECTED_KV` block printed real (non-sentinel) IDs.
- [ ] `wrangler kv namespace list` (with the stage/prod-scoped CF
      tokens — these are available to the verifier session per
      orchestrator.md § Operational access) returns two namespaces
      whose IDs match what's in `wrangler.jsonc`. Record names + IDs
      in the verifier report (non-secret — same disclosure as
      Hyperdrive IDs already in tree).

### Phase 8 — Live evidence cases (a)–(g) on stage; (a)/(isolation)/(g) on prod

Reuse the test-case grid in `ai/tasks/task-0095-verifier.md` § Phase
8 verbatim. Do not skip — this is the entire point of B3.

Stage (`https://api-edge-stage.workers.dev`):

- [ ] **(a) Hit replay:** POST a mutation route twice with the same
      `Idempotency-Key` and identical body. Second response body
      equals the first byte-for-byte; `X-Idempotency-Replay: hit`
      header (or whatever the implementer chose — record it). Latency
      on second call markedly lower than first. Evidence:
      `curl -i` for both calls in the verifier report.
- [ ] **(b) Miss-then-store:** Fresh key, single POST → response
      stored; same key+body 30s later → replay.
- [ ] **(c) GET passthrough:** GET on any facade with an
      `Idempotency-Key` header → no replay envelope ever stored
      (verify with `wrangler kv get` showing no key under the route
      namespace).
- [ ] **(d) 4xx cached:** POST with a body that triggers a 422 from
      the facade → second call returns identical 422 envelope.
- [ ] **(e) 5xx not cached:** Force a 500 (e.g., kill the downstream
      worker briefly OR a known 500-emitting test path) → second call
      with the same key re-executes (does NOT replay the 500).
- [ ] **(f) Identity-agnostic key:** Two different bearer tokens from
      different actors hitting the same route with the same
      `Idempotency-Key` and same body → same envelope returned. (Per
      the implementer's choice — Stripe parity. Confirm against
      `apps/api-edge/src/idempotency.ts` Phase-4 sealed behavior.)
- [ ] **(g) Header allowlist:** Replay envelope does NOT carry
      `Authorization`, `Cookie`, or any actor-bearing header. Inspect
      the raw KV value with `wrangler kv get`.

Prod (`https://api-edge-prod.workers.dev`):

- [ ] **(a) Hit replay** smoke.
- [ ] **(isolation)** stage key DOES NOT replay on prod (different KV
      namespaces).
- [ ] **(g) Header allowlist** smoke.

Console smoke unchanged from Task 0094:

- [ ] `curl -sI https://stage.sourceplane.ai/` → 307 to `/orgs`.
- [ ] `curl -sI https://prod.sourceplane.ai/` → 307 to `/orgs`.

### Phase 9 — Open-risks confirmation + state-file updates

- [ ] `ai/context/open-risks.md` lines 83–91 are now under "Resolved
      Risks" with a one-line PR #143 reference and the
      `replayOrExecute` + real-KV-ID closure rationale. Confirm via
      `git diff origin/main..origin/main` (post-merge).
- [ ] `ai/state.json`:
      - `current_task` advances to the next planned candidate (Task
        0096 rate limiting per the established roadmap, OR
        class-B warning cleanup wave if 0096 is being held)
      - `completed` array gains `"0095"` and `"0095.1"`
      - `last_verified` timestamp updated
      - `task_agent` updated to the next task prompt path
      - `notes` revised with the Task 0095 + 0095.1 closure summary
        (mirroring the structure of the Task 0093 / 0094 closure
        notes)
- [ ] `ai/context/current.md` updated with: Task 0095 + 0095.1 closure
      summary; current task = next candidate; deferred set unchanged
      unless something legitimately unblocked.
- [ ] `ai/context/task-ledger.md` appended with `## Task 0095` and
      `## Task 0095.1` entries in the established format.
- [ ] Verifier report committed: `ai/reports/task-0095.1-verifier.md`
      (or appended to `ai/reports/task-0095-verifier.md` as a `## Task
      0095.1 Resumption` section — implementer's call, but a separate
      file is cleaner for cron/log audit).

### Phase 10 — Final sweep

- [ ] `git status --short` clean. `git log --oneline -3` shows the
      Task 0095 squash and the verifier-artifact commit on `main`.
- [ ] PR #143 closed with a merge commit reference. Branch
      `impl/task-0095-edge-idempotency-replay-store` deleted at the
      remote. Local `main` fast-forwarded.
- [ ] No post-merge alarms in the api-edge stage/prod logs (5 min
      observation window — short, but per the verifier skill enough
      to catch obvious regressions).

## Constraints

1. The Phase 4 PASS is sealed. Do NOT re-run the full code-path
   inspection on `replayOrExecute`, the envelope shape, the header
   allowlist, the 4xx/5xx caching policy, or any facade call site
   unless the 0095.1 commits actually touched those paths (in which
   case it's an immediate FAIL — they're explicitly OUT of scope per
   the implementer prompt).
2. Provider pin stays at `cloudflare ~> 4.52`. Do not accept a bump.
3. The deferred boundaries from `ai/state.json` `notes`
   (`infra/terraform/cloudflare-domain/**`, the cloudflare 4.52 pin,
   `apps/notifications-worker/**`, `apps/web-console-next/**`,
   `tooling/eslint/index.js`, `pnpm-lock.yaml`, any non-
   `@saas/contracts` `package.json`) all stay untouched.
4. KV namespace IDs are non-secret bindings (same disclosure posture
   as Hyperdrive IDs already in the tree). They MAY appear in
   `wrangler.jsonc` and the verifier report. They MUST NOT appear
   anywhere as Cloudflare API tokens / account IDs / signing secrets.
5. Verifier MUST inspect post-merge main-CI logs with `gh run view
   --log` before declaring PASS. PR-time green is necessary but not
   sufficient for deploy-gated components (per
   `references/post-merge-deploy-profile-gap.md`). Skipping this
   caused the Task 0082/0082.1 white-page regression.
6. `wrangler kv namespace list` MUST be run for direct provider
   verification — do not rely on Terraform output alone or on `gh run
   view --log`. The deferred-path failure mode is a phantom binding
   that succeeds at deploy time but throws on first KV op; only
   provider-side verification catches it.

## Acceptance Criteria

✅ Phase 5-delta PASS: real 32-char hex KV IDs in `wrangler.jsonc`
for stage AND prod, `verify-bindings.mjs` extended with
`EXPECTED_KV`, the script exits 0 locally.

✅ Phase 2-delta PASS: 0095.1 commits touch ONLY IN-scope paths,
zero new `+eslint-disable*` / `+@ts-ignore` / `+as any`, no edits to
sealed Phase-4 paths or any OUT-of-scope path.

✅ Phase 3-delta PASS: typecheck / lint / 282 tests / terraform
validate / orun validate-plan-dry-run all green.

✅ Phase 6 PASS: PR #143 squash-merged at the verifier's hand,
`main` fast-forwarded, branch deleted at the remote.

✅ Phase 7 PASS: post-merge main-CI run = all required jobs SUCCESS;
verify-bindings.mjs in the deploy logs printed real (non-sentinel)
KV IDs; `wrangler kv namespace list` shows real namespaces matching
`wrangler.jsonc`.

✅ Phase 8 PASS: cases (a)–(g) on stage AND (a)/(isolation)/(g) on
prod all behave as specified. Console 307 → `/orgs` smoke green on
stage + prod.

✅ Phase 9 PASS: open-risks lines 83–91 closed under "Resolved
Risks", state files (`ai/state.json`, `ai/context/current.md`,
`ai/context/task-ledger.md`) updated, verifier report committed.

✅ Phase 10 PASS: working tree clean, PR closed, branch deleted, no
post-merge alarms in 5-min observation window.

## Verification (what the verifier physically runs)

```bash
# Setup
git fetch origin
gh pr checkout 143
git diff e47248e..HEAD --name-only          # delta scan
git diff e47248e..HEAD | grep -E '^\+.*(eslint-disable|@ts-ignore|@ts-expect-error|as any)'  # MUST be empty

# Phase 5-delta
node apps/api-edge/scripts/verify-bindings.mjs              # exit 0
jq '.env.stage.kv_namespaces[0].id, .env.prod.kv_namespaces[0].id' apps/api-edge/wrangler.jsonc

# Phase 3-delta
pnpm install --frozen-lockfile
pnpm --filter @saas/api-edge typecheck
pnpm --filter @saas/api-edge lint
pnpm --filter @saas/api-edge-tests test                     # 282 passed
terraform -chdir=infra/terraform/cloudflare-kv/terraform fmt -check
terraform -chdir=infra/terraform/cloudflare-kv/terraform init -backend=false && \
  terraform -chdir=infra/terraform/cloudflare-kv/terraform validate
kiox -- orun validate --intent intent.yaml
kiox -- orun plan --changed --intent intent.yaml --output plan.json
kiox -- orun run --plan plan.json --dry-run --runner github-actions

# Phase 6 (only if all of the above are green AND PR-CI on the latest head is green)
gh pr merge 143 --squash --delete-branch
git checkout main && git pull --ff-only origin main

# Phase 7
gh run watch <run-id>
gh run view <run-id> --log | grep -E 'IDEMPOTENCY_KV|verify-bindings'
wrangler kv namespace list                                  # see real IDs

# Phase 8 (per case grid above; record curl evidence)
# Phase 9, 10: state file edits + git status clean + PR closed
```

## PR Creation Requirement

The Implementer has already created (and updated) PR #143. Your job
is to verify it. The verifier may commit Phase-9 state-file updates
directly to `main` after merge per the orchestrator.md verifier
protocol, OR open a small follow-up PR for the state-file updates if
that's the repo policy at this point. Use the same protocol as the
Task 0094 verifier did.

## When Done Report

`ai/reports/task-0095.1-verifier.md` (a separate file, cleaner for
cron/log audit than appending to the existing 0095 verifier report).
Required sections:

- **Result:** PASS or FAIL (single word).
- **Phase 5-delta evidence:** `wrangler.jsonc` IDs (real hex),
  `verify-bindings.mjs` delta diff, local script exit code.
- **Phase 2-delta scan:** `git diff e47248e..HEAD --name-only`
  output, hazard regex result.
- **Phase 3-delta gates:** typecheck / lint / test count / terraform
  / orun results.
- **Phase 6 merge:** squash commit SHA, `main` advancement.
- **Phase 7 post-merge CI:** run id, jobs status table, deploy-log
  excerpts proving real IDs were used, `wrangler kv namespace list`
  output.
- **Phase 8 live evidence:** case grid (a)–(g) stage +
  (a)/(isolation)/(g) prod with `curl -i` excerpts and KV `get`
  reads.
- **Phase 9 closure:** open-risks diff, state-file diffs.
- **Phase 10 final sweep:** `git status --short`, `git log
  --oneline -3`, post-merge alarm window observation.
- **Risk Notes:** anything residual.
- **Spec Proposals:** zero or links.
- **Recommended Next Move:** Task 0096 rate-limiting prompt, OR
  class-B warning cleanup wave, OR a re-deferral note if something
  unexpected surfaced.
