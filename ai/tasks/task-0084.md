# Task 0084 — Drop legacy Pages residuals from cloudflare-domain

**Agent**: Implementer
**Branch**: `impl/task-0084-drop-pages-residuals`
**Prior context**: Tasks 0082, 0082.1, 0082.2, 0082.2.1, 0082.2.2, 0083,
0083.1 cut the console over from Cloudflare Pages
(`apps/web-console` → `sourceplane-web-console-{dev,stage,prod}`) to
Workers + Static Assets (`apps/web-console-next` →
`sourceplane-web-console-next-{stage,prod}`) with apex hostnames
`stage.sourceplane.ai` / `prod.sourceplane.ai` attached via
`cloudflare_workers_domain.console`. Per the Task 0083 implementer
deviation, `pagesProjectPrefix` variable + `pages_project_name` output
were kept "read-only for one soak cycle" so the first post-cutover plan
diff stayed reviewable. The soak cycle is complete — Task 0083.1
verifier (2026-05-29T13:30Z) confirmed both apex hostnames live, CORS
healthy, rollback hatch healthy, `repo_health: green`. These residuals
are now dead code.

## Objective

1. Delete the dead `pagesProjectPrefix` variable, `local.pages_project_name`
   binding, and `output "pages_project_name"` from
   `infra/terraform/cloudflare-domain/terraform/main.tf`.
2. Delete the `pagesProjectPrefix` parameter line from
   `infra/terraform/cloudflare-domain/component.yaml`.
3. Drop the two "Retained read-only…" rows from
   `infra/terraform/cloudflare-domain/README.md`.
4. Imperatively delete the three legacy Pages projects
   `sourceplane-web-console-{dev,stage,prod}` via wrangler (Orun has no
   managed record of them — `cloudflare_pages_domain.console` was the
   only managed handle and it was destroyed in Task 0083). Capture the
   `wrangler pages project list` before/after as evidence in the
   implementer report.

## Scope boundary (PR must contain only these)

- `infra/terraform/cloudflare-domain/terraform/main.tf`
- `infra/terraform/cloudflare-domain/component.yaml`
- `infra/terraform/cloudflare-domain/README.md`
- `ai/reports/task-0084-implementer.md`

NO change to:
- `cloudflare_workers_domain.console` resource shape, count gate, or
  any other resource.
- Cloudflare provider pin (`~> 4.52` holds — provider v5 rename is
  deferred to Task 0085).
- `apps/web-console-next/**`, `apps/api-edge/**`, any worker, contract,
  db, policy, migration.
- `intent.yaml` (no env-var changes — `CONSOLE_CUSTOM_DOMAIN` wiring is
  correct as of Task 0083.1).
- Any new orun composition or job template.

The wrangler Pages-project deletion is **imperative** and happens
outside the PR. Record exact commands + before/after listing in the
implementer report so a future reader can audit it.

## Acceptance — pre-merge (orchestrator/CI)

- `orun validate` PASS.
- `orun plan --changed` PASS; plan diff for `cloudflare-domain ·
  {stage,prod}` shows ONLY the removal of the variable/output (no
  resource churn — no `+/-` on `cloudflare_workers_domain.console`).
- `orun run --plan <plan> --dry-run --runner github-actions` PASS, 2
  cloudflare-domain Terraform jobs selected.
- PR CI green.

## Acceptance — post-merge (verifier-owned soak)

- `cloudflare-domain · {stage,prod} · Terraform · apply` logs:
  `Apply complete! Resources: 0 added, 0 changed, 0 destroyed.` (clean
  no-op — confirms the dropped variable/output were truly dead).
- `curl -sfL https://stage.sourceplane.ai/` → 200 with `Sourceplane Console`.
- `curl -sfL https://prod.sourceplane.ai/` → 200 with `Sourceplane Console`.
- Rollback hatch
  `sourceplane-web-console-next-{stage,prod}.rahulvarghesepullely.workers.dev`
  → 200.
- `wrangler pages project list` does not list any of
  `sourceplane-web-console-{dev,stage,prod}`.

## Failure mode + rollback

If the Terraform plan shows any unintended diff (e.g. provider hiccup
re-reads `cloudflare_workers_domain.console`), abort and re-scope. If
post-merge apply is non-empty, revert the merge commit — the dropped
variable/output are pure dead code, so reverting is safe.

For the wrangler deletion: if any of the three Pages projects has
unexpected traffic in `wrangler pages deployment tail` over a brief
window before deletion, hold and surface to orchestrator instead of
deleting.

## Architect notes

- The variable/output are dead because no resource consumes
  `local.pages_project_name` after Task 0083 swapped
  `cloudflare_pages_domain.console` for `cloudflare_workers_domain.console`.
  Confirmed by `grep -n pages_project_name infra/terraform/cloudflare-domain/terraform/main.tf`
  showing only the local binding and the output that consumes it.
- The wrangler deletion is irreversible but recoverable — Pages
  projects can be recreated by `wrangler pages project create` if ever
  needed, and there is no DNS dependency now that Task 0083.1 routes
  apex hostnames through `cloudflare_workers_domain.console`.
- Provider pin stays `~> 4.52`. Bumping to v5 (with the corresponding
  `cloudflare_workers_domain` → `cloudflare_workers_custom_domain`
  rename) is the proposed Task 0085, kept separate to keep blast
  radius small.
