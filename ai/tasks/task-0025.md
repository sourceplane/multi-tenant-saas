# Task ID

Task 0025

# Agent

Implementer

# Current Repo Context

- Repo main is at `b31d228` (`docs: add task-0024 verifier report and update state`).
- Open PR list was empty when this prompt was written.
- Latest main CI run `26380191982` passed, and Task 0024 post-merge CI run
  `26380045214` passed.
- Tasks 0001-0024 are verified.
- Task 0024 established `TransactionalSqlExecutor` and wired
  `invite.revoked` event/audit emission atomically with invitation revocation.
- The next feature direction remains invitation lifecycle audit coverage
  (`invite.created` and `invite.accepted`) or destructive member-admin
  mutations, but a local quality gate must be repaired first.
- Orchestrator local checks on 2026-05-25:
  - `pnpm --filter @saas/membership-worker-tests test` passed (124 tests).
  - `pnpm --filter @saas/api-edge-tests test` passed (85 tests).
  - `pnpm --filter @saas/membership-worker typecheck` passed.
  - `pnpm --filter @saas/db-tests typecheck` passed.
  - `pnpm --filter @saas/db-tests test` failed before executing
    `tests/db/src/membership.test.ts` with TypeScript `TS2307`:
    `Cannot find module '@saas/db/membership' or its corresponding type declarations`.
- `tests/db/package.json` already has Jest `moduleNameMapper` entries for
  `@saas/db/membership` and `@saas/db/events`.
- `tests/db/tsconfig.json` currently maps `@saas/db`, `@saas/db/hyperdrive`,
  `@saas/db/identity`, `@saas/db/runner`, `@saas/db/manifest`, and
  `@saas/db/types`, but it does not map `@saas/db/membership` or
  `@saas/db/events`.
- `tests/membership-worker/tsconfig.json` already maps
  `@saas/db/membership`, which is a useful local reference.
- The working tree may contain unrelated untracked historical `ai/tasks/**` and
  `ai/reports/**` files plus ignored generated outputs. Do not stage, delete,
  or normalize unrelated files.

# Objective

Restore the local `@saas/db-tests` Jest/ts-jest module resolution path so the
db test suite can execute from this checkout without `TS2307` failures for
exported `@saas/db/*` subpaths.

This is a maintenance/quality-gate task only. Keep it narrowly scoped.

# PR Boundary

One PR should make the minimal durable test-configuration fix needed for
`pnpm --filter @saas/db-tests test` to pass locally and in CI.

Expected scope is likely `tests/db/tsconfig.json` and, only if code reality
requires it, the nearest db test Jest/TypeScript configuration.

This PR must not add invitation lifecycle event emission, member removal, member
role updates, organization settings, public audit APIs, runtime feature code,
database migrations, Terraform, Cloudflare, Supabase, AWS, UI, SDK, CLI, or
`specs-v2/**` work.

# Read First

- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/state.json`
- `specs/repo.md`
- `specs/orun-golden-path.md`

Then inspect current code before editing:

- `tests/db/package.json`
- `tests/db/tsconfig.json`
- `tests/db/src/membership.test.ts`
- `tests/db/src/events.test.ts`
- `tests/membership-worker/tsconfig.json`
- `packages/db/package.json`
- `packages/db/src/membership/index.ts`
- `packages/db/src/events/index.ts`
- `tooling/tsconfig/base.json`
- Any relevant Jest/ts-jest config already used by adjacent test packages

# Required Outcomes

1. Reproduce or explain the db-tests module-resolution failure.
   - Start from the current repo state, create or reuse a task branch, and run:
     `pnpm --filter @saas/db-tests test`.
   - If the failure reproduces, keep the fix focused on the failing resolution
     path.
   - If the failure does not reproduce, still inspect the test config mismatch
     and make only a defensible explicit-config fix if it is clearly needed.

2. Fix exported db subpath resolution for db tests.
   - Ensure `@saas/db/membership` resolves to
     `packages/db/src/membership/index.ts` for the db test TypeScript/Jest path.
   - Ensure `@saas/db/events` resolves to `packages/db/src/events/index.ts`
     for the same path if the current config also needs that explicit mapping.
   - Prefer aligning `tests/db/tsconfig.json` with existing package-local
     patterns over changing package exports or runtime code.
   - Do not broaden path aliases more than needed.

3. Preserve runtime and CI behavior.
   - No app source behavior should change.
   - No package runtime exports should change unless you discover the package
     exports are the actual root cause and cannot be fixed in test config.
   - No Orun component behavior should change except normal changed-file
     planning for the touched test package.

4. Keep the worktree clean and scoped.
   - Do not stage ignored generated outputs such as `dist`, `node_modules`,
     `.orun`, Terraform working directories, TypeScript build info, or
     `plan.json`.
   - Do not stage unrelated existing `ai/tasks/**` or `ai/reports/**` files.
   - Avoid formatting churn in unrelated configs.

# Non-Goals

- No invitation create/accept event or audit wiring.
- No member removal endpoint.
- No member role update endpoint.
- No public audit read API.
- No db migration.
- No transaction seam changes unless directly required by the test-config
  failure.
- No app runtime code changes.
- No Terraform, Supabase project, AWS IAM, S3, Secrets Manager, or Cloudflare
  resource changes.
- No README refresh or broad docs cleanup.
- No `specs-v2/**` work.

# Constraints

- Trust code reality over stale docs.
- Keep this as a single small maintenance PR.
- Use existing repo patterns for TypeScript and Jest config.
- Do not mask real test failures by disabling diagnostics, skipping
  `membership.test.ts`, narrowing `testMatch`, or suppressing TypeScript errors.
- Do not remove assertions from db tests to make the suite pass.
- If the root cause turns out to be a generated artifact or local cache issue,
  document the finding in the report and make no unnecessary code changes.

# Acceptance Criteria

- `pnpm --filter @saas/db-tests test` passes locally.
- `pnpm --filter @saas/db-tests typecheck` passes locally.
- The fix is limited to test/module-resolution configuration unless a stronger
  root cause is proven.
- No runtime behavior, migrations, app Worker code, or live resource config
  changes are included.
- Orun validation and changed-plan dry-run pass.
- A GitHub PR is opened for exactly this task.

# Verification

Run at minimum:

```bash
pnpm --filter @saas/db typecheck
pnpm --filter @saas/db-tests typecheck
pnpm --filter @saas/db-tests test
pnpm --filter @saas/membership-worker-tests test
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
git diff --check
```

If a verification command fails for an environment reason, record the exact
command, error, and why the PR is still safe or blocked.

# Git And PR Requirements

- Create or reuse a branch named for this task, for example
  `codex/task-0025-db-tests-module-resolution`.
- Commit only task-scoped changes.
- Push the branch.
- Open a GitHub PR before reporting complete.
- The implementer report is not complete with only local changes.

# Report Expectations

Write `ai/reports/task-0025-implementer.md` with:

- Summary: 3-5 bullets.
- Files Changed: grouped by subsystem.
- Checks Run: exact commands and results.
- Assumptions: only durable assumptions.
- Spec Proposals: links only, if any.
- Remaining Gaps: actionable residual risk only.
- PR Number: one line with the real PR number.
