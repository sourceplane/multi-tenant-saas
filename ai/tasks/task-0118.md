# Task 0118

Agent: Implementer

## Current Repo Context

- Task 0117 verified + merged (PR #172, squash `7440179`). main is fully green;
  the last standing baseline test failure (`migrations.test.ts` VALID_CONTEXTS)
  is cleared.
- Task 0111 extracted a shared `packages/cli/src/commands/helpers.ts` with
  `resolveOrgId(ctx, allowOverride)` + `readIdempotencyKey(ctx)`, folding the
  inline duplication in `writes.ts` and `webhook-secrets-rotate.ts`. The Task
  0111 report flagged a **Remaining Gap**: `cross-reads.ts` still carries its
  own private `resolveOrgId(ctx)` (no-override variant) that was NOT folded into
  the shared helper at the time. This task closes that gap.

## Objective

Fold the private `resolveOrgId` in `packages/cli/src/commands/cross-reads.ts`
(lines 36–43) onto the shared `helpers.ts` no-override variant
(`resolveOrgId(ctx, /* allowOverride */ false)`), eliminating the last byte-
equivalent copy of org-id resolution in the CLI. Pure internal refactor — zero
behaviour change.

## PR Boundary

1. `packages/cli/src/commands/cross-reads.ts` — delete the local
   `resolveOrgId` function; import `resolveOrgId` from `./helpers.js`; drop the
   now-unused `MissingOrgContextError` from the `../errors.js` import (keep
   `UsageError`); update the 3 call sites (lines ~70, ~112, ~174) from
   `resolveOrgId(ctx)` to `resolveOrgId(ctx, /* allowOverride */ false)`.
2. `ai/reports/task-0118-implementer.md` — NEW, real PR#.

Explicit non-goals: see below.

## Read First

- `packages/cli/src/commands/helpers.ts` — the shared `resolveOrgId` signature
  and the no-override branch semantics (must be byte-equivalent to the deleted
  local copy)
- `packages/cli/src/commands/cross-reads.ts` — the 3 call sites + local def
- `packages/cli/src/commands/webhook-disable.ts` — canonical consumer of the
  `resolveOrgId(ctx, false)` shared form (mirror the inline-comment style)
- `ai/reports/task-0111-implementer.md` — original extraction + the deferred gap

## Required Outcomes

- [ ] Local `resolveOrgId` in cross-reads.ts deleted; shared one imported
- [ ] `MissingOrgContextError` import removed (now unused); `UsageError` kept
- [ ] All 3 call sites pass `false` as the override arg
- [ ] `@saas/cli` typecheck 0 errors
- [ ] `@saas/cli` lint 0 new warnings (no unused-import warning for the dropped symbol)
- [ ] `@saas/cli` test suite green, case count UNCHANGED (pure refactor)
- [ ] Orun validate/plan/run-dry-run green; changed-plan selects ONLY cli
- [ ] `kiox.lock` reverted; no `plan.json` committed

## Non-Goals

- No change to the shared `helpers.ts` (it is already correct)
- No extraction of `assertOutputModeValid` or any other helper
- No behaviour change to `usage summary` / `billing summary` / `audit list`
- No new test cases (existing `helpers.test.ts` already covers `resolveOrgId`
  both branches); no edits to any test file unless a typecheck/lint failure
  forces a minimal import fix
- No edits outside `packages/cli/src/commands/cross-reads.ts` (+ the report)

## Constraints

1. Byte-equivalent behaviour: the shared no-override branch
   (`helpers.ts:43-48`) is identical logic to the deleted local function —
   load contextStore, read `activeOrgId`, throw `MissingOrgContextError` when
   empty. Confirm equivalence before deleting.
2. No new `eslint-disable` / `@ts-ignore` / `as any`.
3. Single-component turbo-package PR: Orun changed-plan selects ONLY `cli`
   (3 envs × Verify = 3 jobs + plan). Do NOT expect deploy/stage/prod app lanes.
4. Real PR# (TBD = BLOCKED). BEHIND-main rebase is verifier responsibility.

## Acceptance Criteria

✅ Diff boundary EXACTLY 2 files (cross-reads.ts + report)
✅ `pnpm --filter @saas/cli typecheck` = 0
✅ `pnpm --filter @saas/cli lint` = 0 new warnings
✅ `pnpm --filter @saas/cli test` green, case count unchanged
✅ `kiox -- orun validate --intent intent.yaml` passes
✅ `kiox -- orun plan --changed --base origin/main --intent intent.yaml --output plan.json` = 1 component (cli) × 3 envs = 3 jobs
✅ `kiox -- orun run --plan plan.json --dry-run --runner github-actions` = 3 selected, green
✅ PR-CI green (plan + cli·{dev,stage,prod}·Verify)
✅ MergeStateStatus CLEAN

## Verification

Verifier confirms the 2-file boundary, byte-equivalence of the fold (no
behaviour drift in org-id resolution), unused-import cleanup, unchanged CLI test
count, Orun cli-only changed-plan, and PR-CI green via `gh run view --log`.

## PR Creation Requirement

You must create branch `impl/task-0118-cli-cross-reads-resolveorgid-fold`,
commit, push, and open a GitHub PR before reporting complete. PR Number: TBD is
not acceptable. After `gh pr create`, update this report's PR Number with the
real value, commit, and push.

## When Done Report

`/ai/reports/task-0118-implementer.md` with: Summary, Files Changed, Checks Run,
Assumptions, Spec Proposals, Remaining Gaps, Next Task Dependencies, PR Number.
