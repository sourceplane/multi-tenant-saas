# Task ID

Task 0007.1 Verifier

# Agent

Verifier

# Current Repo Context

Task 0007.1 implementation is complete and open as PR #34:

- URL: https://github.com/sourceplane/multi-tenant-saas/pull/34
- Branch: `codex/task-0007.1-db-migration-harness`
- Head commit: `cd03c2244addaa87ade94b7bc05da033bfa7fd33`
- Base: `main` at `29cd21c125423adfd7a17b1a1b12707d63f7982d`
- Current PR state: open
- Latest PR CI run: `26220171323`

Observed repo reality when this prompt was generated:

- `gh pr list --state open` returns PR #34 as the only open PR for this repo.
- Local checkout is on `codex/task-0007.1-db-migration-harness` with a clean
  worktree.
- PR #34 adds `packages/db/` and `tests/db/` as the first database migration
  harness and verifier path, updates `README.md`, and includes Task 0007/0007.1
  orchestration artifacts.
- PR #34 checks currently pass in GitHub Actions run `26220171323`: `plan`,
  `db · dev · Verify`, `db · stage · Verify`, `db · prod · Verify`, and
  `db-tests · dev · Verify`.
- The branch does not declare a `dependsOn` edge from `db` to `db-tests`; the
  implementer report argues this is due to an environment-subscription mismatch.
- The PR diff includes `agents/orchestrator.md` plus `ai/context/**`,
  `ai/state.json`, and task/report history files in addition to the DB harness.
  Verification must decide whether those files are acceptable task-history
  context or task-scope overreach.

The next safe move is to verify whether PR #34 is a bounded, production-grade
Task 0007 completion PR. If it passes, merge it and leave local `main` synced
and clean. If it fails, leave the PR open with clear blockers.

# Objective

Independently verify PR #34 against Task 0007 and Task 0007.1. Confirm the DB
migration harness is correct, bounded to one PR-sized task, and ready to merge.
If it passes, merge it, sync local `main`, and leave the repo clean. If it
fails, leave the PR open with explicit blockers.

# PR Boundary

This verifier task covers PR #34 only.

Allowed verifier changes are limited to a concise verifier report and any small,
strictly Task 0007-scoped verification fix needed to complete the task. Do not
start the next implementation task, do not add live migration apply behavior,
and do not preserve unrelated files in the PR just because CI is green.

# Read First

- `agents/orchestrator.md`
- `ai/tasks/task-0007.md`
- `ai/tasks/task-0007.1.md`
- `ai/reports/task-0007-implementer.md`
- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/constitution.md`
- `specs/repo.md`
- `specs/domain-model.md`
- `specs/contracts/tenancy-and-rbac.md`
- `specs/components/00-foundation-and-tooling.md`
- `specs/orun-golden-path.md`
- `README.md`
- `packages/db/**`
- `tests/db/**`
- PR #34 diff, commits, review state, and CI logs
- GitHub Actions run `26220171323`

# Required Outcomes

- Confirm PR #34 maps to exactly one PR-sized task: the first DB migration
  harness and verifier conventions, without live schema apply or runtime DB
  adapters.
- Inspect the actual PR diff, branch content, implementer report, and CI logs,
  not only the check summaries.
- Verify `packages/db` public exports remain Worker-safe and do not pull in
  Node-only modules from default entry points.
- Verify the manifest and tests actually enforce deterministic ordering,
  duplicate-ID rejection, file existence, checksum drift detection, owning
  bounded context, and the `org_id + project_id` invariant for project-scoped
  migrations.
- Decide whether the missing `dependsOn` edge from `db` to `db-tests` is an
  accepted limitation of the local composition/env model or a Task 0007 merge
  blocker under `specs/repo.md` and `specs/components/00-foundation-and-tooling.md`.
- Decide whether the PR's `agents/orchestrator.md` and `ai/context/**` changes
  are acceptable durable orchestration history for this task or must be removed
  as out-of-scope changes before merge.
- Confirm the root `README.md` is now factually aligned with current repo
  reality: Supabase `stage` and `prod` exist, while Hyperdrive and live
  migration apply do not.
- Run local focused checks plus local Orun verification.
- Inspect successful PR CI logs with `gh` and confirm expected commands actually
  ran.
- Write `ai/reports/task-0007.1-verifier.md`.

# Non-Goals

- No live Supabase schema apply.
- No Cloudflare Hyperdrive Terraform component.
- No Worker database adapter implementation.
- No identity, membership, projects, policy, events, billing, or UI runtime
  behavior.
- No new product-specific `specs-v2/**` work.
- No broad orchestrator or AI-state refactor beyond what is strictly required to
  pass or fail Task 0007.1.

# Constraints

- Trust repo code, PR diff, rendered Orun behavior, GitHub logs, and actual repo
  state over stale notes.
- Do not commit `.orun/**`, `plan.json`, `dist/`, `node_modules/`, TypeScript
  build info files, Terraform working directories, or secrets.
- If verification needs a fix, keep it strictly bounded to Task 0007 scope,
  commit it on the PR branch, push, and wait for CI again before deciding
  PASS/FAIL.
- Do not merge while the PR contains unrelated task-history churn that is not
  justified by the task or while acceptance-critical repo/spec contracts remain
  unresolved.
- Do not treat green CI alone as sufficient evidence.

# Integration Notes

- `dev` has no Supabase project; tests must stay local/offline and must not rely
  on live database credentials.
- Task 0007 establishes migration ownership conventions only. Any live migration
  runner or apply path is a later task.
- The missing `dependsOn` edge is the most likely contract question in this PR.
- The primary follow-up after a successful merge is whichever DB-adjacent seam is
  most blocked in practice, likely a migration runner/apply path or Hyperdrive
  wiring, but that should not be started here.

# Acceptance Criteria

- Local verification passes or exact blockers are reported:
  - `pnpm install`
  - `pnpm --filter @saas/db build`
  - `pnpm --filter @saas/db typecheck`
  - `pnpm --filter @saas/db lint`
  - `pnpm --filter @saas/db-tests test`
  - `pnpm --filter @saas/db-tests typecheck`
  - `pnpm --filter @saas/db-tests lint`
  - `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  - `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json`
  - `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions`
- PR #34 CI run `26220171323` is acceptable and its logs show the expected Orun
  plan/run and package verification commands.
- The PR remains bounded to Task 0007/0007.1 or the report lists concrete scope
  blockers.
- The verifier report clearly states whether the missing `dependsOn` edge is
  acceptable, deferred, or blocking, with evidence.
- If PASS, PR #34 is merged, local `main` is fast-forwarded to `origin/main`,
  and the repo is left clean.
- If FAIL, PR #34 remains open and the report lists concrete blockers.

# Verification

Use the verifier merge protocol from `agents/orchestrator.md`:

- inspect prompt, report, PR diff, local checks, CI logs, and actual repo state;
- verify the PR stays within one bounded DB-harness task;
- check successful GitHub Actions logs, not only summaries;
- merge only on PASS;
- sync local `main` after merge.

# PR Creation Requirement

PR #34 already exists and is the only PR in scope for this verifier task. Do not
open a new PR. If verification requires a small fix, keep it on the existing PR
branch, push the branch, and continue verification there.

# When Done Report

Write `ai/reports/task-0007.1-verifier.md` with:

- Result: PASS or FAIL
- Checks
- Issues
- Risk Notes
- Spec Proposals
- Recommended Next Move
