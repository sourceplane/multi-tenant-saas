# Task ID

Task 0003

# Agent

Implementer

# Current Repo Context

`multi-tenant-saas` main is green at `431cbcc`. Tasks 0001, 0001.1, and 0002
are verified and merged. The repo has a pnpm/Turbo/Orun scaffold, committed
`stack-tectonic/`, Orun-only CI pinned to the current v1.26.0 runtime, and
Terraform placeholders that still reflect the older R2/Cloudflare baseline.

The new spec direction is to follow the `aws-admin` Orun golden path:

- Orun runtime: `ghcr.io/sourceplane/orun:v2.1.0`
- GitHub Action: `sourceplane/orun-action@v1.2.0` with `version: v2.1.0`
- Environments: `dev`, `stage`, `prod`
- Terraform defaults: `parameterDefaults.terraform`
- Terraform component style: `spec.parameters`, colocated README, explicit
  `dependsOn`, `plan-only` by default, `apply` selected by profile rules
- Terraform state target: AWS S3, but live S3 backend consumption is Task 0005

# Objective

Align `multi-tenant-saas` Orun runtime, environment model, Terraform composition
contract, Terraform component manifests, component READMEs, and CI/local
behavior with the `aws-admin` golden path.

# PR Boundary

One PR in `multi-tenant-saas`.

This task updates repo operations structure and non-domain infrastructure
descriptors. It must not create, mutate, import, or destroy live AWS,
Cloudflare, or Supabase resources.

# Read First

- `agents/orchestrator.md`
- `ai/context/current.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `specs/orun-golden-path.md`
- `specs/repo.md`
- `specs/access-and-infra.md`
- `specs/components/00-foundation-and-tooling.md`
- `intent.yaml`
- `kiox.yaml`
- `.github/workflows/ci.yml`
- `stack-tectonic/compositions/terraform/**`
- `infra/terraform/**/component.yaml`
- `infra/terraform/**/README.md`
- `../aws-admin/intent.yaml`
- `../aws-admin/kiox.yaml`
- `../aws-admin/.github/workflows/ci.yaml`
- `../aws-admin/stacks/aws-admin-terraform/**`
- `../aws-admin/domains/**/component.yaml`
- `../aws-admin/domains/**/README.md`

# Required Outcomes

- Update `kiox.yaml` and `kiox.lock` so local execution uses the same Orun
  version as `aws-admin`.
- Update `.github/workflows/ci.yml` to use the same Orun action version and
  plan/run shape as `aws-admin`, preserving an Orun-only workflow.
- Update `intent.yaml` to use `dev`, `stage`, and `prod` environments with
  promotion/defaults shaped like `aws-admin`.
- Keep composition source and binding behavior explicit in `intent.yaml`.
  Inspect the current published-stack setup and the committed `stack-tectonic/`
  directory before choosing whether the repo should bind to the local directory
  or a published source in this PR.
- Bring `stack-tectonic/compositions/terraform/**` in line with
  `../aws-admin/stacks/aws-admin-terraform/**` for:
  - schema naming and `spec.parameters`;
  - `terraformDir` and `terraformVersion` handling;
  - `plan-only` and `apply` profiles;
  - `ORUN_ENV`/`TF_VAR_*` export behavior;
  - `terraform -chdir={{ .parameters.terraformDir }}` usage;
  - README explanation of the contract.
- Update existing Terraform component manifests to the `aws-admin` style:
  `spec.parameters`, labels, env hardening, subscriptions, profile rules, and
  dependencies.
- Remove the temporary duplicated top-level `inputs` compatibility fields from
  existing Terraform components once the repo is fully on the Orun v2
  `parameters` contract.
- Update colocated component READMEs so they follow the `aws-admin` component
  README pattern: metadata, purpose, resources, parameters, outputs,
  dependencies, local verification, and operational notes.
- Preserve a passing local and CI plan while S3 backend consumption is deferred
  to Task 0005. Do not require the not-yet-created `aws-admin` roles in this PR.
- Update `ai/context/*` and write the implementer report.

# Non-Goals

- No AWS IAM component creation. That is Task 0004 in `aws-admin`.
- No migration from R2 to S3 backend usage. That is Task 0005.
- No Supabase project/database creation. That is Task 0006.
- No Cloudflare Hyperdrive creation or import.
- No domain code, database migrations, UI, API, identity, billing, audit, or
  product behavior.
- No product-specific `specs-v2/**` work.

# Constraints

- Trust code and rendered Orun plans over stale docs.
- Do not commit `.orun/**`, `plan.json`, Terraform state, `.terraform/**`, or
  generated caches.
- Do not add direct Terraform, Wrangler, Supabase, AWS, pnpm, or turbo CI jobs
  outside Orun.
- If the current Stack Tectonic source choice conflicts with the specs, make
  the smallest coherent migration and document the plan impact.
- If local `kiox` behavior differs from GitHub Actions behavior, fix the
  Orun intent/composition/CI contract instead of hiding the difference in shell.

# Integration Notes

- The user phrase `multi-tenant-saas/agents/stack-tectonic` appears to refer to
  the committed `multi-tenant-saas/stack-tectonic/` directory. Do not create a
  new `agents/stack-tectonic` path unless repo reality proves that path exists.
- Existing historical R2/Hyperdrive Terraform files are allowed to remain only
  as migration targets. Update docs so future agents know they are not the new
  backend direction.
- AWS-admin's Terraform composition currently initializes S3 state. If adopting
  that exact behavior would break this PR before Task 0004 creates roles, stage
  the behavior explicitly and document the Task 0005 flip.

# Acceptance Criteria

- `kiox.yaml`, `kiox.lock`, and CI use Orun v2.1.0 / `orun-action@v1.2.0`.
- `intent.yaml` has `dev`, `stage`, `prod` environments and Terraform defaults
  aligned with `aws-admin`.
- Terraform composition docs/schema/profiles use the same mental model and
  naming as `aws-admin`.
- Existing Terraform component manifests use `spec.parameters` and `plan-only`
  plus `apply` profile rules.
- Component READMEs follow the `aws-admin` README style.
- `/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml`
  passes.
- `/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent
  intent.yaml --output plan.json` passes.
- `/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run
  --runner github-actions` passes or records a true no-op if no jobs are
  planned.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` still pass unless
  an unrelated external blocker is documented with exact output.
- PR CI passes and logs show Orun plan/run behavior, not direct task execution.

# Verification

The verifier should inspect the diff, rendered Orun plan, component manifests,
Terraform composition files, README updates, and GitHub Actions logs. The
verifier must confirm this PR did not create or mutate live resources and did
not smuggle S3/backend role consumption ahead of Task 0004/0005.

# When Done Report

Write `ai/reports/task-0003-implementer.md` with:

- Summary
- Files Changed
- Orun Plan Impact
- Checks Run
- Assumptions
- Spec Proposals
- Remaining Gaps
- Next Task Dependencies
- PR Number
