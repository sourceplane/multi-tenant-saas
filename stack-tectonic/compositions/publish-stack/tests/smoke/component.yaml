apiVersion: sourceplane.io/v1
kind: Component

metadata:
  name: stack-tectonic-smoke

spec:
  type: publish-stack
  domain: platform-stack
  subscribe:
    environments:
      - name: development
        profile: dry-run
  inputs:
    registry: ghcr.io/sourceplane/stack-tectonic
    root: .
    version: "0.0.0-smoke"
  labels:
    layer: platform
    provider: orun
