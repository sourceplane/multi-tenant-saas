apiVersion: sourceplane.io/v1
kind: Component
metadata:
  name: infra-terraform-state
spec:
  type: turbo-package
  domain: starter-infra
  subscribe:
    environments:
      - dev
  inputs:
    nodeVersion: "20"
    pnpmVersion: "10.12.1"
  labels:
    layer: infra
    provider: terraform
